# Receipt — adhoc

- Date: 2026-08-02
- Slug: adhoc
- Status: shipped

## Actions taken

- [x] Renamed the layout-density menu labels from **Chunky / Slim** to **Desktop / Mobile** (`ThemeSection.tsx`); internal stored values (`"chunky"` / `"slim"`) and CSS attribute selectors are unchanged, so existing per-browser preferences are preserved.
- [x] Changed the default layout density from `"chunky"` (Desktop) to `"slim"` (Mobile) (`layoutDensity.ts`); this only affects browsers with no stored preference — existing explicit selections are unaffected.
- [x] Removed the `trade-balancer` entry from `PORTAL_DESTINATIONS` (`destinationRegistry.tsx`) so **Trade** no longer appears in the feature-portal menu. `TradeBalancerPlaceholder.tsx` is untouched and the entry can be re-added when `card-trade-balancer` ships.
- [x] Updated tests to match: `ThemeSection.test.tsx`, `App.layout-density.test.tsx`, `layoutDensityPrefs.test.ts`, `FeaturePortalMenu.test.tsx`, `destinationRegistry.test.tsx`. Two `FeaturePortalMenu.test.tsx` integration tests that exercised the real app's Trade destination were re-pointed at Quick Lookup (the only other real destination without vs. with a header slot changed as a result — both remaining destinations render a header slot, so the "no header slot" fixed-tab-fallback path is now covered only by the generic `FeaturePortalMenu` harness tests, not a real destination).
- [x] Ran ship gate `npm run quality:check` (typecheck, lint, format:check, frontend + backend coverage:check, which runs the full test suites) — all green.
- [x] Promoted durable outcomes: amended DEC-075 (`decisions/personalization.md`) and REQ-055 (`functional-requirements.md`) for the new labels/default; updated the **Feature portal** and **Trade balancer** entries in `system-map.md` to reflect the current registered-destinations list and the density default. No `DEC`/`REQ` `Status:` fields were edited.
- [x] Reviewed the working-tree diff for whitespace errors and secrets; none found.
- [x] Deleted `PRD/work/adhoc/`.
- [x] Follow-up (same day, post-user-testing): relabeled the two remaining feature-portal menu destinations from **MTG Assistant** / **Quick Lookup** to **In-Depth Question** / **Quick Question** (`destinationRegistry.tsx`), chosen through discussion with the user to lead with each flow's depth/effort rather than internal naming. Destination ids (`mtg-assistant`, `quick-lookup`), component names, and file paths are unchanged. Also renamed the matching in-flow step header in `QuickLookupApp.tsx` (`stepName="Quick Lookup"` → `"Quick Question"`) so the screen title matches the menu entry that leads to it; the `MtgAssistantApp` flow has no single equivalent step name to rename (it uses per-step names like "Game context"). Left the static "MTG Assistant" subtitle in `StagedStepHeader.tsx` untouched — it's a shared branding line rendered on every staged screen (both destinations), not tied to either menu label, and wasn't part of what was discussed.
- [x] Updated tests for the relabel: `destinationRegistry.test.tsx`, `FeaturePortalMenu.test.tsx` (Chrome-integration block only — the generic `FeaturePortalMenu` harness tests use synthetic fixture labels, untouched).
- [x] Re-ran `npm run typecheck`, `npm run lint`, and the frontend test suite — all green.
- [x] Promoted the relabel into `system-map.md`'s Feature portal entry.

## Files created

- `PRD/instructions/receipts/adhoc-2026-08-02.md`

## Files updated

- `apps/frontend/src/lib/theme/layoutDensity.ts` (default density `"chunky"` → `"slim"`)
- `apps/frontend/src/components/portal/ThemeSection.tsx` (density option labels `Chunky`/`Slim` → `Desktop`/`Mobile`)
- `apps/frontend/src/components/portal/destinationRegistry.tsx` (removed `trade-balancer` entry; added a comment pointing back at `TradeBalancerPlaceholder.tsx`; relabeled `mtg-assistant` → "In-Depth Question" and `quick-lookup` → "Quick Question")
- `apps/frontend/src/components/portal/quick-lookup/QuickLookupApp.tsx` (`stepName` "Quick Lookup" → "Quick Question")
- `apps/frontend/src/components/portal/ThemeSection.test.tsx`
- `apps/frontend/src/App.layout-density.test.tsx`
- `apps/frontend/src/lib/theme/layoutDensityPrefs.test.ts`
- `apps/frontend/src/components/portal/FeaturePortalMenu.test.tsx`
- `apps/frontend/src/components/portal/destinationRegistry.test.tsx`
- `PRD/sections/decisions/personalization.md` (DEC-075: Desktop/Mobile labels, Mobile default)
- `PRD/sections/functional-requirements.md` (REQ-055: Desktop/Mobile labels, Mobile default)
- `PRD/sections/system-map.md` (Feature portal: registered-destinations list + labels + density default; Trade balancer: destination currently unregistered)

## Files deleted

- `PRD/work/adhoc/adhoc.md`

## Verification results

- `npm run quality:check` — passed: typecheck (frontend + backend) clean, lint clean, format:check clean.
- Frontend tests: 71 files, 627 tests passed (via `test:coverage`, then re-confirmed via plain `test` run after the relabel follow-up).
- Backend tests: 23 files, 251 tests passed (via `test:coverage`) — unaffected by this change, run for completeness.
- No live-browser/manual visual verification performed in this session — `chromium-cli` and Playwright are not installed in this environment, and installing a browser binary was judged disproportionate for text-label and array-entry changes already covered by component tests that render real DOM and assert on the rendered button/heading text.
