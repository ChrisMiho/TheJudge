# Receipt — scan-spec

- **Date:** 2026-08-26
- **Slug:** `scan-spec`
- **Status:** shipped
- **Type:** documentation only — the DEC-168 current-state feature-spec
  layer's fourth instance (Phase A #4, docs-refactor gameplan). Scan is the
  gameplan's first cross-cutting spec (shared by In-Depth, Quick Lookup,
  Trade Balancer). No `apps/` code, no backend route change, no UI behavior
  change, and no shipped Card Scanning behavior change.

## Actions taken

- [x] Slice A (verify-only) — verified the already-committed
      `PRD/sections/scan/README.md`, `PRD/sections/scan/data/cardhashes.md`,
      and `PRD/sections/scan/data/cardScanMap.md` (written and
      owner-accepted at the `define` gate, commit `7cd4f41`) against their
      cited sources, the DEC-168 template, and the two committed data
      artifacts. Made two bounded, sourced corrections during `build`:
      `cardhashes.md`'s `~14 MB` → `~13 MB` (the committed
      `cardhashes.bin` is 13,047,744 bytes), and removed a stray `~67px`
      card-back figure that belongs to Life Tracker (REQ-112/DEC-136), not
      DEC-055.
- [x] Slice B (verify-only) — verified the `PRD/README.md` Section
      Inventory row for `sections/scan/` and proved the package-wide diff
      since the fork point touched nothing outside the licensed set.
- [x] All 17 acceptance criteria (`slice-a.criteria.json` A1–A12,
      `slice-b.criteria.json` B1–B5) confirmed `true` in the criteria
      files, independently re-confirmed by the node 7 no-write reviewer:
      verdict **APPROVE**, 0 Critical, 0 Important, 1 Minor (recorded as a
      follow-up below).
- [x] PR #112 (base `thejudge-auto/scan-spec`, head
      `thejudge-auto/scan-spec-work`) merged by the owner
      2026-08-26T05:16:43Z, merge commit `f42662e`. Follow-up PR #113
      (same base, head `thejudge-auto/scan-spec-followup`) merged
      2026-08-26T16:50:05Z, merge commit `8e05bb2`, reconciling the
      driver's node-6/node-7 local ledger commits onto the base the first
      merge left them behind (README/STATUS/board resolved to
      `ship-ready`).
- [x] Durable promotion: none required at cleanup. All three deliverables
      (`PRD/sections/scan/README.md`, the two `data/` docs, the
      `PRD/README.md` row) were already committed to the recorded
      autonomous base by the `define` gate and the two bounded `build`
      corrections, owner-accepted at the `define` gate (16/16 items
      accepted). No new stable IDs were minted and no existing DEC/REQ/
      FLOW/NFR body was modified, so no `decisions.md` promotion applies —
      confirmed still present on the merged base at cleanup time.
- [x] System-map promotion gate: no flip required.
      `PRD/sections/system-map.md`'s `## Card scanning` entry (line 274)
      already reads `Status: shipped` from prior shipped feature work;
      this package did not change shipped product behavior, only added a
      derived documentation view plus its per-subsystem breakdown
      (Identification core, Fingerprint library build, Scan-to-metadata
      resolver, etc. — all already `Status: shipped`, unchanged).
- [x] `PRD/work/scan-spec/GRAPH-RUN.md`'s `## Node ledger` and
      `## Instruction ledger` folded verbatim into `## Graph run` below,
      before the package folder was deleted, per this run's node-9
      requirement.
- [x] `intake/refactor-gameplan.md` recorded under `## Intake` below,
      before the package folder was deleted.
- [x] Autonomous merge-proof gate — all four checks satisfied; see
      `## Merge-proof gate` below for the full evidence and verdict on
      each.

## Merge-proof gate

1. **Current branch equals recorded base.** `git branch --show-current` →
   `thejudge-auto/scan-spec`, matching `README.md`'s
   `Autonomous base: origin/thejudge-auto/scan-spec` exactly. **Met.**
2. **PR merged into the recorded base, verified via `gh`.**
   `gh pr view 112 --json state,baseRefName,mergedAt,mergeCommit` →
   `state: MERGED`, `baseRefName: thejudge-auto/scan-spec`,
   `mergedAt: 2026-08-26T05:16:43Z`, `mergeCommit.oid: f42662e742b6…`.
   `gh pr view 113` (the follow-up) → `state: MERGED`, same base,
   `mergeCommit.oid: 8e05bb236fa1…`. The GitHub API was reachable, so
   `gh` stays authoritative. **Met.**
3. **Worktree fully merged.** `.worktrees/implement-scan-spec` is on
   branch `implement-scan-spec-contributor`, tip `43c79c3`;
   `git merge-base --is-ancestor 43c79c3 HEAD` succeeds — the worktree's
   tip is an ancestor of the current base tip (`8e05bb2`).
   `git -C .worktrees/implement-scan-spec status --porcelain` is empty.
   **Met.**
4. **Runtime-cleanup criteria.** Confirmed against both slice evidence
   files and `GAMEPLAN.md` rather than assumed: neither
   `slice-a.evidence.md` nor `slice-b.evidence.md` mentions a browser, a
   dev server, or a port (the only keyword hits were false positives —
   `doc-process.md` and file names containing "portal"). `GAMEPLAN.md`'s
   `## Runtime / browser risk` section states "None. This package is
   documentation-only... No Playwright verification is required."
   **Met — vacuously, no runtime session was ever opened.**

## Files created

- `PRD/instructions/receipts/scan-spec-2026-08-26.md` (this file)

## Files updated

- `PRD/work/STATUS.md` — removed the `scan-spec` row from the
  `## ship-ready` section

## Files deleted

- `PRD/work/scan-spec/` (entire work folder): `README.md`, `IDEA.md`,
  `DESIGN-BRIEF.md`, `GAMEPLAN.md`, `GRAPH-RUN.md`, `STATUS.ship-ready`,
  `slice-a-verify-spec.md` + `slice-a.criteria.json` +
  `slice-a.evidence.md`, `slice-b-diff-proof.md` + `slice-b.criteria.json`
  + `slice-b.evidence.md`, `intake/refactor-gameplan.md`
- `.worktrees/implement-scan-spec/` (autonomous implementation worktree,
  clean and fully merged per merge-proof check 3) and its local branch
  `implement-scan-spec-contributor`
- Local branch `thejudge-auto/scan-spec-followup` (the PR-#113 head,
  fully merged into `thejudge-auto/scan-spec` per `git branch --merged`;
  its remote counterpart was already gone —
  `git fetch origin --prune` reported it `[deleted]`, auto-deleted by
  GitHub on merge)

## Durable outcomes already shipped (present on the base prior to this node)

- `PRD/sections/scan/README.md` — the DEC-168 current-state feature spec
  for Card Scanning, the gameplan's first cross-cutting spec (In-Depth,
  Quick Lookup, Trade Balancer).
- `PRD/sections/scan/data/cardhashes.md` and
  `PRD/sections/scan/data/cardScanMap.md` — the two corpus docs, each
  passing the `data/` bucket's four-clause test (external source, build
  command, committed artifact, Magic-not-TheJudge content).
- `PRD/README.md` — one Section Inventory row for `sections/scan/`.
- `PRD/sections/system-map.md`'s `## Card scanning` entry and its
  subsystem breakdown — already `Status: shipped` from prior, unrelated
  shipped work; unchanged by this package.

## Verification results

- `npm run quality:check` — exit 0. `test:scripts`: 402/402 tests
  passing, 0 failures.
- `git branch --show-current` → `thejudge-auto/scan-spec` (matches the
  recorded autonomous base).
- `gh pr view 112 --json state,baseRefName,mergedAt,mergeCommit` →
  `MERGED`, base `thejudge-auto/scan-spec`, merge `f42662e`.
  `gh pr view 113` → `MERGED`, same base, merge `8e05bb2`.
- `git merge-base --is-ancestor f42662e HEAD` → success.
  `git merge-base --is-ancestor 43c79c3 HEAD` → success (worktree tip is
  an ancestor of the merged base tip).
- `git -C .worktrees/implement-scan-spec status --porcelain` → empty.
- `python3` check on both criteria files: `slice-a.criteria.json` 12/12
  `value: true`; `slice-b.criteria.json` 5/5 `value: true`.
- `git branch --merged thejudge-auto/scan-spec` includes
  `implement-scan-spec-contributor` and `thejudge-auto/scan-spec-followup`
  — both fully merged, safe to delete locally.

## Follow-ups (node 7 reviewer, rated Minor, not fixed by this node)

1. **Stale grep in `slice-a.criteria.json` A4's evidence command.** The
   reviewer noted the recorded evidence command for A4 no longer matches
   the current file shape, but independently confirmed the underlying
   deliverable is correct. No action needed — the criterion's `value`
   reflects the correct, re-verified state, only its evidence-command
   string is stale.

## Known defects (contributing causes of this run, not fixed here)

1. `thejudge-implement-all/SKILL.md:36` derives the shared remote branch
   as `thejudge-auto/<slug>` — the same name as the recorded autonomous
   base. On this run that collided with itself again: node 6 (`build`)
   forked `thejudge-auto/scan-spec-work` to hold the deliverable and
   ledger commits. This is the same recurring pattern already recorded in
   `user-feedback-spec-2026-08-25.md`, `life-tracker-spec-2026-08-25.md`,
   and `codebase-duplication-audit-2026-08-23.md`'s receipts. The fix
   belongs to an ordinary session — a graph run may not edit a
   `thejudge-*` skill, so it is recorded here rather than patched in
   flight.
2. Merging PR #112 left the base ahead of the driver's local node-6/
   node-7 ledger commits (kept local while the base was frozen behind the
   open PR). Unlike the prior receipts' in-flight conflict resolution,
   this run resolved it after the fact via a second, follow-up PR (#113,
   branch `thejudge-auto/scan-spec-followup`) rather than during the
   original PR's lifetime. No product deliverable was lost — the
   reconciliation was confined to `PRD/work/scan-spec/README.md`,
   `STATUS.*`, and the board row. Recorded here, not fixed, for the same
   reason as defect 1.

## Graph run

- Run ID: `graph-20260825-212621` | Profile: `unverified` | Terminal state: `close (node 9, this receipt)`

### Node ledger

| # | Node | Model | Outcome | Heartbeat | Evidence | Date |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | preflight | haiku | ok | `0 → 5` | branch `thejudge-auto/scan-spec` pushed to origin (`git ls-remote` = 0d7b59d); lock taken (pid 27180); clean tree, no stash; universal canary denied (`rm -rf`), graph canary denied (`nohup` while lock held) | 2026-08-25 |
| 2 | shape | sonnet | ok | `0 → 35` | package `PRD/work/scan-spec/` created (IDEA.md, README.md, STATUS.ideation, intake/refactor-gameplan.md byte-identical); STATUS.md board row added; committed `91cd0ec`, pushed | 2026-08-25 |
| 3 | define | opus | ok | `0 → 41` | spec authored `PRD/sections/scan/README.md` (337 lines) + `PRD/sections/scan/data/{cardhashes,cardScanMap}.md` + `PRD/README.md` nav row + `DESIGN-BRIEF.md`; zero new stable IDs (derived view over existing DEC/REQ/FLOW/NFR); STATUS.refined; non-empty `PRD/sections/` diff → parks at define gate | 2026-08-25 |
| — | define-gate | (owner) | resolved | — | `graph-gate-review` walked the diff — 16/16 items accepted, 0 edits, 0 rejects; STATUS restored to refined; committed `5f3eeee` | 2026-08-25 |
| 4 | gate-qc | sonnet | ok | `0 → 15` | quality-check **PASS**, findings none; `DESIGN-BRIEF.md` validated against the accepted spec, corpus docs, and nav row; STATUS.refined unchanged. Resume re-proof: lock re-taken (pid 32334), graph canary denied (`nohup`) | 2026-08-25 |
| 5 | plan | sonnet | ok | `0 → 61` | GAMEPLAN + 2 verify-only slices — A (12 criteria): spec + 2 corpus docs vs. cited sources/DEC-168/committed artifacts; B (5 criteria): nav row + package diff-scope proof. Flagged bounded correction for slice A: `cardhashes.md` `~14 MB` → `~13 MB` (bin is 13,047,744 bytes). Runtime/browser risk none. STATUS.active; writes all inside `PRD/work/scan-spec/` + board | 2026-08-25 |
| 6 | build | sonnet | ok | `0 → 146` | worktree `.worktrees/implement-scan-spec`, head `thejudge-auto/scan-spec-work`; slice A **12/12** + slice B **5/5** criteria true (verified in worktree criteria files); 2 bounded corrections (`~14 MB`→`~13 MB`; removed a stray `~67px` card-back figure that belongs to Life Tracker, not DEC-055); **PR #112** OPEN (base `thejudge-auto/scan-spec` ← head `thejudge-auto/scan-spec-work`); STATUS.ship-ready. Write-scope: launch checkout byte-unchanged (`git status --porcelain` empty), all writes in worktree | 2026-08-25 |
| 7 | review | opus | ok | `0 → 10` | no-write reviewer (`Plan` agent, no Edit/Write): **APPROVE**, 0 Critical, 0 Important. A1–A12 + B1–B5 all independently confirmed against the artifacts; both bounded corrections verified correct and in-scope (bin = 13,047,744 bytes; `~67px` is a Life Tracker bound, REQ-112/DEC-136, not DEC-055). 1 Minor note (stale grep in `slice-a.criteria.json` A4 evidence — deliverable correct) — no loop | 2026-08-25 |
| 8 | land | (owner) | ok | — | **PR #112 merged** by owner (merge commit `f42662e`, base `thejudge-auto/scan-spec`). Head `thejudge-auto/scan-spec-work` auto-deleted on merge. Driver's node-6/node-7 local ledger commits reconciled onto the merged base via follow-up branch `thejudge-auto/scan-spec-followup` (README/STATUS/board conflicts resolved to ship-ready) | 2026-08-25 |

**Node 9's row was written by the driver after the fact, not by the run.**
The run cannot record its own final node: node 9 deletes
`PRD/work/<slug>/`, and `GRAPH-RUN.md` lives inside it, so there is no
ledger left to write the `close` row into. This is the same structural gap
`user-feedback-spec-2026-08-25.md`'s and prior receipts already record —
recorded here rather than worked around.

### Instruction ledger

| Instruction | Class | Node | Rule |
| --- | --- | --- | --- |
| Write the current-state feature spec for the scan feature — Phase A #4 of the docs-refactor gameplan. Land it at PRD/sections/scan/README.md on the DEC-168 template. Scan is cross-cutting — referenced by multiple destinations — so capture how it feeds each one, not just its own screen. Keep it draft and non-authoritative. | answered-once | shape | — |
| current-state feature spec | answered-once | shape | — |
| Backing sources | answered-once | define | — |
| write the spec | answered-once | plan | — |

## Intake

- `intake/refactor-gameplan.md` — staged docs-refactor gameplan, copied
  verbatim from `.worktrees/.graph-intake/graph-20260825-212621/`, per
  `GRAPH-RUN.md`'s node 2 dispatch prompt. Not opened by this node —
  recorded as a citation only, per the graph-workflow-contract's intake
  rule.
