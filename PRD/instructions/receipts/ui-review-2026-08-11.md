# Receipt — ui-review

- Date: 2026-08-11
- Slug: `ui-review`
- Status: shipped

## Actions taken

- [x] Verified all eight slices A–H are `done` and the package carried `status: ship-ready` + `STATUS.ship-ready`.
- [x] Confirmed product code for every slice (see **Shipped behavior** below).
- [x] Promoted the six durable corrections slice H identified into `sections/` (details below).
- [x] Updated `system-map.md` — Shared card presentation, Adaptive context overlay, Quick Lookup, and Trade balancer entries now describe shipped reality; all four were already `shipped` and stay so.
- [x] Wrote this receipt **before** deleting the work folder.
- [x] Deleted `PRD/work/ui-review/` and removed the slug from `PRD/work/STATUS.md`.
- [x] Cleanup ran on the implementation branch and shipped through PR #88 rather than after its merge — the operator explicitly ordered that sequence. See **Gate deviation**.

## Shipped behavior

Slices A–C shipped earlier via PR #86 (`feature/routing` → `main`): one shared
themed overlay close control and outside-dismiss hook across six overlays, the
portal-hosted card detail bottom sheet / side panel, and container-relative card
sizing on all six card surfaces.

Slices D–H shipped via PR #88 (`thejudge-auto/ui-review` → `main`):

- **D** — `QuickLookupApp`'s counter, `maxLength`, and submit gate all read the raw
  textarea. Live submission then returned `400 VALIDATION_ERROR` because
  `questionSchema` was `boundedText(300, 0)`; the wire bound is now 600.
- **E** — one shared 20×20 inline-SVG triangle disclosure (rotated when expanded,
  unboxed, ≥44px hit area) in `PlayerRosterEditor`; one grouped row pattern with an
  8px label→input gap across commander-damage, named-counter, and scalar rows;
  poison/energy/experience as stacked 78px selects with explicit `Unset` and fixed
  0–11 / 0–100 / 0–100 ranges, preserving out-of-range seeded values.
- **F** — `.portal-menu-rail` is `position: relative`, giving the rail a real 44px
  in-flow footprint; `.adaptive-context-trigger`'s compensating `margin-top` is gone.
- **G** — Trade Balancer renders `Prices as of 5 June 2026` from the ISO snapshot,
  omitting the line entirely when the value is unparseable.
- **H** — integrated sweep at 390×844 and 1440×900 found no regression; no product
  file needed an integration fix.

## Durable corrections promoted

1. **REQ-134 / REQ-091** — the "no downstream limit is at risk (`MAX_PROMPT_CHAR_BUDGET`
   is 1,000,000, DEC-042)" reasoning was wrong: it skipped the request schema's own
   bound. Both entries now record the measured 400 and the shipped resolution — raw cap
   stays 300, **wire bound is 600** — and REQ-091's constraint block notes the one Zod
   change. The "do not reintroduce a composed-length cap" instruction is retained.
2. **`user-flows.md`** — fixed compact-image wording replaced with container-relative
   sizing (DEC-160) on zone collection, enrichment, the zone strip, the scan-review
   panel, and the mobile add-action edge case; the Quick Question flow no longer says
   the 300-character cap measures the composed pill+textarea string.
3. **`screen-layout.md` — Quick Question pre-submit** — card share re-measured on ship
   at 45.3% (151px of a 333px column); REQ-141's "clear majority" is explicitly recorded
   as **not met** on that surface, bounded by REQ-129's Fit rule, with the note that
   closing it needs a change to the surrounding column rather than to the cap.
4. **`screen-layout.md` — Quick Question answered workspace** — new **Rail clearance**
   row: the compensating constant is retired and spacing is `--layout-surface-gap`
   (8px phone / 16px desktop), with an explicit "do not reintroduce" instruction.
5. **`screen-layout.md` — In-Depth Game context** — new **Player-detail controls** row
   recording the shipped triangle, grouped row, and select shapes with measurements.
6. **`screen-layout.md` — Trade Balancer** — new **Price freshness** row recording the
   date-level copy, the one-line measurement, and the unparseable-value fallback.

## Files created

- `PRD/instructions/receipts/ui-review-2026-08-11.md`

## Files updated

- `PRD/sections/functional-requirements.md` (REQ-091, REQ-134)
- `PRD/sections/user-flows.md` (FLOW-001, FLOW-002, FLOW-005/006 wording, Quick Question flow)
- `PRD/sections/screen-layout.md` (Quick Question pre-submit / answered workspace, In-Depth Game context, Trade Balancer)
- `PRD/sections/system-map.md` (Shared card presentation, Adaptive context overlay, Quick Lookup, Trade balancer)
- `PRD/work/STATUS.md`

## Files deleted

- `PRD/work/ui-review/` (README, GAMEPLAN, DESIGN-BRIEF, IDEA, braindump, eight slice docs, mockups, `STATUS.ship-ready`, and the disposable `.playwright-mcp/` captures)

## Verification results

- `npm run quality:check` green on the cleanup tree (typecheck, lint, format:check, coverage thresholds, script tests).
- Slice gates before cleanup: frontend 1298 tests, backend 273 tests, production build, all green; CI on PR #88 green across backend, three frontend shards, static, and coverage-merge.
- Runtime cleanup evidence recorded on every browser-verified slice: `browser_close`
  called, owned dev servers stopped by their exact `node scripts/dev.mjs` manager PID,
  ports `5183`/`3111` confirmed released by `lsof`.
- **Recorded hygiene violation (slice D):** a `pkill -f "node scripts/dev.mjs"` pattern
  also matched the operator's own dev servers on `5173`/`3000`, contrary to
  `runtime-process-hygiene.md`'s broad-kill prohibition. Reported at the time; every
  later slice stopped servers by exact PID.

## Carried forward (not resolved by this package)

- **REQ-141 is not met on Quick Question** (45.3% of the content column at 390×844).
  Recorded on that `screen-layout.md` row rather than closed. Needs a layout change to
  the surrounding Quick Question column.
- **Scan review has never been verified live.** The client-side perceptual-hash
  identifier does not converge on Chrome's synthetic fake-camera pattern and the
  Playwright MCP server exposes no `--use-file-for-fake-video-capture` control.
  Covered by `ScanReviewBubble.test.tsx` plus the shared component rule measured live on
  the other five surfaces. Re-attempt only if a real-camera path becomes available.
- **The card-detail trigger lives on the image branch** of `CardPresentation`, so the
  missing-image fallback renders the card name and Remove but no detail trigger. Shipped
  structure from slice B/C, noted here in case the catalog should state it explicitly.

## Gate deviation

`thejudge-cleanup`'s autonomous merge-proof gate expects cleanup to run on the recorded
autonomous base (`main`) **after** the implementation PR merges. The operator explicitly
ordered cleanup first, pushed onto PR #88, with the merge following. Checks 1 and 2
(current branch equals the recorded base; PR already merged) were therefore knowingly
skipped; checks 3 and 4 (clean fully-merged worktree, runtime-cleanup evidence passing)
were satisfied. The end state is equivalent — the work folder's deletion reaches `main`
only through the same merge that carries the implementation — and nothing is lost if the
merge is delayed, because the branch retains every artifact until then.
