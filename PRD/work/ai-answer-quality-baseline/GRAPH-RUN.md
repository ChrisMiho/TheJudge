# Graph run — ai-answer-quality-baseline

- Run ID: `graph-20260907-032104` (build half, `/loop graph-implement`, claimed 2026-09-07 UTC; spec-forming half `graph-20260906-092312`, rows 1–4 below)
- Profile: `loaded (env sentinel)` (spec-forming half: observed by the preflight script at node 1; build half: `THEJUDGE_GRAPH_PROFILE=1` observed in the driver's shell at the claim, and `ps` shows pid 2370 launched as `claude --settings .claude/graph-profile.json` — proof the file loaded, not that any rule fired)
- Canary: `denied — hook live (rm -rf .worktrees/.graph-canary-nonexistent → "[graph-boundary] rm -rf is denied in every session."; graph tier: nohup true → "[graph-boundary] nohup is denied while a graph run holds the lock: a detached command outlives the run that started it.")` — build half, at the claim: graph tier `nohup true` → same deny text, row `graph-20260907-032104/driver-bookkeeping` in `.worktrees/.graph-denials.jsonl` at 2026-09-07T03:22:52Z
- Autonomous base: `origin/main` (rewritten by the build half's claim; the spec-forming half's base `origin/thejudge-auto/ai-answer-quality-baseline` merged to `main` as docs PR #200 on 2026-09-06)
- Worktree: `/Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline` on `thejudge-auto/ai-answer-quality-baseline-work`, cut from `origin/main` at `787ca5f` (the spec-forming half ran in the launch checkout, before REQ-191 landed)
- Staging: `.worktrees/.graph-intake/graph-20260906-092312/` (IDEA.md, README.md — copied verbatim from `PRD/ideasForLater/ai-answer-quality-baseline/`; `answer-quality-context.md` — the driver's measurement note from the hybrid-rule-retrieval session)
- Current node: `build` (attempt 3 — review loop 1 of 2) — the reviewer returned three Important findings (ranking rationale, ranking rubric, system-map placement); build fixes them, then `review` (attempt 2), then `close`
- Next action: none from the owner until the run ends — `COMPLETE` leaves PR #203 open for the owner's merge

## Node ledger

| # | Node | Model | Outcome | Heartbeat | Evidence | Date |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | preflight | haiku | ok | `0 → 7` | classification `clean`; branch `thejudge-auto/ai-answer-quality-baseline` created from `main` (`8d139aa`, tree clean, no stash) and pushed (`git ls-remote` confirms); lock `.worktrees/.graph-run.lock` runId `graph-20260906-092312` pid 77812 (the driver session); both canaries denied (2 rows in `.worktrees/.graph-denials.jsonl` for this run); `Profile: loaded (env sentinel)`; base→main guard passed (no open `thejudge-auto/*` PR); 10 self-reported calls, no subagents | 2026-09-06 |
| 2 | shape | sonnet | ok | `0 → 33` | package `PRD/work/ai-answer-quality-baseline/` created (`IDEA.md` with prior-run lines naming 11 receipts, `README.md` `status: ideation`, `STATUS.ideation`, board row under `## ideation`); 3 intake files + `MANIFEST.md` copied verbatim into `intake/` (`cmp` identical ×3, driver re-listed), staging `.worktrees/.graph-intake/graph-20260906-092312/` emptied; parked idea retired (`git rm -r PRD/ideasForLater/ai-answer-quality-baseline/`, its `## parked in ideasForLater` row removed); commit `c66faf4` pushed to `origin/thejudge-auto/ai-answer-quality-baseline`; tree clean; 20 self-reported calls, no subagents, 0 new denials | 2026-09-06 |
| — | driver-bookkeeping | — | ok | `n/a (driver)` | ledger moved into the package from the driver's scratchpad; `## Autonomous metadata` (`origin/thejudge-auto/ai-answer-quality-baseline`) appended to the package README; committed with the node 3 dispatch prompt recorded and pushed | 2026-09-06 |
| 3 | define | opus | ok | `0 → 53` | `STATUS.refined`, README `status: refined`, board row under `## refined`; `DESIGN-BRIEF.md` (326 lines: scope, 10 assumptions with evidence and ladder rung, measurement plan, non-goals) + `GATE-QUESTIONS.md` (584 lines, 10 slots all blank: 6 new REQ-185..190 — gold set, three-layer judge with the published worked solution as reference, four 0–2 axes, `npm run eval:answer-quality` behind `--confirm-live-calls` and never in a gate script, committed numeric scorecard with gitignored transcripts, excerpt cap as a run parameter; 4 amended — NFR-018, `system-map.md` `## Eval harness`, `goals-and-non-goals.md` non-goal, REQ-146; 0 blocker questions; REQ-032 deliberately not amended, assumption A10). Offline measurements, no live call: 6 worked-solution cases all with a published answer, 31 fixtures / 10 labelled / 0 with an answer key; prompt size mean 10,754 chars over the six cases on the real hybrid path (`usedSemantic` true); cost estimate ≈ $0.04 (mini) / $0.19 (gpt-4.1) per two-leg run, labelled estimate; cap 5 is a hard-coded literal at four `preparation.ts` sites, `runnerUp` already returns ranks 6–15, expected rule in top 5 for 6/6 and unchanged at 6/8/10/15 — so the cap experiment measures distraction, not recall. No `PRD/sections/` or code edits (`git diff --stat` empty). Gate signal present (`GATE-QUESTIONS.md`) → continue to `gate-qc`; 48 self-reported calls, no subagents, 0 new denials; commit `4719033` pushed | 2026-09-06 |
| 4 | gate-qc | sonnet | ok | `0 → 44` | PASS, no findings: 4/4 `Current:` excerpts byte-identical by script (NFR-018, `system-map.md` `## Eval harness`, `goals-and-non-goals.md:83`, REQ-146 lines); REQ-185–190 unused, next free after REQ-184; heading hygiene clean (11 top-level headings = 10 stable-id blocks + `## Blocker questions`; the two `## Eval harness` excerpts are inside ```` ```markdown ```` fences); 12-term amendment-set re-grep leaves no uncovered assertion; measurements reproduced once each — 6 cases, 31/10/0 fixtures, four cap-of-5 call sites (the brief cites the call-start lines 223/267/313/351, the literal sits 4–5 lines below each; not scope-affecting), `eval:worked-solutions` 6/6 under `EMBEDDING_PROVIDER=local`; prompt-size mean skipped (brief names no runnable command); cost labelled an estimate, no numeric target; default model `gpt-4.1-mini` confirmed; artifact home and `.gitignore` claims confirmed; `STATUS.refined` unchanged, nothing committed; 44 tool calls, no subagents, 0 new denials. Driver wrote `## Preparation gate` PASS to the README → stop at PASS: docs PR + `owner-action` park | 2026-09-06 |
| — | driver-claim | — | ok | `n/a (driver)` | build half (`/loop graph-implement`, tick 1): `git fetch origin`; no stop sentinel; no lock held; ready-spec scan of `origin/main` found `ai-answer-quality-baseline` at `STATUS.refined` with 10/10 `- Verdict:` slots answered (6 edit / 4 accept, already applied by `graph-gate-review` on 2026-09-06 — `## Gate verdicts` present), docs PR #200 MERGED 2026-09-06T18:00:07Z, no `-work` branch locally or on `origin`, no `.worktrees/implement-*` or `kickoff-*` folder for it; `probe-graph-workflow-audit` skipped (no `STATUS.*` marker, not a spec). Claim: `git worktree add .worktrees/implement-ai-answer-quality-baseline -b thejudge-auto/ai-answer-quality-baseline-work origin/main` (`787ca5f`, clean); README `## Autonomous metadata` → `origin/main`; ledger header rewritten (run id, worktree, base); `STATUS.refined` untouched. Lock taken (`npm run graph:preflight -- --take-lock --slug ai-answer-quality-baseline --run-id graph-20260907-032104 --pid 2370`); graph canary `nohup true` denied; `graph-ledger-check` ok before the claim edits. Launch checkout `main` at `787ca5f`, `git status --porcelain` empty, never switched. Note for gate-qc: `DESIGN-BRIEF.md` predates the owner's six edits (0 mentions of the tiered gold set, the `gpt-5` judge, the model lineup, latency, or the blind rank) | 2026-09-07 |
| 4 | gate-qc (build half, attempt 1) | sonnet | failed → define (loop 1 of 3) | `0 → 42` (node self-reported 38) | FAIL, 7 findings. 1–6: `DESIGN-BRIEF.md` contradicts the finalized proposal — line 47 six gold cases vs REQ-185's at-least-six plus ~a dozen tier-1/2 seeds; lines 11–13 and 41–42 no model swap vs REQ-188's four-model lineup; A9 (line 245) judge defaults to `OPENAI_MODEL` vs REQ-186's `gpt-5` default under `ANSWER_QUALITY_JUDGE_MODEL`; M3 (lines 102–121) six-case single-model cost ≈ $0.04–$0.19 vs REQ-188's lineup × ~18 cases × 2 caps; measurement plan (lines 299–308) lacks per-call latency, the blind rank, and the model × cap crossing (REQ-188/189/190); judge section (lines 192–205) lacks the blind side-by-side rank (REQ-186). 7: REQ-185's edited diff and `- Reason:` call `apps/backend/data/cr/source.txt` committed CR text — it is gitignored (`.gitignore:47`, `integrations-and-data.md:255`), absent in this worktree, so the 277 `Example:` count could not be reproduced there (driver re-measured in the launch checkout: file present, 277 lines, written by `scripts/refresh-scryfall-data.mjs`, read by `scripts/build-game-rules.mjs`). Everything else passed: provider `openAiResponsesProvider.ts` sends only `model` and `input`, so the lineup and judge are addressable by model id; rulings file 19,542 cards / 76,605 rulings (matches); four cap-of-5 sites (`preparation.ts` 228/272/317/355); `eval:worked-solutions` 6/6 under `EMBEDDING_PROVIDER=local`; 4/4 `Current:` excerpts byte-identical at `787ca5f`; REQ-185–190 unused; heading hygiene clean; 16-term amendment-set grep leaves no uncovered assertion. Package → `STATUS.refining`, README `status: refining`, board row under `## refining`; commit `6270fcb` pushed to `thejudge-auto/ai-answer-quality-baseline-work`; tree clean; no subagents. Driver rewrote `## Preparation gate` FAIL in the README | 2026-09-07 |
| 3 | define (build half, attempt 1 — loop 1 of 3) | opus | ok | `0 → 52` (node self-reported 49) | brief reconciled with the finalized proposal, verdicts untouched (driver check: `git diff fe4435a..2de4800` changes 0 `- Verdict:`/`- Reason:` lines; `GATE-QUESTIONS.md` changed on exactly 2 lines, both inside the REQ-185 block, lines 49 and 64). `DESIGN-BRIEF.md` +389/−99: header and non-goals (lineup is a run option, production model and `OPENAI_MODEL` untouched, no reasoning-effort setting); new `### How the gold set is defined and how it grows` (tiers 1–2, four-field validity test, ≥6 named + ~12 seeds, combos out); judge section now four layers incl. the blind side-by-side rank and the stronger-than-every-contestant rule; A9 revised (`ANSWER_QUALITY_JUDGE_MODEL`, default `gpt-5`, match flagged), A8 revised, A11–A13 new (model access via a models-list request; lineup a run option; re-check prices and case count before the first run); M3 re-derived: 4 models × ~18 cases × 2 caps = 144 answer + 144 lone-judge + 36 blind-rank calls, ≈ $0.55 answers + ≈ $1.90 judge ≈ $2.50, stated as $1–4, labelled estimate; measurement plan items 4–5 carry model × cap legs, latency ms, blind rank, headline per model per cap, the model-comparison rule, first live run every model × cap 5/10. Finding 7: the committed index `apps/backend/data/gameRulesRuleIndex.json` (emitted by `build-game-rules.mjs`) carries 277 `Example:` lines across 215 of 2,873 entries (driver re-grep: 215 matching entries) and rule 603.7a's entry contains the `delayed-trigger-created-too-late` worked solution verbatim, so tier-1 authoring reads the committed index; REQ-185's tier-1 criterion and measurement note now say so, the gitignored `cr/source.txt` named only as the raw download; new M6 records both committed pools; no blocker question (condition 2 fails). `STATUS.refined`, README `status: refined` + intro updated, board row under `## refined`; commit `2de4800` pushed to `thejudge-auto/ai-answer-quality-baseline-work`; tree clean; no subagents | 2026-09-07 |
| 4 | gate-qc (build half, attempt 2) | sonnet | ok | `0 → 45` (node self-reported 39) | PASS, no findings: all seven attempt-1 findings closed with brief lines (`:373`, `:18-21`, `:426`/`:330`, `:151-199`, `:463-520`, `:305-316`); `gameRulesRuleIndex.json` 277 `Example:` occurrences / 215 entries reproduced; `git diff fe4435a..2de4800 -- GATE-QUESTIONS.md` = 2 provenance lines in REQ-185, owner lines byte-identical; M3 shows call count 4 × ~18 × 2 = 144 answer + 144 lone-judge + 36 blind-rank, M2/M4 token sizes, published prices, labelled estimate, no numeric cost target; 4/4 `Current:` excerpts byte-identical, `PRD/sections/` unchanged from `787ca5f`; REQ-185–190 unused; heading hygiene clean; amendment-set result stands; technical-design-rules hold; `STATUS.refined` unchanged, nothing committed; 39 tool calls, no subagents. Non-blocking note: `DESIGN-BRIEF.md:208` cap-site line numbers are 5 lines stale. Driver wrote `## Preparation gate` PASS to the README | 2026-09-07 |
| 5 | plan | sonnet | ok | `0 → 57` (node self-reported 34) | `GAMEPLAN.md` + five slice docs (A `slice-a-gold-set-and-validity.md` REQ-185; B `slice-b-cap-and-run-command.md` REQ-188/190; C `slice-c-judge-and-rubric.md` REQ-186/187; D `slice-d-artifact-and-transcripts.md` REQ-189; E `slice-e-integrate-and-apply.md` NFR-018/REQ-146/SYSTEM-MAP-EVAL-HARNESS/GOALS-ANSWER-QUALITY-NON-GOAL, depends on A–D) + `slice-{a..e}.criteria.json` (7/9/8/7/10 criteria, driver-checked by script: 41 total, all `false`, every one with an `evidence` block, 2 `manual` — E9 owner-confirmed first live run, E10 human review pass); A–D parallel-ready, E sequential; no Playwright criteria (no screen change); verification checklist: `test:scripts`, `test:eval` + backend suite, `eval:worked-solutions` still 6/6+, `eval:answer-quality` with no flag prints the plan and exits 0 with no network call, `quality:check` green plus a test that the command is in no gate script. `STATUS.active`, README `status: active` with slice table and implementation map, board row under `## active`; commit `a18fe29` pushed to `thejudge-auto/ai-answer-quality-baseline-work`; tree clean; no subagents. Driver note before `build`: E9/E10 need a human, so `build` is expected to end `failed` at those two criteria with everything else green — a `build` blocker park, not a defect. Launch checkout snapshot before dispatch: `main` at `787ca5f`, `git status --porcelain` empty (saved to `.worktrees/.graph-intake/launch-status-before-build.txt`) | 2026-09-07 |
| 6 | build | sonnet | failed → parked (build blocker: two human-only criteria) | `0 → 337` (node self-reported 336) | slices A–D `done`, one milestone commit each, pushed: A `a285a52` (gold set, `scripts/lib/gold-cases.mjs`, 18 cases incl. 12 tier-1/tier-2 seeds; REQ-185 applied), B `6d1e462` (named cap constant in `preparation.ts`, cap as a run parameter, `scripts/eval-answer-quality.mjs` scaffold; REQ-188/190 applied), C `2d1b7e0` (`apps/backend/src/eval/answer-quality/{rubric,assertions,judge}.ts` + tests; REQ-186/187 applied), D `686c275` (`artifact.ts`, `results.json`, gitignored transcripts; REQ-189 applied). E `f2433fb` (local at node return; the driver pushes it with this park): wiring, regression guard, worked-solutions README, NFR-018 / REQ-146 / `system-map.md` `## Eval harness` / `goals-and-non-goals.md` applied, `quality:check` green, dry run `npm run eval:answer-quality` prints 18 cases × lineup of 4 × caps 5/10 = 144 answer + 144 lone-judge + 36 blind-rank calls, ≈ $2.56, no network, exit 0. Criteria read from the emitted files by the driver: A 7/7, B 9/9, C 8/8, D 7/7, E 8/10 — E9 (owner-confirmed first live run) and E10 (a human read the record) `false`, as the dispatch required; no paid call made, no observation line fabricated. PR #203 https://github.com/ChrisMiho/TheJudge/pull/203 (`thejudge-auto/ai-answer-quality-baseline-work` → `main`, MERGEABLE, head `686c275` at node return). Return-side assertions (REQ-193): launch checkout `main` at `787ca5f`, `git status --porcelain` identical to the pre-dispatch snapshot (empty); 54 written paths from `git diff --name-only e0c5e27 f2433fb`, `classifyBuildWrites` → `ok`, none outside `.worktrees/implement-ai-answer-quality-baseline/`. One profile denial (`sed -i`, retried via Edit), 0 hook denials; no subagents | 2026-09-07 |
| — | driver (owner in session, lock released) | — | ok | `n/a (driver)` | park pushed `6ce9b03`. Owner cleared the live run. First attempt stopped on unset `ASK_AI_PROVIDER`; fixed by giving `eval:answer-quality` the same local-env loader as `openai:verify-credentials` (`scripts/lib/local-openai-env.mjs`, 28/28 + 500/500 script tests, commit `549b12c` pushed). Run 1 (mock embedder, 06:47 UTC, $0.67) preserved under `output/answer-quality/mock-embeddings-2026-09-07/`; run 2 (`EMBEDDING_PROVIDER=local`, 08:03 UTC, $0.69, 324 calls, 0 undetermined) is the committed `results.json` — cap 5 / cap 10 fully correct: gpt-4.1-mini 16/15, gpt-4.1 14/17, gpt-5-mini 15/16, gpt-5-nano 14/15. E9 flipped `true` by the driver on the owner's clearance (lock not held, so the flip guard did not apply); E10 stays `false` for the owner. Deployed model confirmed `gpt-4.1` from the Lambda env. Production finding (owner's 20 s request → CloudWatch): every cold start since 2026-09-06 logged `local provider failed; falling back to lexical` because `package-lambda.sh` kept `linux/x64` on an arm64 Lambda — fix PR #204 off `origin/main` (pruner keeps the function's platform and refuses without it; 470/470 script tests; real package 206 MB, arm64 only) | 2026-09-07 |
| — | driver (owner in session, lock not held; instrument corrected) | — | ok | `n/a (driver)` | Two instrument defects found from the handoff's agreed next step: `runLiveEvaluation` asked tier-2 cases bare (no card, so no oracle text and no ruling in the prompt) and passed no `queryEmbedding` to `preparePromptInput`, so runs 1 and 2 both ranked lexically under whatever `EMBEDDING_PROVIDER` said. Fix in `scripts/eval-answer-quality.mjs`: `buildCaseRequest` attaches a tier-2 case's card by oracle id; the run loads the card-detail and card-rulings indexes; the question is embedded once per case via `buildRetrievalQueryText` by `EMBEDDING_PROVIDER` (default now `local`); `assertQueryEmbedded` / `describeRetrieval` abort a real-provider run that fell back to lexical; transcripts gain `cards` + `retrieval`, per-case records gain `goldRuleInPrompt` (`artifact.ts`). 22/22 script tests, backend typecheck, eslint clean; offline probe before spending: all 3 tier-2 prompts carry the cited ruling verbatim, 18/18 `usedSemantic`. Run 2's transcripts preserved under `output/answer-quality/local-label-lexical-2026-09-07/`; model cache copied from the main checkout (gitignored). Run 3 (09:48 UTC, $0.63, 324 calls, 0 undetermined) is the committed `results.json`: cap 5 / cap 10 fully correct gpt-4.1-mini 17/15, gpt-4.1 16/18, gpt-5-mini 17/18, gpt-5-nano 15/13; gold rule in prompt 14/18 at cap 5, 16/18 at cap 10 — the two cap-10 gains are 510.1c and 113.7a, the very cases the lexical runs never reached, so the handoff's tokenizer follow-up is closed. REQ-185/188/189/190 and the worked-solutions README amended; full record in `slice-e-integrate-and-apply.md` `## Third live run`. E9 stays `true`, E10 stays `false` for the owner | 2026-09-07 |
| — | driver (gate resolution, lock re-taken) | — | ok | `n/a (driver)` | Owner read run 3's transcripts in session (E10 `true`, commit `104642d`); the retrieval check `eval:worked-solutions` gained the same production fidelity and the run command now formats `results.json` (`928f705`). Lock re-taken (`graph-preflight --take-lock`, pid 38605, `THEJUDGE_GRAPH_PROFILE=1` observed); graph canary `nohup true` denied (hook live, graph tier armed; run-state absent at that moment, so the cap was degraded for the canary only). `STATUS.active` restored (marker, README, board row), `## Open gate` resolved, build attempt 2 prompt recorded, ledger-check ok, driver commit `2e2decc` pushed; launch checkout `main` at `787ca5f`, `git status --porcelain` empty (snapshot `.worktrees/.graph-intake/launch-status-before-build-2.txt`) | 2026-09-07 |
| 6 | build (attempt 2 — confirmation pass) | sonnet | ok | `0 → 45` (node self-reported ≈43) | 41/41 criteria `true` read from the five `slice-*.criteria.json` (A 7/7, B 9/9, C 8/8, D 7/7, E 10/10), every slice doc `## Status: done`; gates on this head: `npm run test:scripts` 506/506, `npm --prefix apps/backend run test` 494/494, `npm run quality:check` green, `npm run eval:answer-quality` dry plan exit 0 (no completion call), `npm run eval:worked-solutions` 14/18 (informational). `STATUS.ship-ready` (git mv), README `status: ship-ready` + confirmation paragraph, board row under `## ship-ready`; PR #203 retitled `[THEJUDGE-AUTO][READY] …` and body rewritten with run 3; commit `41efa41` pushed, head = `origin/thejudge-auto/ai-answer-quality-baseline-work`. Return-side (REQ-193): launch checkout `git status --porcelain` empty before and after; 3 written paths, all inside `.worktrees/implement-ai-answer-quality-baseline/` (`classifyBuildWrites` → ok). No code, tests, gold cases, or `PRD/sections/` changed; no criterion flipped; no denials; no subagents | 2026-09-07 |
| 7 | review | opus | failed → build (loop 1 of 2) | `0 → 63` (node self-reported 58) | Verdict `return to build`, three Important findings, all confirmed by the driver against the files: (1) D3/REQ-189 — the blind ranking never produces a rationale: `judge.ts:197` asks for `ranks` only, `parseRankingResponse` reads only `ranks`, `RankingTranscript` has no rationale, and a real run-3 ranking file holds `caseId`/`excerptCap`/`ranks`/`undetermined` only, while `functional-requirements.md:4421` says transcripts carry the side-by-side ranking rationale; (2) C8/REQ-186 — the layer-2b bullet says the ranking call sees the reference and the rubric, but `buildRankingPrompt` (`judge.ts:181-199`) never calls `formatRubricForJudge()` (only the lone-judge prompt does); (3) E6 — the `### Answer-quality baseline` sub-entry was inserted one line high in `system-map.md`: `### Retrieval relevance report` lost its `- Backed by: DEC-047, REQ-032, REQ-177` line, which now sits as a second `Backed by:` under the new entry. Minor, no loop: B2's byte-identity test stands in for the M4 char counts (declared in the slice notes); slice E's five `## Ship gates` boxes unticked though `done`; REQ-189's closing note still calls `results.json` a placeholder. All other criteria met (A 7/7, B 9/9, C 7/8, D 6/7, E 9/10 on the files; E9/E10 on the dated lines matching `results.json` exactly). Read-only commands: `test:scripts` 506/506, backend 494/494, `eval:worked-solutions` 14/18 (18/18 semantic), dry plan exit 0, typecheck/lint/format:check green; no file, branch, or PR touched; no subagents | 2026-09-07 |

## Open gate

- **Ask:** Decide. Answer every `- Verdict:` slot (accept / edit / reject, with a reason on edit or reject) in `PRD/work/ai-answer-quality-baseline/GATE-QUESTIONS.md` — 10 slots, 0 blocker questions — then merge the docs-only PR to `main` to build.
- **In plain terms:** the ten slots decide the shape of the answer-quality instrument: which six hard questions with published official answers it grades (REQ-185), how an answer is judged — fixed checks, then a model judge handed the published answer as its reference, then a human pass (REQ-186) — what a good answer is scored on, four axes at 0–2 with correctness as the headline (REQ-187), an on-demand command that needs a live model call behind an explicit confirm flag, never in any build gate, with a cost estimate of a few cents per run and no cost target (REQ-188), a small committed scorecard with transcripts kept out of git (REQ-189), the excerpt cap as a run parameter so five-versus-more measures distraction on these cases (REQ-190), and four amendments that make the live specs say so (NFR-018, the system map's eval-harness entry, the goals non-goal, REQ-146).
- **Evidence:** gate-qc PASS (row 4 above); `## Preparation gate` in `README.md`; the offline measurements under `DESIGN-BRIEF.md` `## Measurement plan`.
- **PR:** https://github.com/ChrisMiho/TheJudge/pull/200 (docs-only, `thejudge-auto/ai-answer-quality-baseline` → `main`; opened 2026-09-06, not merged).
- **Resume:** `/loop graph-implement` (the build loop) picks the package up from `main` after the merge. Manual equivalent: `/graph-implement PRD/work/ai-answer-quality-baseline/`.
- Parked 2026-09-06 at `owner-action`; run `graph-20260906-092312` released its lock with state `PARKED`.
- **Resolved 2026-09-06** by `graph-gate-review`: 10 of 10 verdicts read from the answered `GATE-QUESTIONS.md` — 6 `edit`, 4 `accept`, 0 `reject`; every `edit` applied inside that ID's proposed diff in `GATE-QUESTIONS.md`, nothing written to `PRD/sections/`; `STATUS.refined` restored. See `## Gate verdicts`.

### Build gate — parked 2026-09-07 (run `graph-20260907-032104`)

- **Ask:** Run the instrument once and read what it wrote. Two acceptance criteria in slice E can only be earned by a person: E9, the first live run with your confirmation (a paid call to OpenAI, about $2.56 for 324 sequential calls across four answer models, 18 cases, two excerpt caps, plus the `gpt-5` judge), and E10, a human reading the written record. The build made no paid call and wrote no claim that a human did anything.
- **In plain terms:** everything else is built and green — the 18-case tiered gold set, the run command with its dry run and confirm flag, the judge that scores alone and ranks blind, the committed scorecard with gitignored transcripts, and the ten product-truth blocks applied to `PRD/sections/`. Slices A–D are done (41 criteria, 39 true); slice E is `blocked` only on E9 and E10.
- **Evidence:** row 6 above; `PRD/work/ai-answer-quality-baseline/slice-e.criteria.json` (E9, E10 `false`); the owner steps in `slice-e-integrate-and-apply.md`; PR #203 https://github.com/ChrisMiho/TheJudge/pull/203.
- **Do this, in the build worktree** `/Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline` (branch `thejudge-auto/ai-answer-quality-baseline-work`):
  1. Make sure an OpenAI key with access to `gpt-4.1-mini`, `gpt-4.1`, `gpt-5-mini`, `gpt-5-nano`, and `gpt-5` is configured for `ASK_AI_PROVIDER=openai`; `npm run eval:answer-quality` with no flag prints the plan and cost with no call.
  2. Run `npm run eval:answer-quality -- --confirm-live-calls`. It writes `apps/backend/src/eval/answer-quality/results.json` (committed) and transcripts under `output/answer-quality/` (gitignored).
  3. Read at least one transcript per lineup model and one `*--ranking.json`, and record a dated conclusion line in `slice-e-integrate-and-apply.md`.
  4. In `slice-e.criteria.json` set `E9` and `E10` to `true`; commit `results.json`, the slice doc, and the criteria file on this branch and push (no force).
  5. Resume with `/loop graph-implement` (or `/graph-implement PRD/work/ai-answer-quality-baseline/`). The driver reads E9/E10 from the criteria file: both `true` resolves this gate, restores `STATUS.active`, and re-enters at `build` to confirm 41/41 before `review → close`. If you would rather not run it live yet, say so and the driver parks the run as is.
- Parked 2026-09-07 at `owner-action`; run `graph-20260907-032104` released its lock with state `PARKED`.
- **Update 2026-09-07, later the same night (owner in session):** the owner cleared the live run ("we are cleared for testing"). The driver ran it twice from the build worktree — once under the mock embedder by omission ($0.67, kept as the comparison), then under `EMBEDDING_PROVIDER=local` ($0.69, the committed baseline) — and recorded both in `slice-e-integrate-and-apply.md` `## First live run`. **E9 is `true`; only E10 (the owner's read-through, steps 4–6 of the owner steps) remains.** Along the way: `eval:answer-quality` now loads the key from `.secrets/openai-dev.env` itself (`549b12c`, REQ-188 amended), and a production bug surfaced from the owner's own slow request — the deployed Lambda has had no loadable onnxruntime binding since the 2026-09-06 deploy and has run lexical retrieval; fix PR #204 (`fix/lambda-arm64-onnx-binding`, off `origin/main`, outside this package).
- **Update 2026-09-07, 09:48 UTC (owner in session):** the instrument was found to be asking tier-2 cases without their card and ranking lexically in every run regardless of `EMBEDDING_PROVIDER`; fixed and re-run (run 3, $0.63). `results.json` now holds run 3; the earlier scorecards sit beside their transcripts under `output/answer-quality/` (gitignored). **E9 stays `true`; E10 is still the owner's, now over run 3's transcripts.** Row "driver (owner in session, lock not held; instrument corrected)" above and `slice-e-integrate-and-apply.md` `## Third live run — instrument corrected` carry the record.
- **Resolved 2026-09-07, 15:59 UTC** by `/loop graph-implement` (owner in session): the owner read run 3's transcripts and said so ("I've reviewed the files and I'm happy for now"); the dated conclusion line sits in slice E's `## Owner steps for E9 and E10` and E10 is `true` in `slice-e.criteria.json` (commit `104642d`, pushed). Both manual criteria `true` → gate resolved: `STATUS.active` restored (marker, README `status:`, board row), lock re-taken (`graph-preflight --take-lock`, pid 38605), graph canary `nohup true` denied. Re-entering at `build` (attempt 2) to confirm 41/41, then `review → close`.

## Gate verdicts

Read from the owner's answered `GATE-QUESTIONS.md` on 2026-09-06 and applied inside each ID's proposed diff there. The owner reshaped the instrument into a multi-model bake-off over a tiered, all-official gold set; the full reasons are the `- Reason:` lines in `GATE-QUESTIONS.md`.

| Stable ID | Verdict | Reason |
| --- | --- | --- |
| `REQ-185` | edit | "Keep the six CR cases but define the gold set as tiers, all official, so it can grow past six without inventing an answer. Tier 1: a Comprehensive Rules `Example:` line verbatim (277 in the committed CR text, six used). Tier 2: a WotC card ruling verbatim with a hand-authored, human-reviewed question. No tier 3: community articles pick questions, never answers. At least the six named cases; seed with roughly a dozen more hard cases from tiers 1 and 2. Combos stay out." |
| `REQ-186` | edit | "The judge must be stronger than every contestant and never one of them: default `gpt-5`, own setting `ANSWER_QUALITY_JUDGE_MODEL`, artifact flags a judge that matches any answer model. Add a second judge job: after scoring each answer alone, see all answers to one question side by side, labels hidden and order shuffled, and rank them." |
| `REQ-187` | accept | — |
| `REQ-188` | edit | "The answer model is a list of legs: first-ship lineup `gpt-4.1-mini`, `gpt-4.1`, `gpt-5-mini`, `gpt-5-nano`; judge `gpt-5`. Dry run checks model access. Record wall-clock latency per call so the bake-off cannot crown a model the 3-second target cannot use. Lineup differences are a model comparison, not incomparable. Provider sends only model and prompt; a reasoning-effort setting is a follow-up package. Re-derive the cost note for the lineup." |
| `REQ-189` | edit | "The per-leg unit is model × cap. Per case per leg add latency in ms and the blind rank; headline count per model per cap." |
| `REQ-190` | edit | "Generalize leg to model × cap; each cap value is answered by every model. Note that tier-2 ruling cases are not known to be retrieval-saturated, so the cap experiment gains recall signal as the set grows." |
| `NFR-018` | accept | — |
| `SYSTEM-MAP-EVAL-HARNESS` | edit | "Wording only in the appended sub-entry: each model in a configured lineup; a judge model stronger than every contestant, scoring alone and ranking blind side by side." |
| `GOALS-ANSWER-QUALITY-NON-GOAL` | accept | — |
| `REQ-146` | accept | — |

Blocker questions: none were asked, none to answer.

## Dispatch prompts

### preflight

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge

You are node 1 (`preflight`) of graph run `graph-20260906-092312`, the spec-forming half driven by `graph-kickoff`. Invoke the `graph-preflight` skill with the Skill tool (skill name `graph-preflight`) and follow it exactly. Read `.claude/skills/graph-preflight/SKILL.md` and `PRD/instructions/graph-workflow-contract.md` before acting.

Inputs (fixed by the driver; do not change them):
- `--branch thejudge-auto/ai-answer-quality-baseline`
- `--run-id graph-20260906-092312`
- `--slug ai-answer-quality-baseline`
- `--pid 77812` (the driver session's own long-lived pid, so the lock does not read stale)
- Base: the checkout is on `main`, in sync with `origin/main` at `8d139aa`, tree clean. Do not pass `--base`; report the resolved `base:` line the script prints.

Procedure:
1. Run `npm run graph:preflight -- --branch thejudge-auto/ai-answer-quality-baseline --run-id graph-20260906-092312 --slug ai-answer-quality-baseline --pid 77812 --dry-run`. Report the classification, the resolved base, the planned commands, and the `profile sentinel:` / `Profile:` lines verbatim.
2. If the classification is `blocked`, or the script exits 2 (stop sentinel, base→main guard, lock held, or branch collision), stop and relay the script's message verbatim. Never hand-resolve anything to get past it.
3. Otherwise run the identical command without `--dry-run`.
4. Issue the universal canary (`CANARY_COMMAND`) and the graph canary (`GRAPH_CANARY_COMMAND`) as real Bash tool calls, exactly as the script prints them. Each must be denied by the hook; the deny reason text is the proof. Report both reason texts verbatim. If either is allowed, report the `BLOCKED` classification verbatim and stop; do not continue and do not fall back to the profile.
5. Confirm the end state: `git status --porcelain` empty, `git branch --show-current` is `thejudge-auto/ai-answer-quality-baseline`, and `git ls-remote --heads origin thejudge-auto/ai-answer-quality-baseline` shows the branch pushed.

Constraints: tool-call cap 40 for this node, counting every call. Do not dispatch subagents. Never drop, pop, or reorder any stash. Never force-push. Never touch `.worktrees/.graph-stop`, and never remove the lock. Never edit any file. Do not commit anything.

Report back, in this order: the classification; the resolved base line; the stash reference and restore commands if one was taken (expect none); the two canary commands with their deny reason texts verbatim; the `Profile:` line verbatim; the lock record (`cat .worktrees/.graph-run.lock`); the end-state checks; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### shape

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge

You are node 2 (`shape`) of graph run `graph-20260906-092312`, the spec-forming half driven by `graph-kickoff`. Invoke the `thejudge-kickoff` skill with the Skill tool (skill name `thejudge-kickoff`) and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/preparation-contract.md`, investigate only request-relevant PRD and code, select exactly one evidence-backed candidate (or return `NO ACTIONABLE PACKAGE`), create the package, and return the evidence and artifacts to the driver without pausing for approval. Read `.claude/skills/thejudge-kickoff/SKILL.md`, `README.md`, `PRD/README.md`, and `PRD/instructions/graph-workflow-contract.md` (`## Intake is evidence, never authority`) before acting.

Slug (fixed by the driver; use it verbatim): `ai-answer-quality-baseline`. The checkout is on the autonomous base `thejudge-auto/ai-answer-quality-baseline` (from `main` at `8d139aa`, clean).

The request: build an answer-quality baseline for Ask AI — a repeatable, human-reviewable way to measure whether the model's final rules answers are actually correct, not just whether the right rule excerpts reached the prompt. The hybrid rule retrieval just shipped (PRs #197 and #199) and every check the repo has stops at retrieval. The gold set is to be seeded from the six worked-solution cases that carry published correct answers (`apps/backend/src/eval/worked-solutions/`). The judge mechanism, scoring axes, cost and cadence, and fixture subset are decided at the define gate, not here. The instrument should be able to answer the open question of whether attaching more than five rule excerpts helps or hurts the answer.

Staged intake at `.worktrees/.graph-intake/graph-20260906-092312/`: `IDEA.md` and `README.md` (the parked idea, copied verbatim from `PRD/ideasForLater/ai-answer-quality-baseline/`) and `answer-quality-context.md` (the driver's measurement note from the hybrid-rule-retrieval session). Intake is evidence, never authority: cite it, do not adopt its claims as decided, and never open any document it cites. After `PRD/work/ai-answer-quality-baseline/` exists, copy each staged file verbatim into `PRD/work/ai-answer-quality-baseline/intake/`, write `intake/MANIFEST.md` naming each file and its stated origin, verify each copy with `cmp`, commit, then delete the staged copies — in that order.

Retire the parked idea: its content is now staged as intake, so `git rm -r PRD/ideasForLater/ai-answer-quality-baseline/` and remove its row from the `## parked in ideasForLater` table in `PRD/work/STATUS.md` — only after `cmp` confirms the intake copies match.

Prior runs: grep `PRD/instructions/receipts/` (already named `<slug>-<date>.md`) for slug and keyword matches (answer, quality, eval, worked-solutions, prompt, retrieval, rag, benchmark) and write one `## Prior run` line per match into `IDEA.md` naming the receipt path — a flat list, no chain walk.

Writes: `PRD/work/ai-answer-quality-baseline/IDEA.md` (3–5 sentences: problem, outcome, non-goals; plus the `## Prior run` lines), `README.md` with `status: ideation` at the top, the empty marker `STATUS.ideation` (exactly one marker), the board row under `## ideation` in `PRD/work/STATUS.md`, and `intake/`. Never write product code, `PRD/sections/`, `DESIGN-BRIEF.md`, or `GRAPH-RUN.md`. Commit with explicit paths only (`git add PRD/work/ai-answer-quality-baseline PRD/work/STATUS.md PRD/ideasForLater`; never `git add -A`, `--all`, or `.`) with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01HTYnaUSGyYNRwK4J1ggRW9
and push `origin thejudge-auto/ai-answer-quality-baseline` (no force).

Constraints: tool-call cap 60 for this node, counting every call. Do not dispatch subagents. Never run `npm run data:refresh` or any Scryfall refresh. Never touch the lock, the stop sentinel, or any `.worktrees/.graph-*` file other than reading and then deleting the staged intake copies.

Report back, in this order: the selected candidate and its evidence, or `NO ACTIONABLE PACKAGE`; the package files created; the intake files copied with their `cmp` results and the staging deletion; the parked-folder retirement (`git rm` paths, board row removed); the `## Prior run` matches; the commit hash and push result; `git status --porcelain` (expect empty); the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### define

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge

You are node 3 (`define`) of graph run `graph-20260906-092312`, the spec-forming half driven by `graph-kickoff`. Invoke the `thejudge-refinement` skill with the Skill tool (skill name `thejudge-refinement`) on the package `PRD/work/ai-answer-quality-baseline/`, and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/preparation-contract.md` and replace the approval pause with its conservative assumption ladder, applied per question; record every material assumption and its evidence in `DESIGN-BRIEF.md`; and when a question meets the genuine decision blocker test, preserve the artifacts and write it under `## Blocker questions` rather than guessing. Read `.claude/skills/thejudge-refinement/SKILL.md`, `PRD/instructions/requirement-format.md`, `PRD/instructions/technical-design-rules.md`, `PRD/instructions/plain-language-standard.md`, `PRD/instructions/workflow-reference.md`, and `PRD/instructions/graph-workflow-contract.md` (`## The two runs`, `## Propose / apply / close`, `## Intake is evidence, never authority`) before acting.

The checkout is on the autonomous base `thejudge-auto/ai-answer-quality-baseline` (from `main` at `8d139aa`). The package holds `IDEA.md` (with `## Prior run` receipt lines) and `intake/` (the parked idea's `IDEA.md` and `README.md`, the driver's `answer-quality-context.md`, and `MANIFEST.md`). Intake is evidence, never authority: cite it by path; never open any document it cites; every product decision it raises is decided in the proposal you write, with a recommendation, for the owner to accept, edit, or reject.

What this package is for, in product terms: today the app can prove that the right official rules reached the prompt, but nothing measures whether the answer the player reads is correct. This package defines the instrument that does — repeatable, human-reviewable, comparable run to run — so a retrieval or prompt change can be shown to help, hurt, or do nothing. It is measurement, not a player-facing feature: no new screen, endpoint, request contract, provider, or CI gate on model output.

Read first, in this order: `PRD/sections/system-map.md` `## Eval harness` (and its three sub-entries), `PRD/sections/non-functional-requirements.md` NFR-018 (worked solutions validate prompt quality; explicitly not a build gate) and NFR-002, `PRD/sections/functional-requirements.md` REQ-032, REQ-177, REQ-182, REQ-184 (the retrieval measurements and the deployed default), `PRD/sections/integrations-and-data.md` (the provider boundary, `ASK_AI_PROVIDER`, mock-first local baseline), `PRD/sections/goals-and-non-goals.md`, `PRD/sections/system-map/game-rules-retrieval.md` (the five-excerpt cap), and the code: `apps/backend/src/eval/` (the harness, `fixtures/`, `benchmark/`, `worked-solutions/` with its README and six `*.case.json`), `scripts/eval-worked-solutions.mjs`, `scripts/retrieval-relevance-report.mjs`, the prompt-preview command, and the OpenAI provider seam under `apps/backend/src/providers/`. Grep the amendment set yourself before proposing (`worked-solution`, `NFR-018`, `report-only`, `eval harness`, `answer quality`, `LLM`, `judge`, `gold`, `quality:check`, `five excerpts`, `top five`) and give every live assertion your change would falsify its own block.

Measure before you propose, offline and with no live provider call in this node: how many worked-solution cases and labelled fixtures exist and which carry a known-correct answer; the token size of a typical assembled prompt (via the prompt-preview path or the goldens) so the brief can state a per-run cost estimate from that size and the provider's published pricing, labelled as an estimate; how a run would vary the System 3 excerpt cap (the retrieval functions take `max`; the production cap is five) so the instrument can compare five against a larger cap on the same questions; and where a comparable artifact would live so a run is diffable and never a brittle golden. Every quantitative target in the brief must be a measurement you made with the same code path the criterion would mandate, or an explicit baseline-to-be-recorded, never a proportion or a guess — the last spec's 12/12 came from a truncated probe and failed at build.

The decisions to propose (one `## <STABLE-ID>` block each, with your recommendation stated and the alternatives named): the judge mechanism (model-as-judge against a rubric, a human review pass, assertion checks per case, or a combination — the owner said to decide this here); the scoring axes; the gold set (the six worked-solution cases as the seed, plus which labelled fixtures, with their known answers stated or marked as needing an answer key); cost and cadence (an on-demand command with a stated per-run estimate, never a CI gate, with what a run must record so two runs compare); the artifact shape and where it is committed; and the excerpt-cap experiment as a parameter of the instrument, with the 2026-09-06 recall@k measurements from `intake/answer-quality-context.md` cited as evidence, not adopted. Reserve new ids as the next free numbers — after REQ-184, NFR-019, FLOW-024 — and amend NFR-018 (its scope grows from prompt/retrieval validation to answer validation), REQ-032 if its text asserts the harness is the only quality instrument, and the `system-map.md` `## Eval harness` entry, each with its byte-for-byte `Current:` excerpt and complete replacement. If a run would ever need a live provider call, say so plainly and state the mock-first fallback; never propose committing a secret, a new dependency without cause, or any Scryfall refresh.

Writes, inside `PRD/work/ai-answer-quality-baseline/` only: `DESIGN-BRIEF.md` (scope, decisions with evidence, assumptions with evidence, non-goals, a measurement plan, REQ/NFR references), `GATE-QUESTIONS.md` (every block opening with the three plain-language lines — what this decides, in plain terms with every cited id inlined and every technical term defined, what happens if you say no — then the complete diff, then `- Verdict:` and `- Reason:` slots; a `## Blocker questions` section only for a genuine blocker), the README `status:` line, the `STATUS.*` marker (`STATUS.refining` while in flux, `STATUS.refined` when the brief is complete; exactly one marker), and the board row. Never edit `PRD/sections/`, code, `GRAPH-RUN.md`, or `intake/`. Commit with explicit paths only (`git add PRD/work/ai-answer-quality-baseline PRD/work/STATUS.md`; never `git add -A`, `--all`, or `.`) with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01HTYnaUSGyYNRwK4J1ggRW9
and push `origin thejudge-auto/ai-answer-quality-baseline` (no force).

Constraints: tool-call cap 150 for this node, counting every call. Do not dispatch subagents; a helper's calls charge this node's budget. No live provider calls, no network calls beyond git. Never run `npm run data:refresh` or any Scryfall refresh. Never touch the lock, the stop sentinel, or any `.worktrees/.graph-*` file.

Report back, in this order: the design summary in three sentences; the list of `GATE-QUESTIONS.md` blocks (stable id, new or amended, target file, your recommendation) and the count of `## Blocker questions`; the measurements you made with the command or path behind each; the material assumptions and their evidence; the package state after the node (marker, README status, board heading); the commit hash and push result; `git status --porcelain` (expect empty); the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### gate-qc (attempt 1)

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge

You are node 4 (`gate-qc`), attempt 1, of graph run `graph-20260906-092312`, the spec-forming half driven by `graph-kickoff`. Invoke the `thejudge-quality-check` skill with the Skill tool (skill name `thejudge-quality-check`) on the package `PRD/work/ai-answer-quality-baseline/`, and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/preparation-contract.md`, emit an explicit PASS or FAIL verdict with the complete findings, and return it to the driver, which records it in the package README's `## Preparation gate` section. Read `.claude/skills/thejudge-quality-check/SKILL.md`, `PRD/instructions/technical-design-rules.md`, `PRD/instructions/plain-language-standard.md`, `PRD/instructions/requirement-format.md`, and `PRD/instructions/graph-workflow-contract.md` (`## The two runs`, `## Propose / apply / close`) before acting.

What to check, beyond the skill's own checklist:
1. `DESIGN-BRIEF.md` against the current-state specs it touches (`PRD/sections/system-map.md` `## Eval harness`, `PRD/sections/non-functional-requirements.md` NFR-018 and NFR-002, `PRD/sections/functional-requirements.md` REQ-032, REQ-146, REQ-177, REQ-182, REQ-184, `PRD/sections/goals-and-non-goals.md`, `PRD/sections/integrations-and-data.md`, `PRD/sections/system-map/game-rules-retrieval.md`): no contradictions, current vocabulary, technical-design-rules respected (one endpoint, no new product surface, no CI gate on model output, mock-first provider boundary), scope implementable without hidden assumptions, and no user-visible screen change (confirm that claim, so no `screen-layout.md` row is needed).
2. `GATE-QUESTIONS.md`: every `## <STABLE-ID>` block opens with the three-line plain-language block, inlines every cited id, defines its technical terms, carries a complete diff (never a summary), and has `- Verdict:` / `- Reason:` slots. Verify every `Current:` excerpt byte-for-byte against the live `PRD/sections/` file by script, not by eye. Confirm REQ-185 through REQ-190 are unused in live `PRD/sections/` and are the next free numbers after REQ-184. Heading hygiene: every top-level `## ` heading in the file must be a stable-id block or `## Blocker questions` — the file currently shows `## Eval harness` headings at about lines 459 and 471 inside the `SYSTEM-MAP-EVAL-HARNESS` block's excerpts; if those excerpts are not fenced so that their markdown headings stay inside the block, that is a FAIL finding (the gate-review parser reads one block per `## ` heading). Re-grep the amendment set yourself (`worked-solution`, `NFR-018`, `report-only`, `eval harness`, `answer quality`, `judge`, `gold`, `quality:check`, `five excerpts`, `top five`, `separate scope`, `REQ-146`) and FAIL if a live assertion the change would falsify has no block.
3. Measurements: the brief's numbers must rest on recorded, reproducible measurements. Reproduce the cheap offline ones once each and compare: the counts of worked-solution cases and of labelled fixtures with and without an answer key (`ls`/grep over `apps/backend/src/eval/worked-solutions/` and `apps/backend/src/eval/fixtures/`); the four hard-coded cap-of-5 call sites in `apps/backend/src/prompt/preparation.ts`; `npm run eval:worked-solutions` with `EMBEDDING_PROVIDER=local` (offline, packaged model cache, expect 6/6); and the mean assembled-prompt size across the six cases if the brief names the command or script it used (skip with a note if it names none). Confirm the cost figure is labelled an estimate derived from a stated token count and published pricing, not a target, and that the brief sets no unmeasured numeric acceptance gate. Do not run `npm run benchmark:rag-retrieval` (it rewrites tracked result files) and make no live provider call.
4. Package state: `STATUS.refined` is the only marker, `README.md` reads `status: refined`, the board row sits under `## refined`, `README.md` carries `## Autonomous metadata`, and `intake/` is untouched.

Verdict handling: on PASS, change nothing (leave `STATUS.refined`; do not write `GAMEPLAN.md`, slice docs, or code; do not edit the README, the driver writes the gate section). On FAIL, set `status: refining` in `README.md`, replace the marker with `STATUS.refining`, move the board row under `## refining`, commit with explicit paths only (`git add PRD/work/ai-answer-quality-baseline PRD/work/STATUS.md`, never `git add -A`, `--all`, or `.`) with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01HTYnaUSGyYNRwK4J1ggRW9
and push `origin thejudge-auto/ai-answer-quality-baseline` (no force). Never edit `DESIGN-BRIEF.md`, `GATE-QUESTIONS.md`, `PRD/sections/`, code, `intake/`, or `GRAPH-RUN.md`.

Constraints: tool-call cap 60 for this node, counting every call. Do not dispatch subagents; a helper's calls charge this node's own budget and a prior gate-qc exhausted its cap that way. Run each measurement command once. Never run `npm run data:refresh` or any Scryfall refresh. Never touch the lock, the stop sentinel, or any `.worktrees/.graph-*` file.

Report back, in this order: the one-word verdict (PASS or FAIL); the complete findings list, or `none`; the `Current:` excerpt verification result (count checked, count identical); the heading-hygiene result; the amendment-set grep result; each reproduced measurement with the brief's value beside it; the package state after the node; the commit hash if you committed; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### gate-qc (build half, attempt 1)

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 4 (`gate-qc`), build-half attempt 1, of graph run `graph-20260907-032104` (the build half driven by `graph-implement`; the spec-forming half was `graph-20260906-092312`). Invoke the `thejudge-quality-check` skill with the Skill tool (skill name `thejudge-quality-check`) on the package `PRD/work/ai-answer-quality-baseline/`, and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/preparation-contract.md`, emit an explicit PASS or FAIL verdict with the complete findings, and return it to the driver, which records it in the package README's `## Preparation gate` section. Read `.claude/skills/thejudge-quality-check/SKILL.md`, `PRD/instructions/technical-design-rules.md`, `PRD/instructions/plain-language-standard.md`, `PRD/instructions/requirement-format.md`, and `PRD/instructions/graph-workflow-contract.md` (`## The two runs`, `## Propose / apply / close`) before acting.

Where you are: the checkout is the build worktree named above, on the shared branch `thejudge-auto/ai-answer-quality-baseline-work`, cut from `origin/main` at `787ca5f` (the docs PR #200 is merged; PRs #201 and #202 landed after it). Every command runs there; never touch the launch checkout at `/Users/chrismiho/Coding/Projects/TheJudge`.

Why this re-grade exists: the owner answered the ten verdict slots in `GATE-QUESTIONS.md` with 6 `edit` and 4 `accept`, and `graph-gate-review` applied each `edit` inside that ID's proposed diff (see `GRAPH-RUN.md` `## Gate verdicts` and each block's `- Reason:` line). The verdicts are the owner's decision and are not yours to reopen: grade the finalized proposal as it stands. `DESIGN-BRIEF.md` was written before those edits and has not been touched since; whether it still agrees with the finalized proposal is one of the things you are grading.

What to check, beyond the skill's own checklist:
1. `DESIGN-BRIEF.md` against the finalized `GATE-QUESTIONS.md` and the current-state specs it touches (`PRD/sections/system-map.md` `## Eval harness`, `PRD/sections/non-functional-requirements.md` NFR-018 and NFR-002, `PRD/sections/functional-requirements.md` REQ-032, REQ-146, REQ-177, REQ-182, REQ-184, `PRD/sections/goals-and-non-goals.md`, `PRD/sections/integrations-and-data.md`, `PRD/sections/system-map/game-rules-retrieval.md`): no contradictions between the brief and the finalized proposal (gold-set definition and size, judge model and its own setting, the answer-model lineup, per-leg unit, latency recording, the blind side-by-side rank, the cost note), current vocabulary, technical-design-rules respected (one endpoint, no new product surface, no CI gate on model output, mock-first provider boundary), scope implementable without hidden assumptions, and no user-visible screen change. A brief whose scope, measurement plan, assumptions, or cost estimate contradicts an applied edit is a finding; name each contradiction with the brief line and the block it contradicts.
2. Implementability of the edited proposal against the code, offline: the OpenAI provider under `apps/backend/src/providers/` — confirm what request fields it sends per model and whether the lineup `gpt-4.1-mini`, `gpt-4.1`, `gpt-5-mini`, `gpt-5-nano` and judge `gpt-5` can be addressed through the existing model setting without a provider change beyond the model id (report any request parameter the provider hard-codes that a listed model family rejects, as a finding if the brief or proposal assumes it works unchanged); the data the tier definitions rely on — count `Example:` lines in `apps/backend/data/cr/source.txt` and confirm the rulings file `cardRulingsByOracleId.json` exists with its stated shape (report your counts next to the proposal's 277 and 76,605 / 19,542); the `preparation.ts` cap-of-5 call sites (expect four); `npm run eval:worked-solutions` under `EMBEDDING_PROVIDER=local` (offline, expect 6/6). Run each measurement once. Do not run `npm run benchmark:rag-retrieval` (it rewrites tracked result files) and make no live provider call.
3. `GATE-QUESTIONS.md` after the edits: every `## <STABLE-ID>` block still opens with the three-line plain-language block, inlines every cited id, defines its technical terms, carries a complete diff (never a summary) that now reflects its `- Reason:` where the verdict is `edit`, and keeps `- Verdict:` / `- Reason:` filled. Verify every `Current:` excerpt byte-for-byte against the live `PRD/sections/` file at this checkout by script, not by eye (`main` moved since the last check). Confirm REQ-185 through REQ-190 are still unused in live `PRD/sections/` and the next free numbers after REQ-184. Heading hygiene: every top-level `## ` heading is a stable-id block or `## Blocker questions`, with the two `## Eval harness` excerpts fenced. Re-grep the amendment set (`worked-solution`, `NFR-018`, `report-only`, `eval harness`, `answer quality`, `judge`, `gold`, `quality:check`, `five excerpts`, `top five`, `separate scope`, `REQ-146`, plus the new terms `lineup`, `gpt-5`, `latency`, `tier`) and FAIL if a live assertion the finalized change would falsify has no block.
4. Package state: `STATUS.refined` is the only marker, `README.md` reads `status: refined`, the board row sits under `## refined` in `PRD/work/STATUS.md`, `README.md` carries `## Autonomous metadata` reading `origin/main`, and `intake/` is untouched.

Verdict handling: on PASS, change nothing (leave `STATUS.refined`; do not write `GAMEPLAN.md`, slice docs, or code; do not edit the README, the driver writes the gate section). On FAIL, set `status: refining` in `README.md`, replace the marker with `STATUS.refining`, move the board row under `## refining`, commit with explicit paths only (`git add PRD/work/ai-answer-quality-baseline PRD/work/STATUS.md`, never `git add -A`, `--all`, or `.`) with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01Kg5qFLd4xUzdprSi4LUGua
and push `origin thejudge-auto/ai-answer-quality-baseline-work` (no force). Never edit `DESIGN-BRIEF.md`, `GATE-QUESTIONS.md`, `PRD/sections/`, code, `intake/`, or `GRAPH-RUN.md`.

Constraints: tool-call cap 60 for this node, counting every call, so budget them (the last two attempts spent 44–45). Do not dispatch subagents; a helper's calls charge this node's own budget. Never run `npm run data:refresh` or any Scryfall refresh. Never touch the lock, the stop sentinel, or any `.worktrees/.graph-*` file.

Report back, in this order: the one-word verdict (PASS or FAIL); the complete findings list, or `none`, each with file and line; the brief-versus-finalized-proposal result; the provider and data checks with your counts beside the proposal's; the `Current:` excerpt verification result (count checked, count identical); the heading-hygiene result; the amendment-set grep result; each reproduced measurement with the brief's value beside it; the package state after the node; the commit hash if you committed; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### define (build half, attempt 1 — gate-qc loop 1 of 3)

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 3 (`define`), build-half attempt 1 (gate-qc loop 1 of 3), of graph run `graph-20260907-032104` (the build half driven by `graph-implement`; the spec-forming half was `graph-20260906-092312`). Invoke the `thejudge-refinement` skill with the Skill tool (skill name `thejudge-refinement`) on the package `PRD/work/ai-answer-quality-baseline/`, and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/preparation-contract.md` and replace the approval pause with its conservative assumption ladder, applied per question; record every material assumption and its evidence in `DESIGN-BRIEF.md`; and when a question meets the genuine decision blocker test, preserve the artifacts and write it under `## Blocker questions` rather than guessing. Read `.claude/skills/thejudge-refinement/SKILL.md`, `PRD/instructions/requirement-format.md`, `PRD/instructions/technical-design-rules.md`, `PRD/instructions/plain-language-standard.md`, `PRD/instructions/workflow-reference.md`, and `PRD/instructions/graph-workflow-contract.md` (`## The two runs`, `## Propose / apply / close`) before acting.

Where you are: the checkout is the build worktree named above, on the shared branch `thejudge-auto/ai-answer-quality-baseline-work`, cut from `origin/main` at `787ca5f`. Every command runs there; never touch the launch checkout at `/Users/chrismiho/Coding/Projects/TheJudge` except to read the one file named under finding 7 below.

What happened before you: refinement wrote `DESIGN-BRIEF.md` and a ten-block `GATE-QUESTIONS.md` on 2026-09-06; the owner answered every slot (6 `edit`, 4 `accept`, 0 `reject`) and `graph-gate-review` applied each `edit` inside that ID's proposed diff (`GRAPH-RUN.md` `## Gate verdicts`; each block's `- Reason:` line is the owner's own wording). The docs PR #200 merged. The build-half `gate-qc` then FAILed the package with seven findings, recorded in the README's `## Preparation gate` and in `GRAPH-RUN.md` row 4 (build half). The finalized proposal is the owner's decision record. The brief follows it, never the reverse: you do not reopen, soften, or re-argue any verdict.

Your job, in product terms: make the design brief describe the instrument the owner actually approved — a multi-model bake-off over a tiered, all-official gold set, judged by a stronger model that scores each answer alone and then ranks all answers to a question blind — so that `plan` and `build` read one consistent story. Specifically:
1. Findings 1–6 (the brief lags the edits). Rewrite the affected parts of `DESIGN-BRIEF.md` to match the finalized `GATE-QUESTIONS.md`: the scope and what-it-changes lines (a configured answer-model lineup for the eval run only; production model, provider request shape, and `OPENAI_MODEL` untouched); the non-goals (drop the six-cases-and-one-rubric and no-model-swap wording; keep what is still a non-goal — no production model change, no reasoning-effort setting, no CI gate, no new player-facing surface, no community answers); the judge section (three layers plus the blind side-by-side rank, judge default `gpt-5` under `ANSWER_QUALITY_JUDGE_MODEL`, flagged when it matches any answer model); assumption A9 and any assumption the edits falsify, plus a new assumption for model access checked by the dry run; M3 re-derived for the lineup (`gpt-4.1-mini`, `gpt-4.1`, `gpt-5-mini`, `gpt-5-nano`; judge `gpt-5`) × the seeded case count × 2 caps plus the two judge passes, from M2's measured prompt size and published prices, labelled an estimate with a re-check before the first run; the measurement plan and artifact fields (per-leg unit model × cap, latency in ms per call, the blind rank, the headline count per model per cap, the incomparable-versus-model-comparison rule, the first live run crossing every model with cap 5 and cap 10); and the gold-set section (tiers 1 and 2 as the owner defined them, the four-field validity test, at least the six named cases plus roughly a dozen hand-picked seeds from common-mistake topics, human-reviewed, combos out). Cite the block each change follows.
2. Finding 7 (a factual error inside REQ-185's edited diff). `apps/backend/data/cr/source.txt` is not committed: `.gitignore:47` ignores it, `PRD/sections/integrations-and-data.md:255` says it must not be committed, `scripts/refresh-scryfall-data.mjs` writes it (a network refresh a run never issues) and `scripts/build-game-rules.mjs` reads it. The launch checkout holds a copy (971,061 bytes, 277 `Example:` lines, measured 2026-09-07 at `/Users/chrismiho/Coding/Projects/TheJudge/apps/backend/data/cr/source.txt`); this worktree does not. First check whether the committed rules index that `build-game-rules.mjs` emits still carries the `Example:` text (name the file and count them there); if it does, tier-1 authoring reads the committed index and the diff should say so. If it does not, state plainly in the block how tier-1 authoring gets the text — copying the launch checkout's gitignored file into the same gitignored path in the worktree, or the owner running `npm run data:refresh` before build — and name the count's true source. Correct the provenance wording inside REQ-185's proposed diff and, only where it repeats the error, the brief; keep the owner's `- Verdict: edit` and `- Reason:` lines byte-identical, keep the tiers, the counts, and every other sentence of the block as they stand. This is a provenance correction inside a diff the owner already edited, not a new product decision; if the three-condition test says otherwise, write the question under `## Blocker questions` and leave the block unchanged.
3. Touch no other block's diff. Reserve no new stable ids. Never edit `PRD/sections/`, code, `GRAPH-RUN.md`, or `intake/`. Do not run `npm run data:refresh`, `npm run benchmark:rag-retrieval`, or any live provider call; measurements are offline (the worked-solution cases, the rulings file, the committed index, the four `preparation.ts` cap sites, M2's prompt size if you re-derive it).

Writes, inside `PRD/work/ai-answer-quality-baseline/` and the board only: `DESIGN-BRIEF.md`; `GATE-QUESTIONS.md` (the REQ-185 block only, as above); the README `status:` line and, if you update the intro paragraph's block summary, that paragraph; the marker `STATUS.refining` → `STATUS.refined` when the brief is complete (exactly one marker); the board row in `PRD/work/STATUS.md` moved to `## refined` with a note naming this reconciliation. Commit with explicit paths only (`git add PRD/work/ai-answer-quality-baseline PRD/work/STATUS.md`; never `git add -A`, `--all`, or `.`) with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01Kg5qFLd4xUzdprSi4LUGua
and push `origin thejudge-auto/ai-answer-quality-baseline-work` (no force).

Constraints: tool-call cap 150 for this node, counting every call. Do not dispatch subagents; a helper's calls charge this node's budget. Never touch the lock, the stop sentinel, or any `.worktrees/.graph-*` file.

Report back, in this order: the reconciliation summary in three sentences; for each of the seven findings, what you changed (file, section, and the block it now follows) or the blocker question you wrote instead; the finding-7 resolution with the index file name and your `Example:` count; the re-derived M3 estimate with its inputs; the material assumptions added or changed; the package state after the node (marker, README status, board heading); the commit hash and push result; `git status --porcelain` (expect empty); the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### gate-qc (build half, attempt 2)

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 4 (`gate-qc`), build-half attempt 2, of graph run `graph-20260907-032104` (the build half driven by `graph-implement`; the spec-forming half was `graph-20260906-092312`). Invoke the `thejudge-quality-check` skill with the Skill tool (skill name `thejudge-quality-check`) on the package `PRD/work/ai-answer-quality-baseline/`, and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/preparation-contract.md`, emit an explicit PASS or FAIL verdict with the complete findings, and return it to the driver, which records it in the package README's `## Preparation gate` section. Read `.claude/skills/thejudge-quality-check/SKILL.md`, `PRD/instructions/technical-design-rules.md`, `PRD/instructions/plain-language-standard.md`, `PRD/instructions/requirement-format.md`, and `PRD/instructions/graph-workflow-contract.md` (`## The two runs`, `## Propose / apply / close`) before acting.

Where you are: the checkout is the build worktree named above, on the shared branch `thejudge-auto/ai-answer-quality-baseline-work` at `2de4800`, cut from `origin/main` at `787ca5f`. Every command runs there; never touch the launch checkout at `/Users/chrismiho/Coding/Projects/TheJudge`.

What happened before you: attempt 1 of this node (2026-09-07, `GRAPH-RUN.md` row 4 build half; the README's `## Preparation gate` holds the seven findings) FAILed because `DESIGN-BRIEF.md` still described the pre-edit instrument and REQ-185's diff mis-stated where the CR `Example:` text lives. `define` then reconciled the brief with the finalized `GATE-QUESTIONS.md` (commit `2de4800`: brief +389/−99; `GATE-QUESTIONS.md` changed on two lines inside the REQ-185 block only; no `- Verdict:` or `- Reason:` line changed). The owner's verdicts are the decision record and are not yours to reopen: grade the brief against the finalized proposal as it stands.

What to check, beyond the skill's own checklist:
1. Each of the seven attempt-1 findings, closed or not: read the README's `## Preparation gate` list, then confirm in `DESIGN-BRIEF.md` that the scope and what-it-changes lines, the non-goals, the gold-set section, the judge section, assumption A9, M3, and the measurement plan now follow the finalized REQ-185, REQ-186, REQ-188, REQ-189, and REQ-190 (lineup as a run option with production and `OPENAI_MODEL` untouched; tiers 1–2 with the four-field validity test and ≥6 plus ~12 seeds; four judge layers incl. the blind side-by-side rank; judge default `gpt-5` under `ANSWER_QUALITY_JUDGE_MODEL`, flagged on a match; per-leg unit model × cap; latency in ms; headline per model per cap; the first live run crossing every model with cap 5 and cap 10). Any remaining contradiction is a finding with the brief line and the block.
2. Finding 7's fix: count `Example:` occurrences in `apps/backend/data/gameRulesRuleIndex.json` (the brief and REQ-185 claim 277 lines across 215 entries) and confirm the REQ-185 block's tier-1 criterion and measurement note now name that committed index, with `apps/backend/data/cr/source.txt` named only as the raw uncommitted download. Confirm by `git diff fe4435a..2de4800 -- PRD/work/ai-answer-quality-baseline/GATE-QUESTIONS.md` that only the REQ-185 block changed and that the change is provenance wording, not a change to the tiers, the counts, or the owner's lines.
3. M3 as re-derived: the brief must show its inputs (call count from lineup × cases × caps plus the two judge passes, token sizes from M2/M4, the prices used) and label the result an estimate with a re-check before the first run; no unmeasured numeric acceptance gate anywhere in the brief. Reproduce once only the measurements whose brief value changed since attempt 1 (the index counts, M2/M4 prompt sizes if the brief names a runnable command, the seeded case arithmetic); attempt 1 already reproduced the worked-solution eval (6/6), the rulings file (19,542 / 76,605), and the four `preparation.ts` cap sites, and those values did not change, so cite row 4 instead of re-running them. Do not run `npm run benchmark:rag-retrieval` and make no live provider call.
4. `GATE-QUESTIONS.md`: every `## <STABLE-ID>` block still opens with the three plain-language lines, carries a complete diff, and keeps `- Verdict:` / `- Reason:` filled; verify every `Current:` excerpt byte-for-byte against live `PRD/sections/` at this checkout by script; REQ-185 through REQ-190 still unused in live `PRD/sections/`; heading hygiene (top-level `## ` headings are the ten stable-id blocks plus `## Blocker questions`; the two `## Eval harness` excerpts fenced). Re-grep the amendment set (`worked-solution`, `NFR-018`, `report-only`, `eval harness`, `answer quality`, `judge`, `gold`, `quality:check`, `five excerpts`, `top five`, `separate scope`, `REQ-146`, `lineup`, `gpt-5`, `latency`, `tier`) against live `PRD/sections/` and FAIL if a live assertion the finalized change would falsify has no block.
5. Technical-design-rules and package state: one endpoint, no new player-facing surface, no CI gate on model output, mock-first provider boundary, no screen change; `STATUS.refined` is the only marker, `README.md` reads `status: refined`, the board row sits under `## refined`, `## Autonomous metadata` reads `origin/main`, `intake/` untouched.

Verdict handling: on PASS, change nothing (leave `STATUS.refined`; do not write `GAMEPLAN.md`, slice docs, or code; do not edit the README, the driver writes the gate section). On FAIL, set `status: refining` in `README.md`, replace the marker with `STATUS.refining`, move the board row under `## refining`, commit with explicit paths only (`git add PRD/work/ai-answer-quality-baseline PRD/work/STATUS.md`, never `git add -A`, `--all`, or `.`) with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01Kg5qFLd4xUzdprSi4LUGua
and push `origin thejudge-auto/ai-answer-quality-baseline-work` (no force). Never edit `DESIGN-BRIEF.md`, `GATE-QUESTIONS.md`, `PRD/sections/`, code, `intake/`, or `GRAPH-RUN.md`.

Constraints: tool-call cap 60 for this node, counting every call, so budget them (attempt 1 spent 42 with more measurements than you have). Do not dispatch subagents; a helper's calls charge this node's own budget. Never run `npm run data:refresh` or any Scryfall refresh. Never touch the lock, the stop sentinel, or any `.worktrees/.graph-*` file.

Report back, in this order: the one-word verdict (PASS or FAIL); the complete findings list, or `none`, each with file and line; the seven attempt-1 findings each marked closed or open with the brief line that closes it; the finding-7 checks with your `Example:` count and the diff scope; the M3 inputs check; the `Current:` excerpt verification result (count checked, count identical); the heading-hygiene result; the amendment-set grep result; the package state after the node; the commit hash if you committed; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### plan

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 5 (`plan`) of graph run `graph-20260907-032104` (the build half driven by `graph-implement`). Invoke the `thejudge-map-out` skill with the Skill tool (skill name `thejudge-map-out`) on the package `PRD/work/ai-answer-quality-baseline/`, and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/preparation-contract.md`, require `Quality-check: PASS` in the package README's `## Preparation gate` section (present, attempt 2, 2026-09-07 — never self-certify it), create the GAMEPLAN and slice contract, and return control to the driver. Read `.claude/skills/thejudge-map-out/SKILL.md` and its `reference.md` (slice template, Ship gates block, the `.criteria.json` schema and worked example), `PRD/instructions/doc-lifecycle.md`, `PRD/instructions/workflow-reference.md`, `PRD/instructions/runtime-process-hygiene.md`, `PRD/instructions/requirement-format.md`, and `PRD/instructions/graph-workflow-contract.md` (`## Propose / apply / close`, `## Acceptance criteria are earned, not written`) before acting.

Where you are: the checkout is the build worktree named above, on the shared branch `thejudge-auto/ai-answer-quality-baseline-work` at `b7f1882` (plus this dispatch's ledger commit), cut from `origin/main` at `787ca5f`. Every command runs there; never touch the launch checkout at `/Users/chrismiho/Coding/Projects/TheJudge`.

What you are planning, in product terms: an answer-quality instrument for Ask AI — a repeatable, human-reviewable bake-off that asks each model in a configured lineup (`gpt-4.1-mini`, `gpt-4.1`, `gpt-5-mini`, `gpt-5-nano`) every gold case at excerpt cap 5 and cap 10, has a stronger judge (`gpt-5` by default, under `ANSWER_QUALITY_JUDGE_MODEL`) score each answer alone on four 0–2 axes against the published official answer and then rank all answers to a question blind, and writes a small committed scorecard with transcripts kept out of git. It is measurement, not a player-facing feature: no new screen, endpoint, request contract, provider, or CI gate on model output. Read `DESIGN-BRIEF.md` (reconciled 2026-09-07) and the finalized `GATE-QUESTIONS.md` in full — the ten blocks there (REQ-185 through REQ-190 new; NFR-018, `system-map.md` `## Eval harness`, `goals-and-non-goals.md`, REQ-146 amended) are the product truth `build` will write into `PRD/sections/` by intent, together with the code, and every acceptance criterion traces to one of them or to the brief's measurement plan.

Slicing rules for this package:
- One primary objective per slice, parallel-ready by default, sequential only with a stated blocker. Expect the natural cuts to be: the gold-set schema, validity test, and the tier-1/tier-2 seed cases (authored from the committed `apps/backend/data/gameRulesRuleIndex.json` `Example:` entries and `cardRulingsByOracleId.json`, human-reviewed per the brief); the run command (`npm run eval:answer-quality`) with its dry run, `--confirm-live-calls` gate, lineup and cap legs, latency capture, and the excerpt cap as a run parameter (the four `preparation.ts` literals becoming one named constant, production unchanged at five); the judge (lone scoring, blind side-by-side rank, the judge-matches-answer-model flag); the artifact and its gitignored transcripts; and the `PRD/sections/` apply step. Choose the actual cut from the brief, not from this list.
- Every acceptance criterion is verifiable by a command or an explicit manual check, and each gets a `.criteria.json` entry authored beside it with `value` `false` and an `evidence` block (a command pattern, file paths, or `"manual": true`). Only the `build` node earns evidence, and a criterion proves a command ran, not that it passed — so word command criteria around a test or script that fails loudly.
- No live provider call inside any acceptance-criterion command. The dry run, the mock provider path, and unit tests carry the automated criteria; the first live run and the first human review pass are `manual` criteria or an explicit baseline-to-be-recorded, never a numeric acceptance gate (the brief sets no cost target and no correctness target). Never plan `npm run data:refresh`, a Scryfall refresh, or a new dependency without cause.
- Every quantitative claim a criterion checks must already be measured in the brief (M1–M6) or be recorded as a baseline by the slice; do not invent a target.
- Each slice that touches `PRD/sections/` names the exact `GATE-QUESTIONS.md` blocks it applies, and the final slice carries the PRD promotion checklist and the Ship gates block from `reference.md`. `PRD/sections/` is applied at `build`, not by you.
- Browser-observable risk is nil (no screen change); say so rather than adding Playwright criteria.

Writes, inside `PRD/work/ai-answer-quality-baseline/` and the board only: `GAMEPLAN.md`; `slice-<letter>-<name>.md` per slice; `slice-<letter>.criteria.json` per slice; the README's slice table, implementation map, and `status: active`; the marker `STATUS.refined` → `STATUS.active` (exactly one marker); the board row moved to `## active` in `PRD/work/STATUS.md`. Never write product code, `PRD/sections/`, `DESIGN-BRIEF.md`, `GATE-QUESTIONS.md`, `GRAPH-RUN.md`, or `intake/`; never persist plans anywhere but `PRD/work/`. Commit with explicit paths only (`git add PRD/work/ai-answer-quality-baseline PRD/work/STATUS.md`; never `git add -A`, `--all`, or `.`) with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01Kg5qFLd4xUzdprSi4LUGua
and push `origin thejudge-auto/ai-answer-quality-baseline-work` (no force).

Constraints: tool-call cap 120 for this node, counting every call. Do not dispatch subagents; a helper's calls charge this node's budget. No live provider calls, no network beyond git. Never touch the lock, the stop sentinel, or any `.worktrees/.graph-*` file.

Report back, in this order: the slice list (letter, name, primary objective, dependencies, the `GATE-QUESTIONS.md` blocks it applies); the criteria count per slice and how many are `manual`; the verification checklist from `GAMEPLAN.md`; the package state after the node (marker, README status, board heading); the commit hash and push result; `git status --porcelain` (expect empty); the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### build

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 6 (`build`) of graph run `graph-20260907-032104` (the build half driven by `graph-implement`). Invoke the `thejudge-implement-all` skill with the Skill tool (skill name `thejudge-implement-all`) on the package `PRD/work/ai-answer-quality-baseline/`, and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/graph-workflow-contract.md` (`## Propose / apply / close`, `## Acceptance criteria are earned, not written`, `## Boundaries`), take the recorded autonomous base from the package README's `## Autonomous metadata` (`origin/main`), work in place in this worktree, and treat every stop as a park reported to the driver — a blocked slice, an unresolvable gate failure, or a rebase conflict whose intent is not derivable ends the node `failed` with the evidence. Read `.claude/skills/thejudge-implement-all/SKILL.md` and its `reference.md`, `PRD/instructions/workflow-reference.md`, `PRD/instructions/requirement-format.md`, the package `README.md`, `GAMEPLAN.md`, every `slice-*.md` and `slice-*.criteria.json`, `DESIGN-BRIEF.md`, and the finalized `GATE-QUESTIONS.md` before acting.

Where you are, and the branch: the checkout is the build worktree named above, checked out on the shared branch `thejudge-auto/ai-answer-quality-baseline-work` (cut from `origin/main` at `787ca5f`, already pushed; head `a18fe29` plus this dispatch's ledger commit). That branch name is supplied here explicitly, as the skill requires; confirm it equals `git branch --show-current` and block if it does not. No second worktree, no contributor branch. Every path you write lies inside `/Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline/` — the work package lives there too, so a bare `PRD/work/ai-answer-quality-baseline/…` path resolved against the launch checkout is out of scope and fails this node on return. Never touch the launch checkout at `/Users/chrismiho/Coding/Projects/TheJudge`.

What you are building, in product terms: the instrument that grades whether Ask AI's answer is right, not just whether the right rule reached the prompt — a bake-off that asks each model in the lineup (`gpt-4.1-mini`, `gpt-4.1`, `gpt-5-mini`, `gpt-5-nano`) every gold case at excerpt cap 5 and cap 10, has the judge (`gpt-5` by default under `ANSWER_QUALITY_JUDGE_MODEL`) score each answer alone on four 0–2 axes against the published official answer and then rank all answers to a question blind, and writes a small committed scorecard with transcripts kept out of git. Production stays untouched: the deployed cap stays five, `OPENAI_MODEL` still picks the app's answer model, the provider still sends only model and prompt, no new endpoint, screen, or CI gate on model output.

Slices, in GAMEPLAN order: A gold set and validity (REQ-185), B cap constant and run command (REQ-188, REQ-190), C judge and rubric (REQ-186, REQ-187), D artifact and transcripts (REQ-189), then E integrate and apply (NFR-018, REQ-146, `system-map.md` `## Eval harness`, `goals-and-non-goals.md`), which depends on A–D. Apply the product truth as the GAMEPLAN assigns it: each of A–D writes its own `GATE-QUESTIONS.md` block into `PRD/sections/` **by intent, re-derived against current truth**, together with the code in that slice's commit; E writes the four amendments. Apply each block exactly once across the run; there are no `reject`ed ids. Seed the tier-1 cases from the committed `apps/backend/data/gameRulesRuleIndex.json` `Example:` entries and tier-2 cases from `cardRulingsByOracleId.json`, each passing the four-field validity test; the raw CR download under `apps/backend/data/cr/` is gitignored and absent here, and you never fetch it.

How criteria are earned: a committed `PreToolUse` hook watches this node's tool calls, matches them against each criterion's `evidence` block (a command pattern or file paths), and appends the earned ids to `.worktrees/.graph-evidence.jsonl`; only this node earns evidence. A write that flips a criterion to `true` before its evidence is in the log is denied, and the denial names the criterion and the missing evidence — run the evidence command or write the evidence path, then flip. Never touch `.worktrees/.graph-*` files, the lock, or the stop sentinel. A criterion proves a command ran, not that it passed, so run the real gates and read their output: the slice verification while the slice is `in-progress`, then `npm run quality:check`, both green before a slice is `done`.

The two criteria a human must earn — E9 (the first live run, executed with owner confirmation across the full lineup × both caps) and E10 (a human read the full written record) — are not yours to earn. Make no paid provider call and write no observation line claiming a human did anything. Finish everything else in E (the wiring, the regression guard, the worked-solutions README, the four `PRD/sections/` amendments, `npm run quality:check`, the dry run of `npm run eval:answer-quality` with no flag proving the plan prints and no network call is made), then: write the exact owner steps into slice E's status notes (the command and flags, the M3 cost estimate, where the artifact lands, what the human review reads, which two criteria they flip and how), mark E `blocked` on E9 and E10 only, leave E9 and E10 `false`, keep `STATUS.active` and the README slice table accurate, commit slice E's work locally as `feat(ai-answer-quality-baseline): slice E — wired and applied; E9/E10 await the owner` and report the hash (the driver pushes it with the park), and end the node `failed` naming the two open criteria as the blocker. Every other slice is `done` only when all of its criteria are `true` in the emitted file.

Publishing: one milestone commit per green slice, `feat(ai-answer-quality-baseline): complete slice <letter>`, with explicit paths only (never `git add -A`, `--all`, or `.`) and the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_01Kg5qFLd4xUzdprSi4LUGua
then fetch, rebase onto `origin/thejudge-auto/ai-answer-quality-baseline-work`, rerun both gates after any upstream change, and push `HEAD` to `origin thejudge-auto/ai-answer-quality-baseline-work` without force. After the first push, open the code PR with `gh pr create --base main --head thejudge-auto/ai-answer-quality-baseline-work`, title `[THEJUDGE-AUTO][READY] Answer-quality baseline (ai-answer-quality-baseline)`, body per `PRD/instructions/plain-language-standard.md` (open with what a player or the owner gets, then the slices, the applied product truth, the verification, and the owner's two remaining steps), ending with the line `🤖 Generated with [Claude Code](https://claude.com/claude-code)` and the session URL `https://claude.ai/code/session_01Kg5qFLd4xUzdprSi4LUGua`. Never merge or close the PR. A denied or unlisted command is never retried — report it verbatim and stop.

Constraints: tool-call cap 1200 for this node, counting every call. No implementation subagents. No live provider calls of any kind. Never run `npm run data:refresh`, any Scryfall refresh, or `npm run benchmark:rag-retrieval` (it rewrites tracked result files). Never edit a `thejudge-*` skill, `CLAUDE.md`, or `.claude/settings*.json`. Never force-push, never `git reset --hard`, never drop or pop a stash. Browser risk is nil; start no dev server.

Report back, in this order: per slice, its final status, its commit hash, and its criteria state read from the emitted `slice-<letter>.criteria.json` (count true / count total); the PR URL and head; the `PRD/sections/` blocks applied and in which commit; the dry-run output summary for `npm run eval:answer-quality`; the exact owner steps you wrote for E9/E10; every path you wrote, launch-root-relative (each must begin `.worktrees/implement-ai-answer-quality-baseline/`); `git status --porcelain` in the worktree; any denied command verbatim; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### build (attempt 2 — gate resolved, confirm 41/41 and set ship-ready)

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 6 (`build`, attempt 2) of graph run `graph-20260907-032104`, the build half driven by `graph-implement`. Invoke the `thejudge-implement-all` skill with the Skill tool (skill name `thejudge-implement-all`) on the package `PRD/work/ai-answer-quality-baseline/` and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/graph-workflow-contract.md` (`## Acceptance criteria are earned, not written`, `## Boundaries`), take the recorded autonomous base from the package README's `## Autonomous metadata` (`origin/main`), work in place in this worktree, and treat every stop as a park reported to the driver — end the node `failed` with the evidence rather than waiting for an answer nobody is there to give. Read `.claude/skills/thejudge-implement-all/SKILL.md` and its `reference.md`, `PRD/instructions/workflow-reference.md`, the package `README.md`, `GAMEPLAN.md`, every `slice-*.md` and `slice-*.criteria.json`, and `GRAPH-RUN.md` (`## Open gate`, the last two driver rows of `## Node ledger`) before acting.

Where you are, and the branch: the checkout is the build worktree named above, checked out on the shared branch `thejudge-auto/ai-answer-quality-baseline-work` (cut from `origin/main` at `787ca5f`; head `104642d` plus the driver's gate-resolution commit, all pushed). That branch name is supplied here explicitly, as the skill requires; confirm it equals `git branch --show-current` and block if it does not. No second worktree, no contributor branch. Every path you write lies inside `/Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline/` — the work package lives there too, so a bare `PRD/work/ai-answer-quality-baseline/…` path resolved against the launch checkout is out of scope and fails this node on return. Never touch the launch checkout at `/Users/chrismiho/Coding/Projects/TheJudge`.

What this attempt is: a confirmation pass, not a build. Attempt 1 built slices A–E and parked on the two human-only criteria; since then the owner cleared and the driver ran the live bake-off three times, corrected the instrument (tier-2 cases now asked with their card attached, the question embedded by the local provider, silent fallback refused), re-ran it, and the owner read the record — E9 and E10 are `true` in `slice-e.criteria.json`, every slice doc reads `## Status: done`, and the committed `results.json` is run 3. Your job is to prove the package is ship-ready from the files, not to change what it does. Do not add, edit, or remove code, tests, gold cases, or `PRD/sections/` text; do not re-run the paid bake-off (`npm run eval:answer-quality -- --confirm-live-calls` is a live provider call and is forbidden here); do not flip any criterion — none needs flipping, and a flip to `true` without hook evidence is denied.

Do, in order: (1) read every `slice-<letter>.criteria.json` and confirm 41/41 `true` and every slice doc `## Status: done`; (2) run the slice verification gates and read their output — `npm run test:scripts`, `npm --prefix apps/backend run test`, `npm run quality:check`, `npm run eval:answer-quality` with no flag (the dry plan; it makes a models-list request only when a key is present and never a completion), and `npm run eval:worked-solutions` — all must exit 0; a red gate ends the node `failed` with the output as evidence; (3) `git fetch origin` and confirm `HEAD` equals `origin/thejudge-auto/ai-answer-quality-baseline-work` (rebase without force if not); (4) apply the skill's status transition for a package whose every slice is `done`: README front matter `status: ship-ready`, `git mv` the marker `STATUS.active` to `STATUS.ship-ready`, move the package's board row from `## active` to `## ship-ready` in `PRD/work/STATUS.md` (remove it from `## active`, add it under `## ship-ready` with the table header, reworded for the new state), and refresh the README `## Slices` table if it still shows slice E as blocked; (5) refresh the open PR #203 with `gh pr edit 203` so its title reads `[THEJUDGE-AUTO][READY] Answer-quality baseline (ai-answer-quality-baseline)` and its body, per `PRD/instructions/plain-language-standard.md`, opens with what the owner gets (a bake-off instrument whose run 3 shows gpt-4.1 fully correct on 16 of 18 gold cases at the production excerpt cap of five and 18 of 18 at cap ten, at 3.4–3.5 s), then the five slices, the applied product truth (REQ-185 through REQ-190, NFR-018, REQ-146, the system-map entry, the goals non-goal), the instrument correction, the verification you ran, and the note that `close` will add the receipt and delete the package on this branch before the owner merges — ending with the line `🤖 Generated with [Claude Code](https://claude.com/claude-code)` and the session URL `https://claude.ai/code/session_017nxK3L7uehw5VbcfSXfke1`; never merge or close the PR; (6) commit the status transition with explicit paths only (never `git add -A`, `--all`, or `.`) as `chore(ai-answer-quality-baseline): ship-ready — 41/41 criteria confirmed after the resolved build gate` with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_017nxK3L7uehw5VbcfSXfke1
and push `HEAD` to `origin thejudge-auto/ai-answer-quality-baseline-work` without force; (7) report `ok` only when every criterion in every slice file is `true` and the gates were green.

Constraints: tool-call cap 1200 for this node, counting every call. No implementation subagents. No live provider completion calls of any kind. Never run `npm run data:refresh`, any Scryfall refresh, or `npm run benchmark:rag-retrieval` (it rewrites tracked result files). Never touch `.worktrees/.graph-*` files, the lock, or the stop sentinel. Never edit a `thejudge-*` skill, `CLAUDE.md`, or `.claude/settings*.json`. Never force-push, never `git reset --hard`, never drop or pop a stash. A denied or unlisted command is never retried — report it verbatim and stop. Browser risk is nil; start no dev server.

Report back, in this order: per slice, its status and its criteria state read from the emitted `slice-<letter>.criteria.json` (count true / count total); the exit status and one-line summary of each gate you ran; the commit hash and the PR URL with its head after your push; every path you wrote, launch-root-relative (each must begin `.worktrees/implement-ai-answer-quality-baseline/`); `git status --porcelain` in the worktree; any denied command verbatim; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### review

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 7 (`review`) of graph run `graph-20260907-032104`, a fresh-context, no-write reviewer. You hold read and search tools only — no Write, no Edit, no NotebookEdit — and you change nothing: not a file, not a branch, not the PR. You have not seen how this work was built and must not go looking for a build transcript; you grade the artifacts as they stand.

What you are grading: the branch `thejudge-auto/ai-answer-quality-baseline-work` in the worktree named above, head `41efa41`, against `origin/main` at `787ca5f`. Get the change with `git diff --stat origin/main...HEAD` and `git diff origin/main...HEAD -- <path>` per file (62 files; read the code and tests fully, skim the large committed data and PRD prose for the claims the criteria make). The package is `PRD/work/ai-answer-quality-baseline/`: read `GAMEPLAN.md`, every `slice-*.md`, every `slice-*.criteria.json`, `DESIGN-BRIEF.md`, the finalized `GATE-QUESTIONS.md`, and `README.md`. Read `PRD/instructions/graph-workflow-contract.md` `## Node 7 — the no-write reviewer` for your own rules.

The rubric is the slices' own `## Acceptance criteria` sections — A1–A7, B1–B9, C1–C8, D1–D7, E1–E10 — and nothing else. For each criterion, decide from the files whether the work satisfies it, and name the evidence (a path and line, a test name, a command whose output you read). You may run read-only commands: `npm run test:scripts`, `npm --prefix apps/backend run test`, `npm run eval:answer-quality` with no flag (a dry plan; it makes a models-list request only, never a completion), `npm run eval:worked-solutions`, and any `git`, `grep`, `node -e` that reads. Never run `npm run eval:answer-quality -- --confirm-live-calls` (a paid provider call), `npm run data:refresh`, any Scryfall refresh, or `npm run benchmark:rag-retrieval` (it rewrites tracked result files). Never touch `.worktrees/.graph-*` files, the lock, or the stop sentinel.

Context that matters for E9 and E10: they are `manual` criteria. The evidence the contract allows for them is a dated observation line — slice E's `## First live run`, `## Third live run — instrument corrected`, and the owner conclusion line under `## Owner steps for E9 and E10`. Grade whether those lines exist, are dated, name what was run and read, and match the committed `apps/backend/src/eval/answer-quality/results.json` (run 3: `embeddingProvider` local, 144 case-leg records, `gitCommit` b3f860f); do not grade whether the owner's reading was thorough — that is not knowable from files.

Severity rule, binding: a finding is **Critical** only when a stated acceptance criterion is not met and the gap affects correctness of the shipped instrument or the product truth written to `PRD/sections/`; **Important** when a stated criterion is not met but the gap is contained; **Minor** for everything else. A preference, a style note, an improvement outside the slices' stated requirements, or a follow-up already recorded as a follow-up package is never Critical or Important and never loops the run back to `build`. Do not manufacture findings to look useful: an honest approval with zero findings is a valid and expected outcome for work that meets its criteria.

Report back, in this order: your verdict — `approve` (no Critical or Important finding) or `return to build` (at least one Critical or Important finding); the findings list, each with severity, the criterion id it fails, the file and line, and what would satisfy it; a per-slice line stating criteria checked and any not met; the read-only commands you ran and their exit status; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

### build (attempt 3 — review loop 1 of 2)

graph is controlling.

Working directory: /Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline

You are node 6 (`build`, attempt 3 — the first of at most two review loop-backs) of graph run `graph-20260907-032104`, the build half driven by `graph-implement`. Invoke the `thejudge-implement-all` skill with the Skill tool (skill name `thejudge-implement-all`) on the package `PRD/work/ai-answer-quality-baseline/` and follow its `## Mode` section for an orchestrator that is controlling: read `PRD/instructions/graph-workflow-contract.md` (`## Propose / apply / close`, `## Acceptance criteria are earned, not written`, `## Boundaries`), take the recorded autonomous base from the package README's `## Autonomous metadata` (`origin/main`), work in place in this worktree, and treat every stop as a park reported to the driver — end the node `failed` with the evidence rather than waiting for an answer nobody is there to give. Read `.claude/skills/thejudge-implement-all/SKILL.md` and its `reference.md`, the package `README.md`, `slice-c-judge-and-rubric.md`, `slice-d-artifact-and-transcripts.md`, `slice-e-integrate-and-apply.md`, their `.criteria.json` files, the finalized `GATE-QUESTIONS.md` blocks for REQ-186 and REQ-189, and `GRAPH-RUN.md` row 7 (`review`) before acting.

Where you are, and the branch: the checkout is the build worktree named above, checked out on the shared branch `thejudge-auto/ai-answer-quality-baseline-work` (cut from `origin/main` at `787ca5f`; head `81940a1` plus the driver's dispatch commit, all pushed). That branch name is supplied here explicitly, as the skill requires; confirm it equals `git branch --show-current` and block if it does not. No second worktree, no contributor branch. Every path you write lies inside `/Users/chrismiho/Coding/Projects/TheJudge/.worktrees/implement-ai-answer-quality-baseline/` — the work package lives there too, so a bare `PRD/work/ai-answer-quality-baseline/…` path resolved against the launch checkout is out of scope and fails this node on return. Never touch the launch checkout at `/Users/chrismiho/Coding/Projects/TheJudge`.

What this attempt is: the package is `ship-ready` (41/41 criteria `true`, every slice `done`) and the no-write reviewer approved everything except three Important findings, each a gap between the applied product truth and the code. Fix exactly those three, keep every gate green, and change nothing else in what the instrument does. Do not re-run the paid bake-off (`npm run eval:answer-quality -- --confirm-live-calls` is a live provider call and is forbidden here); do not flip any criterion (all are already `true`; a flip to `true` without hook evidence is denied); do not edit gold cases or `results.json`.

The three findings, and what satisfies each:

(1) The blind ranking produces no rationale, but REQ-189 (`PRD/sections/functional-requirements.md`, the transcripts bullet) and slice D promise the side-by-side ranking rationale per case per cap. In `apps/backend/src/eval/answer-quality/judge.ts`, make `buildRankingPrompt` ask for `{"ranks": {...}, "rationale": "one paragraph"}`, make `parseRankingResponse` read a non-empty `rationale` string and treat a missing or empty one as a malformed response (`undetermined`, the same posture the lone judge takes), thread `rationale` through `judgeBlindRanking`'s return (make it required on the determined branch, replacing the optional field), add `rationale?: string` to `RankingTranscript` in `artifact.ts`, and pass it from `scripts/eval-answer-quality.mjs`'s `writeRankingTranscript` call. Tests: in `judge.test.ts`, assert the ranking prompt requests a rationale and that a response without one is `undetermined`, and that a well-formed response returns it; in `artifact.test.ts`, assert `writeRankingTranscript` places the rationale in the file. Record in slice D's notes (a dated line) that run 3's committed ranking transcripts predate this and the next recorded run carries the rationale; do not claim run 3 has it.

(2) REQ-186's layer-2b bullet says the ranking call sees the answers together with the reference answer and the rubric, but `buildRankingPrompt` never includes `formatRubricForJudge()` (only `buildLoneJudgePrompt` does). Include the rubric text in the ranking prompt, and add a `judge.test.ts` assertion that the ranking prompt carries it. This changes no axis and no score scale, so `RUBRIC_REVISION` stays as it is; say so in slice C's notes with a dated line.

(3) In `PRD/sections/system-map.md`, the `### Answer-quality baseline` sub-entry was inserted one line too high: `### Retrieval relevance report` now ends at its `Lives in:` line, and its `- Backed by: DEC-047, REQ-032, REQ-177` line sits as a second `Backed by:` under the new sub-entry. Move that line back under `### Retrieval relevance report` so each sub-entry carries exactly one `Backed by:` line.

Also, since they are one-line fixes in files you are already editing and the reviewer recorded them as Minor: tick slice E's five `## Ship gates` boxes (the slice is `done` and every gate was verified in build attempt 2), and reword REQ-189's closing note so it no longer calls the committed `results.json` a placeholder — it holds run 3 (state the date and that the first live runs replaced the placeholder).

Then: run `npm --prefix apps/backend run test`, `npm run test:scripts`, and `npm run quality:check` and read their output (all must exit 0; a red gate ends the node `failed` with the output as evidence); `git fetch origin` and confirm `HEAD` equals `origin/thejudge-auto/ai-answer-quality-baseline-work` before committing (rebase without force if not); commit with explicit paths only (never `git add -A`, `--all`, or `.`) as `fix(ai-answer-quality-baseline): review loop 1 — ranking rationale and rubric, system-map placement` with the trailer lines
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_017nxK3L7uehw5VbcfSXfke1
and push `HEAD` to `origin thejudge-auto/ai-answer-quality-baseline-work` without force. The package stays `ship-ready`; the PR stays open and is never merged or closed.

Constraints: tool-call cap 1200 for this node, counting every call. No implementation subagents. No live provider completion calls of any kind. Never run `npm run data:refresh`, any Scryfall refresh, or `npm run benchmark:rag-retrieval` (it rewrites tracked result files). Never touch `.worktrees/.graph-*` files, the lock, or the stop sentinel. Never edit a `thejudge-*` skill, `CLAUDE.md`, or `.claude/settings*.json`. Never force-push, never `git reset --hard`, never drop or pop a stash. A denied or unlisted command is never retried — report it verbatim and stop. Browser risk is nil; start no dev server.

Report back, in this order: per finding, what you changed (file and line) and the test that now covers it; the exit status and one-line summary of each gate you ran; the commit hash and the PR head after your push; every path you wrote, launch-root-relative (each must begin `.worktrees/implement-ai-answer-quality-baseline/`); `git status --porcelain` in the worktree; any denied command verbatim; the tool-call count. Copy the `Working directory:` line above, unchanged, into every prompt you write.

## Instruction ledger

| Instruction | Class | Node | Rule |
| --- | --- | --- | --- |
| "Build an answer-quality baseline for Ask AI: a repeatable, human-reviewable way to measure whether the model's final rules answers are actually correct, not just whether the right rule excerpts reached the prompt. The hybrid rule retrieval just shipped (PR #197/#199) and every check we have stops at retrieval. Seed the gold set from the six worked-solution cases that carry published correct answers, decide the judge mechanism, scoring axes, cost/cadence, and fixture subset at the define gate, and make the instrument able to answer the open question of whether attaching more than five rule excerpts helps or hurts the answer. Retire the parked idea folder PRD/ideasForLater/ai-answer-quality-baseline/ once its content is staged as intake." | answered-once | preflight | — (the owner's launch request as drafted by the driver from the owner's words on 2026-09-06 — "lets cleanup, and thne start scoping out validation of th enhancement?" — and passed to node 2 verbatim as intake; every product question it raises, including the judge mechanism, scoring axes, cost and cadence, fixture subset, and the excerpt cap, is decided at the `define` gate, not pre-resolved) |
| "i added plenty of credits to the account, we are cleared for testing" | answered-once | build (parked at owner-action; owner in session 2026-09-07) | — (the owner's clearance for the one paid live run E9 required; the driver ran it twice — mock embedder by omission, then `EMBEDDING_PROVIDER=local` — and recorded both; no other decision was taken from it, and E10 stays the owner's) |
| "ive reviewed the files and im happy for now, can you manage the rest for me?" | answered-once | build gate resolution (owner in session 2026-09-07, before build attempt 2) | — (the owner's own E10 read-through, recorded as the dated conclusion line in slice E at their instruction, and a request to run the remaining mechanics — the gate resolution, `build` confirmation, `review`, `close` — unattended; no product decision was taken from it: the model stays gpt-4.1 by the owner's earlier decision and the excerpt-cap change stays a follow-up package) |
