# Slice A — manual evidence log

2026-08-24 A7 — grepped `PRD/sections/life-tracker/README.md` for "67px"; the
only occurrence is the "Edge `−`/`+` tap zones for life adjustment, ≈67px
thick" line under **Rejected alternatives and deferred scope**. It does not
appear under **Measured bounds** and is not narrated in **How it works**.

2026-08-24 A8 — read the **Where it lives** section: it names the coarse
frontend/backend directory locations (`apps/frontend/src/components/portal/life-tracker/`,
`apps/frontend/src/lib/lifeTracker/`, the additive `GameContext` backend
files) and explicitly defers to `PRD/sections/system-map.md`'s `## Player
Life Tracker` entry for the full file list and subsystem detail, rather than
duplicating deep code detail.
