# Slice F — manual observation log

Dated observation lines for criteria the hook cannot prove with a command.
The date is required: without it the line is a claim that could have been copied
forward, rather than an observation someone made on a day.

2026-08-20 F8 — ran the live round trip against a throwaway two-criterion slice.
Flipping both with no evidence was denied, naming `Z1` and `Z2` and what each
still needed. Running the evidence command earned `Z1`; the flip was still denied
for `Z2` alone. Writing the dated observation line earned `Z2`; the flip was then
allowed. Re-running an already-earned command left the log at two lines.

2026-08-20 G5 — dispatched a read-only subagent against slice A's commit and doc
with the slice's criteria as the rubric. It reported holding no Write/Edit/
NotebookEdit, graded all seven criteria, and produced four findings. The
repository was byte-unchanged before and after. Three of the four findings were
confirmed as real defects and fixed.
