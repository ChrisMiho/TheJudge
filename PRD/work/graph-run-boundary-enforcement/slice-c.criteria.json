{
  "slug": "graph-run-boundary-enforcement",
  "slice": "C",
  "criteria": [
    {
      "id": "C1",
      "statement": "Unit test: the hook denies a subagent-dispatch tool call while `.worktrees/.graph-stop` exists and allows it when absent.",
      "value": true,
      "evidence": {
        "command": "node --test scripts/graph-boundary-hook\\.test\\.mjs"
      }
    },
    {
      "id": "C2",
      "statement": "Unit test in `scripts/graph-preflight.test.mjs`: the sentinel classifies the run as refusing to start, with the sentinel path in the message.",
      "value": true,
      "evidence": {
        "command": "node --test scripts/graph-preflight\\.test\\.mjs"
      }
    },
    {
      "id": "C3",
      "statement": "`graph-run/SKILL.md` states the pre-dispatch sentinel check, the clean halt sequence in order, and the resume command; the halt states are read from the existing `## Terminal states` table with no sec",
      "value": true,
      "evidence": {
        "paths": [
          ".claude/skills/graph-run/SKILL.md"
        ]
      }
    },
    {
      "id": "C4",
      "statement": "Live halt rehearsal. With a live lock and a stub ledger, create the sentinel and confirm the halt path produces: a terminal state in the ledger, an `## Open gate` entry naming the node, the `STATUS.*`",
      "value": true,
      "evidence": {
        "manual": true
      }
    },
    {
      "id": "C5",
      "statement": "`grep -c \"graph-stop\" PRD/instructions/graph-workflow-contract.md` is non-zero \u2014 the sentinel is documented where the boundaries are.",
      "value": true,
      "evidence": {
        "command": "grep -c .graph-stop. PRD/instructions/graph-workflow-contract\\.md"
      }
    },
    {
      "id": "C6",
      "statement": "`npm run skills:ai-sync` run and `.agents/skills/` mirror clean in `git status --porcelain`.",
      "value": true,
      "evidence": {
        "command": "npm run skills:ai-sync"
      }
    }
  ]
}
