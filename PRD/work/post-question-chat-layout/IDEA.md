The post-question chat screen currently spends too much vertical space on repeated header text before the answer, which makes the actual conversation feel pushed down and visually heavy. The desired outcome is a slimmer answered-state layout that keeps the brand presence to just "TheJudge", removes redundant assistant subtitle copy, and places the frozen game context above the follow-up chat box so the context reads as setup rather than an afterthought. This work should improve scanability and reduce wasted space in the post-question state while preserving the existing answer content, follow-up question behavior, and frozen context semantics. Non-goals include changing the AI response format, changing prompt construction, or redesigning the full zone collection flow.
