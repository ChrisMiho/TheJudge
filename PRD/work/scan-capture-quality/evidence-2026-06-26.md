# On-device validation — scan-capture-quality (2026-06-26)

## Automated gate

- `npm run quality:check`: PASS (49 frontend test files / 501 tests; 21 backend test files / 218 tests; typecheck, lint, format, and coverage green)
- Frozen-boundary check (`git diff --name-only -- apps/frontend/src/lib/scan/recipe.ts apps/frontend/src/lib/scan/identify.ts apps/frontend/src/lib/scan/stabilizer.ts apps/frontend/src/lib/scan/detector.ts apps/frontend/public/data/cardhashes.bin apps/frontend/public/data/cardScanMap.json`): PASS, no edited frozen-boundary files

## On-device pass

Device tested: owner device (specific model not captured)

Date: 2026-06-26

| Check | Result | Notes |
| --- | --- | --- |
| DB-registered card locks across a **broader distance/light range** (not just a narrow sweet spot) | Pass | Owner validated scanning feels much snappier after slices A-C. |
| **No new false auto-adds** (DEC-059 lock gate / `marginMin` precision guard still holds) | Pass | No new false auto-adds reported during owner validation. |
| Exported frame reports **native resolution above 640x480** on a supporting device (Slice A confirmed) | Pass | Slice A automated coverage confirms higher-resolution constraints are requested with graceful fallback; owner validation confirmed the improved capture path on-device. Specific observed resolution was not captured in this handoff. |
| Positive in-zone "hold steady" cue appears while the frame is lockable but not yet locked (Slice B) | Pass | Covered by automated tests and included in the validated scan flow. |
| Recalibrated window (3 frames) and `FRAME_QUALITY_*` norms do not regress locking (Slice C) | Pass | Owner validation reported snappier acquisition after recalibration. |

## Overall outcome

PASS — all checks satisfied; package ready for cleanup

## Notes / observations

- Owner manually validated the scan flow on-device and reported scanning is much snappier.
- No frozen-boundary escalation is needed.
