2026-08-30 C5 — reviewed the slice C diff: `apps/backend/src/prompt/promptAssembly.ts` changed only the text of the existing single `- Treat ...` guardrail instruction line inside `buildLookupPromptText`'s `INSTRUCTIONS` array (no new function, branch, conditional, classifier, validator, or log/diagnostic signal added anywhere in the request or prompt-assembly path); `apps/backend/src/eval/contextEvaluationHarness.ts` gained one more required substring on the existing `checkLookupGuardrails` structural check (not a new detection mechanism, and not production code); the new glossary doc and eval fixture are docs/test-data only. `git diff --stat` for the slice confirms no new source file besides the doc and fixtures, and no change to `apps/backend/src/routes/`, `apps/backend/src/validation/`, or any logging call site.
