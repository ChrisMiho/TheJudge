# Intake manifest — graph-20260830-154444

- Run ID: `graph-20260830-154444`
- Slug: `prompt-context-refinement`
- Branch: `thejudge-auto/prompt-context-refinement-v2` (the derived
  `thejudge-auto/prompt-context-refinement` was taken by a prior BLOCKED run's
  stale remote branch, which the boundary hook forbids deleting; the package
  slug is unchanged)
- Origin: owner handoff via `/graph-run`, 2026-08-30. Relaunch after fixing a
  boundary-hook defect (merged PR #150) that bricked the first attempt.

## Primary intake (staged verbatim)

- `promptRefinement-notes.md` — owner's updated observations and the ideas to
  tackle first. Authoritative handoff; defines the gameplan's starting scope.

## Owner's framing (verbatim from the launch request)

> my updated observations and some ideas id like to start tackling first, there
> are already a lot of prompt refinement docs generated, they may be useful, but
> they do not define our gameplan, we are leveraging them for context on the
> application, but i want a fresh gameplan for approaching these issues, if
> something falls into the rag category, that can be put into its own markdown
> file and noted to be worked on later

## Context documents (cited, NOT authority — do not treat as settled truth)

Prior prompt-refinement docs the owner names as background only. Cited by path,
not opened as authority; any product truth they imply is decided at the `define`
gate. In the launch checkout these are untracked and are captured by node 1's
auto-commit onto the branch.

- `PRD/work/promptRefinement.md`
- `PRD/work/promptRefinement-analysis.md`
- `PRD/work/promptRefinement-enhancements.md`

## Standing scope instruction from the owner

- Items that fall into the **RAG** category are split into their own markdown
  file and noted to be worked on later, rather than folded into this gameplan.
  Observation #1 (mechanic-keyword enrichment) is the owner's own flagged
  candidate. This is a filing instruction, not a pre-authorization of any
  product decision.
