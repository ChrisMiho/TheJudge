2026-08-30 D3 — checked the matrix in `prompt-layout-spec.md` against `promptAssembly.ts`'s actual code (read `buildPromptText` and `buildLookupPromptText` directly, line by line) and against real `npm run prompt:preview` output for one fixture per path, run against an isolated backend on port 3921: `combat-deathtouch` (game mode) confirmed `SYSTEM ROLE PREAMBLE` -> `INSTRUCTIONS` (3 bullets) -> `MTG REFERENCE` -> `GENERAL GAME CONTEXT` -> `PHASE GUIDANCE` -> `ZONE: BATTLEFIELD` -> `GAME RULES (reference)` -> `ADDITIONAL RELEVANT RULE EXCERPTS` -> `SCOPE` -> `QUESTION`; `follow-up-chat` (game-mode follow-up) confirmed the same order plus the 4th INSTRUCTIONS bullet and a `CONVERSATION HISTORY` section between `SCOPE` and `QUESTION`; `quick-lookup-multi-card` (REQ-167, cards attached) confirmed `SYSTEM ROLE PREAMBLE` -> `INSTRUCTIONS` (5 bullets) -> `MTG REFERENCE` -> `GAME RULES (reference)` -> `ADDITIONAL RELEVANT RULE EXCERPTS` -> `CARD (looked up)` (both attached cards under one heading) -> `QUESTION`; `quick-lookup-no-card` confirmed the same order with `CARD (looked up)` absent. `COMMANDER SPELLBOOK COMBO CONTEXT` and `OFFICIAL RULINGS` position were confirmed against the source directly plus the committed eval golden `commander-spellbook-lookup-multi-card-complete.prompt.golden.txt`, since no committed prompt:preview fixture pairs a live ruling match with combo intent. Also ran `npm run prompt:preview:all`: every quick-lookup-*/commander-spellbook-lookup-* fixture (including all four new/touched slice A-C fixtures) returned `ok`; 12 unrelated pre-existing fixtures (e.g. `full-context`, `state-based-actions`, `zero-cards`) failed on a stale `turnPhase: "stack_resolving"` value or a schema-forbidden zero-card payload that predates this work package and is untouched by any of slices A/B/C -- not a regression, out of scope for REQ-169.
