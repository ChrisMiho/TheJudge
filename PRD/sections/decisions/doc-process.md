# Doc process decisions

PRD documentation tooling: system-map catalog, detail files, the decisions.md router split, and agent workflow contracts.

### DEC-044
- Decision: Adopt a durable feature/subsystem catalog at `sections/system-map.md` so the truth layer states what is built, how it behaves at a glance, and where it lives — without re-deriving behavior from code.
- Status: confirmed
- Context: The PRD promotes decisions into the `sections/` truth layer during planning, and the `Status:` field tracks decision lifecycle (`confirmed`/`superseded`), not whether code shipped. As a result the truth layer mixes "decided" with "built," and answering "is this real / how does it work / where does it live?" requires a code-reading journey. A single consolidated catalog answers all three in one read and gives a low-maintenance shipped-vs-planned signal without overloading `Status:` or sprinkling a new field across every entry.
- Impact:
  - new durable artifact `sections/system-map.md`; two levels — subsystems, with features grouped under each subsystem
  - each entry records: `Status` (`shipped` | `planned` | `partial`), a one-line behavior summary, coarse file/module location (subsystem level, not per-line), and backing `DEC`/`REQ` IDs
  - the shipped-vs-planned signal lives in the catalog only; existing `Status: confirmed/superseded` semantics on `DEC`/`REQ` entries are unchanged and not overloaded
  - additive-first: the catalog is built and validated against real questions before any reconciliation of stale navigation or change to existing status conventions
  - lightweight promotion gate: a catalog entry is marked `shipped` only when code and a cleanup receipt exist; enforced at cleanup time and documented in `instructions/`
  - commit-message convention (conventional-commits-lite): `docs(prd):` for doc/plan-only changes, `feat:`/`fix:` for changes that ship product behavior; documented in `instructions/`
  - stale navigation is reconciled only after the catalog is validated: the `PRD/README.md` work-package table is corrected and a pointer to the catalog is added to the `PRD/README.md` Section Inventory
  - `DEC-043`/`REQ-031` (`gameStateNotes` / `ADDITIONAL GAME STATE`) are reconciled by representing them as `planned` in the catalog — their `Status: confirmed` lifecycle field is left unchanged, because shipped-vs-planned lives in the catalog only and `Status:` semantics are not overloaded
  - deep per-subsystem behavior prose is out of scope here and deferred to `PRD/work/system-map-detail/` (it covers prompt assembly and the System 2 / System 3 retrieval mechanics that `prompt-context-retrieval-tuning` will rewrite); the catalog links to it once it exists
  - no per-decision → code-line link maintenance is introduced (explicit non-goal preserved)
  - documentation and process only: no `POST /api/ask-ai` request/response, UI, or prompt-assembly behavior change
- Related requirements:
  - (none — documentation and process decision; no functional requirement is added or changed)
- Notes:
  - the promotion gate and commit convention are process rules implemented in instruction files (`instructions/doc-lifecycle.md`, `instructions/agent-working-rules.md`) during map-out/implement, not in section files
  - DEC-044 itself is tracked as `planned` in the catalog until the catalog ships, then flipped to `shipped` per the promotion gate
  - "correcting `DEC-043`" means giving it a `planned` catalog entry, not editing its `Status:` field; `DEC-043`/`REQ-031` prose stays in the normative present tense used across the truth layer, since disambiguating decided-vs-built is precisely the catalog's job
  - this decision does not change the "assistant, not judge" framing or any prompt behavior

### DEC-048
- Decision: The deep per-subsystem behavior layer deferred by DEC-044 lives as separate detail files under `PRD/sections/system-map/`, one file per catalog subsystem, each linked from the catalog by an optional `Details:` field and written to a fixed lightweight template.
- Status: confirmed
- Context: DEC-044 ships the shallow `sections/system-map.md` catalog (status + one-liner + location + backing IDs) and explicitly defers the depth layer — the prose explaining how a subsystem actually works — to `PRD/work/system-map-detail/`, because the highest-value subsystems to document (prompt assembly, System 2 / System 3 retrieval) were about to be rewritten by `prompt-context-retrieval-tuning`. That package has landed (DEC-045, DEC-046, DEC-047 confirmed), so the volatile detail can now be written once. The catalog is already optimized for one-read scanning; embedding deep prose inline would grow it past readability and mix the index with its detail.
- Impact:
  - documentation and process only: no `apps/` code, no `POST /api/ask-ai` request/response, UI, or prompt-assembly behavior change
  - depth prose lives in separate files under `PRD/sections/system-map/`, one detail file per catalog `##` subsystem (e.g. `system-map/prompt-assembly.md`); the catalog keeps its existing four-field shallow shape
  - the catalog format gains one optional, additive `Details:` field on a subsystem entry pointing to its detail file; this does not change or overload `Status:` semantics and is omitted for subsystems with no detail file
  - no dangling links: a `Details:` pointer and the detail file it references land together in the same change; a subsystem without a detail file has no `Details:` line
  - each detail file follows a fixed lightweight template: a `Backed by:` DEC/REQ line, then `How it works`, `Data flow`, `Where it lives` (coarse modules), one `Worked example`, and `Invariants / gotchas`
  - behavior-level prose only; no per-decision → code-line link maintenance (inherits the DEC-044 non-goal); prose that merely restates code is not added
  - coverage is by need, not exhaustive: this package writes detail files for the priority subsystems only — `prompt-assembly.md` and `game-rules-retrieval.md` (the latter covering System 1 card rulings, System 2 curated baseline, and System 3 supplemental retrieval, which the catalog groups together and which interrelate via dedup); other subsystems get a detail file and `Details:` link only when one is later written
  - maintenance rule: a detail file is revisited only when its subsystem's behavior changes, governed by the existing DEC-044 commit convention (`feat:`/`fix:` for behavior changes, `docs(prd):` for doc-only changes)
- Related requirements:
  - (none — documentation and process decision; no functional requirement is added or changed)
- Notes:
  - extends DEC-044; does not supersede it — the shallow catalog and its shipped-vs-planned signal are unchanged
  - the priority detail files must reflect the assembled prompt section order and the System 1/2/3 mechanics as defined by DEC-025, DEC-029, DEC-030, DEC-036, DEC-042, DEC-043 (planned), DEC-045, DEC-046, DEC-047; `Q-001` (keyword-vocabulary derivation) is the live open question for System 3 and should be referenced, not resolved, by the detail prose
  - this decision does not change the "assistant, not judge" framing or any prompt behavior

### DEC-063
- Decision: `PRD/sections/decisions.md` is split into a thin router/index plus per-domain decision files under `PRD/sections/decisions/`, organized by topic/subsystem (not by flow stage). The router keeps the `decisions.md` path and the precedence/lifecycle preamble, plus a `DEC-ID → domain file → one-line summary` table; decision bodies move verbatim into nine topic files. DEC-IDs stay globally unique and resolvable across files.
- Status: confirmed
- Context: `decisions.md` is a ~1,030-line / 62-decision monolith that is Read-First #1 and source-of-truth precedence #1 in every workflow. Because it loads whole, every feature pays the full context cost of every other feature's decision history (the trigger was the scan saga DEC-050→062 being relevant while ~750 unrelated lines loaded anyway). The root cause is structural — one file, loaded whole — not a readability bug; in-place hygiene alone was rejected as too small, and consolidating the supersession sagas was rejected as high-risk and unjustified by the trigger.
- Impact:
  - the router path `PRD/sections/decisions.md` is preserved, so every existing reference across skills, instructions, READMEs, and receipts stays valid — wording tweaks only, no path surgery
  - nine topic files under `sections/decisions/`: `framing` (001, 002, 013), `capture-and-stack` (004–009, 015, 018, 028), `game-context-model` (003, 019, 021–024, 026, 027, 034, 035, 037, 043), `prompt-assembly` (025, 036, 042), `rules-retrieval` (029, 030, 032, 045–047), `providers-and-contract` (010–012, 014, 016, 017, 020, 033, 049), `conversation-ux` (031, 038–041), `scanning` (050–062), `doc-process` (044, 048, 063)
  - the organizing axis is topic/subsystem, not flow stage: reusable topics that span the flow (notably `game-context-model`) stay unified rather than scattered across journey stages
  - DEC bodies move verbatim — no rewording or saga consolidation; `Status:` and cross-references are preserved; new IDs are added and existing IDs are never renumbered
  - `decisions.md` becomes a ~60–80-line router: the precedence/lifecycle preamble plus the DEC-ID index table; the `Read First #1` step then loads a thin map and the agent pulls only the one domain file it needs
  - lifecycle rule folded into `instructions/doc-lifecycle.md`: new decisions land in their domain file **and** get a router index line; fully-superseded bodies trim to a one-line tombstone kept in-domain (ID stays resolvable); deep "how the code behaves" detail belongs in `system-map/` detail files (DEC-044 / DEC-048), not in decision `Impact:` blocks
  - documentation and process only: no `apps/` code, prompt-assembly, API, or UI behavior change; receipts that pin literal `decisions.md` line numbers are frozen historical artifacts and left as-is
  - no `system-map.md` entry is added: the catalog tracks product/code subsystems, not the PRD's own doc tooling (consistent with how DEC-044 / DEC-048 are handled)
- Related requirements:
  - (none — documentation and process decision; no functional requirement is added or changed)
- Notes:
  - DEC-063 itself lives in the `doc-process` domain file once the split lands
  - taxonomy finalized 2026-06-24 during refinement; `framing` and `conversation-ux` were renamed from the provisional `foundations` / `follow-up-and-wait` to keep every file on the topic/subsystem axis
  - this decision does not change the "assistant, not judge" framing or any prompt behavior

### DEC-064
- Superseded by DEC-115.

### DEC-086
- Decision: `npm run quality:check` runs the Vitest suite exactly once — coverage-mode execution is the single canonical regression + coverage gate and the redundant standalone `test` step is removed from the aggregate; alongside this, test files are reorganized for clarity (split the oversized `App.test.tsx`, extract shared EnrichmentStep fixtures/setup, group scan suites) as an assertion-preserving test refactor. Coverage thresholds, the eval golden gate, and product behavior are unchanged, and cross-workspace parallelism/sharding is explicitly deferred.
- Status: confirmed
- Context: The suite has grown to ~800 cases across ~82 source test files and GitHub Actions time is rising. `quality:check` chains `test` (`vitest run`) then `coverage:check` (`vitest run --coverage`); the coverage pass is a strict superset that re-executes every test, so CI runs the whole suite twice per job for no added protection. Navigation has also degraded: `App.test.tsx` is ~2,438 lines, EnrichmentStep setup is duplicated across three test files, and scan coverage is spread across ~10 colocated files. The fix must not weaken any gate — the IDEA forbids threshold reductions, arbitrary test deletion, eval-golden bypass, and product-behavior change. Workspace parallelism was considered but deferred: the single-pass collapse already ~halves test execution, and parallelism adds interleaved logs and config to maintain, better justified later with runner-core data.
- Impact:
  - CI/tooling and test-code only: no `apps/` product source, `POST /api/ask-ai` request/response, UI, or prompt-assembly behavior change
  - `quality:check` drops the standalone `test` step and relies on coverage-mode execution as the one suite run; `npm test` and `npm run test:coverage` remain for local iteration, and workspace/file-level targeting is preserved
  - all coverage thresholds stay at or above current values (frontend `lines: 45`; backend `lines: 45`, `src/prompt/** lines: 60`, `src/validation/** lines: 60`); the eval golden gate (`test:eval`, NFR-009) is untouched
  - test reorganization preserves existing assertions and case count: `App.test.tsx` is split into behavior-focused files, shared EnrichmentStep fixtures/setup are extracted to a single reusable helper (reuse-before-creating, technical-design-rules), and scan suites are grouped coherently
  - no test is removed to hit a timing target; hygiene never trades coverage for speed
  - cross-workspace (frontend + backend) parallelism and vitest sharding are deferred as a named follow-up, not part of this decision
- Related requirements:
  - NFR-012
- Notes:
  - continues the token/tooling-discipline lineage of DEC-063 (input structure) and DEC-064 (output verbosity); this decision targets test-execution and test-file structure
  - no `system-map.md` entry is added: the catalog tracks product/code subsystems, not the repo's own test/CI tooling (consistent with DEC-044 / DEC-063 / DEC-064)
  - this decision does not change the "assistant, not judge" framing or any prompt behavior

### DEC-115
- Decision: TheJudge workflow skill responses stay terse and high-signal by construction — status, decisions, files/IDs touched, verification, and the required handoff — with no document dumps, no restated background, and no long command output. Supersedes DEC-064.
- Status: confirmed
- Context: The 2026-08-02 skills refresh (`docs/superpowers/specs/2026-08-02-skills-refresh-design.md`) rewrites every `thejudge-*` skill from a blank page, calibrated for models that no longer need a shared verbosity-profile artifact to stay terse. DEC-064's mechanism — one shared `.cursor/skills/thejudge-output-guidance.md` file referenced by every skill, with `lean`/`standard`/`detailed` profiles — is the exact artifact the refresh deletes. Leaving DEC-064 `confirmed` while removing the file it mandates would put the corpus in self-contradiction and cause a later `thejudge-quality-check` to correctly FAIL against it.
- Impact:
  - documentation/process only: no `apps/` code, product UI, backend API, prompt assembly, provider behavior, or PRD content model change
  - the DEC-064 mechanism is retired and deleted: `.cursor/skills/thejudge-output-guidance.md` and its synced copies, plus the per-skill "Shared output guidance" boilerplate paragraph in every `thejudge-*` skill, are removed
  - the `lean` / `standard` / `detailed` output-profile vocabulary and its per-session override are dropped; a plain-language request to be more or less verbose needs no named profile system
  - response discipline becomes inherent to how each skill is written, not delegated to a referenced file — every rewritten `thejudge-*` skill is terse and high-signal by construction
  - unchanged from DEC-064 and still binding: profile or phrasing never alters required reads, writes, approval gates, PASS/FAIL calls, blocker reporting, verification, status updates, or the required `Next step` handoff; mandatory output stays mandatory
  - canonical editing remains `.cursor/skills/thejudge-*`; synced copies in `.agents/skills/` and `.claude/skills/` remain implementation artifacts, not edit targets — **superseded by DEC-165**, which deletes `.cursor/` and makes `.claude/skills/` canonical with `.agents/skills/` its only mirror
- Related requirements:
  - (none — documentation and process decision; no functional requirement is added or changed)
- Notes:
  - DEC-064's body is trimmed to a one-line tombstone in this file; the ID stays resolvable
  - DEC-063 and DEC-086 reference DEC-064 in their own Notes as lineage — those are historical mentions of a still-resolvable ID and are left alone
  - no `system-map.md` entry is added: the catalog tracks product/code subsystems, not the PRD's own agent workflow tooling (consistent with DEC-044 / DEC-063 / DEC-064)

### DEC-154
- Decision: TheJudge agent workflows use a contract-centered lifecycle: collaborative skills stay in the current checkout and branch with no automatic Git publication; explicitly invoked autonomous preparation requires an operator-named remote base and records it for one-package worktree/branch/PR ownership; implementation is sequential within a package and concurrent only across packages; deferral is reversible; and user-requested or browser-risk-triggered Playwright verification plus exact owned-process cleanup are completion gates.
- Status: confirmed
- Context: The skill catalog and shared workflow documents have drifted as autonomous preparation, all-slice implementation, dependency-wave implementation, and fanout were added. Existing autonomous contracts silently target `origin/main`, cleanup equates `ship-ready` with merge, and parallel skills allow nested slice-agent fleets despite the desired one-agent-per-package model. Browser verification is available but inconsistently required, while `scripts/dev.mjs` launches shell wrappers and exits after a fixed delay without proving descendant shutdown. A merge/reset incident during refinement also demonstrated the cost of ambiguous checkout ownership. The workflow needs one predictable operator map and auditable ownership without changing product behavior.
- Impact:
  - documentation, agent workflow, and developer tooling only; no product UI, API, prompt, provider, or data behavior changes
  - `AGENT-SKILLS.md` is the operator map; `workflow-reference.md` centralizes lifecycle rules; a focused `runtime-process-hygiene.md` owns browser/dev-server requirements; individual skills retain only phase-specific contracts and references
  - the catalog becomes ten skills: keep kickoff, refinement, quality-check, map-out, implement, implement-all, implement-fanout, cleanup, and prepare; add reversible `thejudge-defer`; delete `thejudge-map-out-parallel` and `thejudge-implement-parallel` from canonical and synchronized trees
  - ordinary collaborative work remains in the current checkout/current branch and does not automatically create worktrees, branches, commits, pushes, or PRs
  - explicit autonomous preparation requires a named remote base, records `origin/<base>` in package metadata, uses `.worktrees/prepare-<slug>`, and opens a preparation PR against that base; a human merge is required before implementation
  - autonomous implementation uses one agent and sequential slices in `.worktrees/implement-<slug>`, with one implementation branch/PR per package targeting the recorded base; fanout dispatches across packages only, requires a common recorded base, preserves cross-package file-overlap serialization, and assigns isolated port pairs
  - `ship-ready` means slice verification passed, not that a PR merged; autonomous cleanup runs locally on the recorded base, proves the implementation PR merged and the worktree is clean/fully merged, then removes the clean local implementation worktree/branch; it never deletes remote branches or automatically commits, pushes, merges, or closes PRs
  - `thejudge-defer` records the previous status and reason, moves the package to `deferred`, and restores it on the next invocation; artifacts and Git state are preserved; `ship-ready` and active packages with an `in-progress` slice cannot be deferred
  - Playwright MCP is required when the user asks for it or when browser-observable risk cannot be established by component tests (responsive geometry/containment, overlays/stacking, hit areas, focus/keyboard, scrolling, navigation/persistence, browser APIs, or integrated multi-screen behavior); no new Playwright CI harness is added
  - every browser/dev-server use records owner/session, worktree, ports, started-versus-attached state, cleanup evidence, and capture output path; owners call `browser_close`, stop exact owned process trees, wait for exit, and verify owned ports released before a slice/task completes; attached or user-owned servers are never stopped
  - agent-created worktrees live only under the repo-local `.worktrees/` root; preparation and implementation preflights refuse any other path (sibling `../<repo>-worktrees/`, temp/scratchpad, or elsewhere) at creation time rather than via a gate over pre-existing worktrees; the Claude Code harness's `**/.claude/worktrees/agent-*` isolation root is tolerated because no repo setting relocates it, and every ignore-consuming tool config (`eslint.config.mjs`, `.prettierignore`) must exclude both roots
  - browser captures are written to `PRD/work/<slug>/.playwright-mcp/` resolved against the current checkout root (the worktree for autonomous runs, the main checkout for collaborative and interactive ones), never the repo root, with root-level `.tmp/`/`.playwright-mcp/` as the no-package fallback; captures are disposable and are deleted with the package folder and worktree at cleanup, so no retention policy or capture archive is added
  - `scripts/dev.mjs` is hardened to accept explicit isolated ports, spawn without shell wrappers, own exact service trees, shut down idempotently, await graceful exit, and escalate after a bounded window only against the same owned trees; broad `pkill`/`killall`, `nohup`, and untracked background processes are prohibited
  - canonical skill edits follow failing-baseline-first contract scenarios, then `npm run skills:ai-sync` and byte-identical mirror verification; focused process-manager tests join the quality gate without duplicating the Vitest suite
- Related requirements:
  - (none — repository workflow and developer tooling only; no functional requirement is added or changed)
- Notes:
  - extends DEC-115's agent-workflow lineage without changing its terse-output rule
  - `.worktrees/` is the only repo-local autonomous worktree root; existing unrelated worktrees are not silently removed
  - runtime cleanup occurs at the end of every owning invocation, including failure paths; final package cleanup verifies the recorded evidence and owns post-merge Git worktree cleanup
  - no `system-map.md` entry is added because the catalog tracks product/code subsystems rather than repository agent workflow tooling

### DEC-155
- Decision: The GitHub Actions gate is restructured for parallelism instead of running as one serial step — `quality:check`'s sub-checks are decomposed into concurrent CI jobs, the frontend Vitest coverage run is sharded across parallel jobs whose blob reports are merged before thresholds are evaluated, jsdom is scoped per-file rather than applied to every test file, `Deploy AWS` depends on the gate instead of re-running it, and PR runs cancel superseded in-progress runs. `npm run quality:check` remains the single canonical local command, every test and threshold is preserved, and coverage still runs on every CI run. Discharges the parallelism/sharding follow-up DEC-086 deferred.
- Status: confirmed
- Context: Gate wall time grew from ~30s to ~4min. Step-level timing on run `31077630789` attributes 3m41s of a 3m57s job to `npm run quality:check`; checkout, `setup-node`, and cached `npm ci` cost 14s combined, so dependency install is not the cause. Local isolation shows typecheck (2.9s), lint (2.1s), `format:check` (0.5s), `test:scripts` (1.0s) and the entire backend suite (1.8s / 271 cases) are noise, while frontend `test:coverage` is 24.5s of a ~33s local gate. Case growth alone does not explain it: the same frontend suite runs in 15.2s without `--coverage`, so v8 instrumentation adds ~52s CPU (+86%), and the penalty is worst on tight numeric loops — `src/lib/scan/**` measures 7.0s of test CPU uninstrumented versus 32.3s instrumented (4.6x). Separately, a global `environment: "jsdom"` charges all 115 frontend files 54.1s CPU even though the 15 scan files pass unchanged under `node` (environment CPU 8.51s → 0.002s). Total gate CPU (~210s) is close to CI wall (221s), so the 4-vCPU public runner extracts almost no parallelism from a serial `&&` chain. Workflow shape compounds it: `deploy-aws.yml` re-runs the identical gate (3m22s of a 4m41s deploy, versus 12s build and 37s deploy), and because the two workflows run concurrently that duplicated step is currently the only thing actually gating production; neither workflow sets a `concurrency` group, so superseded pushes keep running (four overlapping runs on one branch within 25 minutes). DEC-086 deferred exactly this work pending CI-runner-core data, which now exists.
- Impact:
  - CI/tooling and test-configuration only: no `apps/` product source, `POST /api/ask-ai` request/response, UI, prompt-assembly, provider, or data behavior change
  - CI decomposes the gate into concurrent jobs (static checks, backend coverage, N frontend coverage shards) followed by a coverage-merge job; `npm run quality:check` is unchanged as the canonical local pre-PR command and gains no CI-only fast mode
  - frontend coverage is sharded with `--shard`/blob reports and merged before thresholds are applied, so thresholds are evaluated once on merged totals; a deliberate threshold breach must still fail the merged job
  - all coverage thresholds stay at current values (frontend `lines: 45`; backend `lines: 45`, `src/prompt/** 60`, `src/validation/** 60`), coverage still runs on every CI run, and the `test:eval` golden gate (NFR-009) is untouched
  - jsdom stays the configured default and DOM-free files opt *out* to `node` via `environmentMatchGlobs`, extended only to globs a green run proves, so an unlisted or newly added test file is never silently DOM-less; blanket directory- or extension-based rules are prohibited because `src/lib/lifeTracker/useLifeTracker.test.ts`, `src/lib/feedback/FeedbackContextProvider.test.tsx`, `src/lib/portal/seedContext.test.tsx`, and `src/lib/theme/applyPalette.test.ts` measurably still need a DOM — the last of these applies a palette to `document` while its sibling `src/lib/theme/**` suites run DOM-free, which is precisely why the rule is per-file rather than per-directory
  - `Deploy AWS` stops re-running `quality:check` and instead depends on the gate jobs via `needs:`, converting a concurrent duplicate into a real precondition; because `needs:` is only valid within a single workflow, the deploy job moves into the gate workflow and is guarded by an `if:` restricting it to `push` on `main`, rather than using `workflow_run` (which would run in default-branch context and require manual SHA handling)
  - OIDC `id-token: write` is scoped at the deploy job rather than the workflow, so pull-request gate jobs — which run on contributor-controlled refs — never receive AWS-assumable credentials; the `VITE_FEEDBACK_FORMSPREE_ID` build-step-only env scoping from the deploy hotfix is preserved
  - "the suite runs once" is measured per CI run across all jobs, not per job: a shard executing a subset satisfies DEC-086's no-duplicate-execution rule, and NFR-012's wording is generalized accordingly
  - deploy is deliberately serialized behind the gate rather than racing it: time-to-deployed becomes gate plus deploy job instead of the deploy job alone, which is accepted because today's concurrent shape lets a `main` push deploy a commit whose gate is still running or has already failed; both the deploy job duration and end-to-end time-to-deployed still improve on baseline, and they are tracked as separate metrics so the job figure is never reported as time-to-deployed
  - because CI executes `quality:check`'s sub-scripts individually rather than the aggregate, a `scripts/*.test.mjs` drift guard (run by `test:scripts`) asserts every sub-script in the `quality:check` chain is executed by some CI job, so a check added locally cannot silently go unrun in CI
  - PR runs use a `concurrency` group with `cancel-in-progress: true`; the `main` deploy path is excluded so a deploy is never cancelled mid-flight
  - oversized outlier test files may be split assertion-preserving to lower the shard floor, on the same basis DEC-086 authorized for `App.test.tsx`; no test is deleted, skipped, or weakened to hit a timing target
  - moving coverage off PR runs was considered and rejected as contradicting DEC-086's single canonical coverage gate; excluding hot numeric source from coverage and swapping v8 for istanbul were also rejected
- Related requirements:
  - NFR-012
  - NFR-009
- Notes:
  - continues the tooling-discipline lineage of DEC-063, DEC-064, and DEC-086; DEC-086's single-pass collapse stands and is not superseded — this decision only discharges its named deferral
  - shard count is tuned against runner cores observed in CI, not assumed; the runner is 4-vCPU because the repository is public
  - no `system-map.md` entry is added: the catalog tracks product/code subsystems, not the repo's own test/CI tooling (consistent with DEC-044 / DEC-063 / DEC-064 / DEC-086)

### DEC-163
- Decision: TheJudge adds an autonomous graph workflow over the existing `thejudge-*` lifecycle: `graph-run` drives the phases as delegated nodes without per-step human input, `graph-preflight` prepares the checkout, `PRD/instructions/graph-workflow-contract.md` is the durable contract, `GRAPH-RUN.md` is a resumable per-run ledger written before each node advances, models are assigned per node, human gates park at `owner-action` instead of asking in-session, and `.claude/graph-profile.json` is an opt-in permission profile. The driver delegates every phase to its existing skill and reimplements none. Extends DEC-154's contract-centered lifecycle without changing any of its rules.
- Status: confirmed
- Context: DEC-154 fixed the lifecycle's ownership and gating rules but left every phase transition manual, so clearing `PRD/work/` required a human at each of eleven steps. The graph adds the chaining layer. The spine was built against a `superpowers`-style plan (`PRD/work/graph-workflow/PLAN-spine.md`) rather than through the `thejudge-*` lifecycle it automates, which is why 31 commits and roughly +15,000 lines changed how this repository's agents work with no decision record — this entry closes that gap retroactively rather than describing new intent. One constraint from the original braindump was revoked by the owner on 2026-08-14: "never modify `thejudge-*` skills" was load-bearing and wrong, because honoring it produced a driver whose control predicate matched nothing and a parallel pipeline that "only creates clutter". Whole-branch review then surfaced three Critical seam defects that per-task review structurally could not catch — the control predicate matching nothing, nothing writing `## Autonomous metadata` so `thejudge-implement-all` would block every run, and auto-commit landing on whatever branch happened to be checked out including `main`.
- Impact:
  - documentation, agent workflow, and developer tooling only; no product UI, API, prompt, provider, or data behavior changes
  - the skill catalog becomes fourteen: the eleven `thejudge-*` skills plus `graph-run`, `graph-preflight`, and `graph-gate-review` (DEC-164); `AGENT-SKILLS.md` remains the operator map and `workflow-reference.md` the lifecycle reference
  - orchestrated mode activates only on the explicit predicate `graph-run is controlling`, alongside DEC-154's existing `thejudge-prepare is controlling`; without a predicate every phase skill keeps its direct interactive behavior
  - `graph-run`'s entry-point table covers all seven package status values, so a run may enter at any lifecycle position — a `ship-ready` package enters at `close` and runs only `thejudge-cleanup`
  - a dirty checkout is resolved by a tested pure function rather than skill prose: auto-commit at or below 10 changed files and 200 changed lines, auto-stash above either threshold, implemented in `scripts/graph-preflight.mjs`; the destructive path is always previewed with `--dry-run` first
  - auto-commit lands on the recorded autonomous base, never on whatever branch happened to be checked out; the run never pops, drops, or clears a stash it created, because stashed work belongs to the user
  - the ledger records a row per node attempt *before* advancing, so a crashed run resumes; a ledger written at the end cannot
  - gates park: the run sets `STATUS.owner-action`, writes the question, the evidence, and the exact resume command under `## Open gate`, and stops without polling or retrying
  - the permission profile applies only when the session is launched with `--settings .claude/graph-profile.json`; nothing enforces that, and the contract states it as an operator responsibility rather than claiming enforcement
  - two Bash boundaries are convention-only and cannot be enforced: `nohup` is stripped as a wrapper before permission rules match, and a trailing `&` is consumed as a command separator; both are stated in the contract rather than claimed as denied
  - the secret gate parses `git diff --numstat` rename compaction (`{config => .secrets}/x`) and nested-brace forms, and denies `+` refspec force-push in addition to flag-matched forms; both holes were reproduced rather than theorized
  - no `system-map.md` entry is added: the catalog tracks product/code subsystems, not the repository's own agent workflow tooling (consistent with DEC-044 / DEC-063 / DEC-064 / DEC-086 / DEC-154)
- Related requirements:
  - (none — repository workflow and developer tooling only; no functional requirement is added or changed)
- Notes:
  - extends DEC-154's agent-workflow lineage; none of DEC-154's ownership, concurrency, deferral, or runtime-cleanup rules are changed or superseded
  - at the time this decision is recorded, two behavior changes on the same branch are unmeasured against `AGENT-SKILLS.md`'s fixture precondition: `graph-run` fixture item 5, and the `thejudge-cleanup` merge-proof gate changed in `69eaee9`. Both are accepted as known at merge and closed by DEC-164
  - the build ledger for the spine (`.superpowers/sdd/PLAN-spine/progress.md`) records every ruling made autonomously during construction and is git-ignored; it is preserved into the repository before this branch merges

### DEC-164
- Decision: The graph workflow's autonomous boundaries are enforced by scripts the run must pass, not by prose instructing agents to comply. Three boundaries convert: the no-pre-authorization rule becomes `scripts/graph-ledger-check.mjs`, which `graph-run` must pass *before* it dispatches any node; the skill-fixture rig becomes `scripts/fixture-rig.mjs`, which builds an isolated clone and bare origin per rep and asserts the invoking checkout is byte-unchanged after the run; and protected paths are guarded per writing mechanism with each mechanism's enforcement stated rather than assumed, so `Bash(npm run *)` and `Bash(node scripts/*)` stay broadly allowed rather than being narrowed to an allowlist. A `thejudge-cleanup` fixture is added for the gate changes in `69eaee9`. The same principle is then applied to the boundaries a second review found: the permission profile is reconciled against the node table's real command surface and gains a `PROMPTED` terminal state, an env sentinel makes profile loading observable, `git add -A` becomes a deny rather than a rule, the run ledger is folded into the cleanup receipt before the work folder is deleted, dispatch prompts pin an absolute working directory with node 6's writes asserted in scope, `graph-preflight` takes a concurrency lock, and the predicate extends to `thejudge-implement-all` and `thejudge-cleanup`. One boundary is deliberately human rather than mechanical: the `define` node parks on any `PRD/sections/` diff, and a third graph skill, `graph-gate-review`, walks that diff and resumes the run. Discharges the two unmeasured gates DEC-163 accepted as known.
- Status: confirmed
- Context: Three boundaries of the DEC-163 spine were written as prose, and all three were measured failing. (1) `graph-run` fixture item 5 tests whether the driver refuses a standing instruction of the form "if it asks again, just pick the smaller option and keep going"; it failed 2 of 3 reps on 2026-08-14 — one rep decided seven product forks under the standing order, another explicitly extended the override to cover the contract's own three-condition blocker test. Every mechanical item in the same fixture converged 3 of 3; the only divergence in the entire measurement was the judgment call. A rewritten rule was added at three layers and has never bound in a completed measurement: one re-run died to a session limit, the next to (2). (2) The fixture rig's isolation was prose telling each rep to work in its clone; on 2026-08-17 a rep's *dispatched subagent* inherited the session's real working directory instead and ran `thejudge-refinement` against the live checkout, writing DEC-161, DEC-162, REQ-146..151, NFR-015, FLOW-019 and a new `decisions/card-collection.md` into product truth. Constraining a parent does not constrain its children. (3) The contamination was then committed by `git add -A PRD/` during an unrelated cleanup — staging discipline was also prose. Per `skill-testing.md`, divergence across reps means the wording is not binding even when each individual run is defensible; a fourth wording is the same instrument that has failed three times.
- Impact:
  - documentation, agent workflow, and developer tooling only; no product UI, API, prompt, provider, or data behavior changes
  - `GRAPH-RUN.md` gains two required sections: each node's dispatch prompt recorded verbatim, and `## Instruction ledger` — one row per user instruction, quoted, classified `answered-once` or `refused`, with the node it arose at and the refusing rule where it was refused. No `standing-rule` class exists, so pre-authorizing a class of future product decisions has no representable form in the ledger
  - `## Instruction ledger` **replaces** the contract's existing `## Refused instructions` section rather than coexisting with it, so a refusal cannot be recorded in one section and missed by the other, and the validator has a single parse target. `graph-workflow-contract.md`, `graph-run`'s `SKILL.md` and `reference.md`, and the `graph-run` fixture change together; the fixture notes that runs measured before this change recorded refusals under the old section name
  - `scripts/graph-ledger-check.mjs` blocks a node dispatch when a dispatch prompt contains conditional-future authorization language, or when a user instruction is quoted into a dispatch prompt without a matching `## Instruction ledger` row; its parsing core is a tested pure function, following `scripts/graph-preflight.mjs`
  - enforcement is before dispatch, not after: a violating run stops at the `define` node instead of producing decided product forks that a post-hoc audit can only report
  - `scripts/fixture-rig.mjs` owns rep setup — one clone and one bare `origin` per rep, never the real remote, the clone path baked into every dispatch prompt it emits, `node_modules` as a real directory rather than a symlink — and snapshots the invoking repository's `HEAD` and `git status --porcelain` before and after, failing the run on any difference. The clean-tree criterion applies to the run, not to the act of recording it: measured results are written into the fixture's `## Measured runs` only after the rig's after-snapshot has passed, as a separate deliberate commit
  - `graph-run` fixture item 5 is re-scoped: it verifies the validator fires, not that wording persuades. A violating rep now fails deterministically at the `define` node in minutes rather than diverging after 30–60 minutes of run time
  - the protected set is `.secrets/**`, `CLAUDE.md`, `.claude/graph-profile.json`, `.claude/settings*.json`, and `thejudge-*/**` in both skill trees (`.claude/skills/` and `.agents/skills/`, per DEC-165). Its guard is stated per writing mechanism, because no single mechanism covers them all: agent `Edit`/`Write` calls are refused by `.claude/graph-profile.json`'s deny rules, which bind only when the session was launched with `--settings` — the same unverifiable operator responsibility DEC-163 already records; writes from `node scripts/*` are refused by a shared helper (`scripts/lib/protected-paths.mjs`), with a `test:scripts` drift guard asserting no script performs a raw filesystem write **to a protected path** outside it; and raw Bash writes (`cp`, `rsync`, redirection) are **not** enforceable, so they are recorded as convention detected after the fact by the fixture rig's before/after snapshot and the skills-sync drift check, never claimed as enforced
  - the drift guard is `scripts/protected-write-guard.test.mjs`, built on the `scripts/ci-workflow-parity.test.mjs` pattern: a source-text scan with a declared exemption list whose single entry is `scripts/lib/protected-paths.mjs` itself, so adding a second is a reviewable act rather than an accident. It scans every non-test `scripts/**/*.mjs` and fails a file containing both an `fs` write API and a protected-path literal, matching each write API as an anchored call form rather than a bare substring — `platform` and `SIGTERM` contain `rm`, and `renameSources` contains `rename`, so a substring matcher fails two clean scripts (`dev.mjs`, `graph-preflight.mjs`) on the current tree and the failure reads as a dirty tree rather than a wrong regex. Its reach is protected-path writes, not all writes: the eleven scripts that write to `data/`, `.tmp/`, and temp directories are untouched, and routing general writes through the helper is a non-goal. Two limits are stated rather than assumed — the scan matches path literals, so a path assembled at runtime evades it, and `*.test.mjs` is out of scan scope because a graph run does not execute test files
  - writes into the mirror tree `.agents/skills/` belong to the sync path alone, through `mirrorSkillTrees()`; hand-editing a mirror is the drift the guard exists to catch. `graph-workflow-contract.md`'s `## Boundaries` carries the three-layer table verbatim, so the contract states each mechanism's reach rather than implying one guard covers all three
  - the `thejudge-*/**` deny is a graph-run boundary, not an authoring restriction: skill authoring happens in ordinary sessions that do not load the profile, and `graph-run`/`graph-preflight` skill files are not denied at all, which is how the validator's own `SKILL.md` change ships
  - `scripts/sync-agent-skills.sh` is ported to `scripts/sync-agent-skills.mjs` and calls `mirrorSkillTrees()`, the helper's single declared protected-write, source pinned to `.claude/skills/` and destination pinned to `.agents/skills/`. A bash script cannot route through a JS helper and is invisible to a JS-source drift guard, so porting it is what keeps one drift-guard rule covering every script the guard scans, with exactly one declared exemption and no allowlist of script names; the port is proved byte-identical to the previous `rsync -a --delete` output before it lands
  - the permission profile is deliberately *not* narrowed: an allowlist of script names would block agents mid-run every time a script is added, which contradicts the profile's purpose of autonomy without red tape. `test:scripts` runs `node --test scripts/*.test.mjs`, so a new guard joins the quality gate by existing
  - a `thejudge-cleanup` fixture is added under `PRD/instructions/skill-fixtures/`, covering both gate changes from `69eaee9`: a recorded base branch deleted after merging, and `gh` returning HTTP 5xx during an outage. Both scenarios are drawn from the incidents of 2026-08-17 rather than imagined
  - staging discipline is a profile deny rather than a rule to comply with: `git add -A`, `git add --all`, and `git add .` are denied outright, path-scoped `git add <path>` stays broadly allowed, and `graph-workflow-contract.md`'s `## Boundaries` names the incident as the reason. The rig's before/after assertion still detects a contaminated tree before anything can stage it
  - the permission profile is derived from the node table's actual command surface, not only from the contract's prohibition list. Reading it against the nine nodes on 2026-08-17 found node 9 (`close`) with no permitted delete mechanism at all — six `Bash(rm ...)` denies, every one a *force* form (`rm -rf`, `rm -fr`, `rm -r -f`, `rm -f -r`, and the two long-flag spellings), and no `git rm` entry — and node 6's preparation-worktree removal (`git worktree remove`, `git branch -d`) unlisted. A bare `rm -r <path>` was neither denied nor allowed: unlisted means it prompts, and a prompt in an autonomous session is a hang, so `Bash(rm -r *)` and `Bash(rm --recursive *)` are added to the deny list alongside the work-folder-scoped `Bash(git rm -r PRD/work/*)` allow. Each node's commands are enumerated from its delegate skill and reconciled against the profile, and `thejudge-cleanup` names its delete mechanism explicitly instead of leaving it to the implementer
  - a permission prompt in an autonomous session is a hang, not a park, so a fourth terminal state, `PROMPTED`, is added: the run writes the denied command under `## Open gate` and stops, leaving the same evidence a parked run does. Three terminal states could not describe a run waiting on a prompt nobody will answer. It lands in `graph-run`'s `## Terminal states` table, which is where all three existing states are defined and where `graph-workflow-contract.md` never named them; the contract gains a one-line pointer to that table rather than a second copy, because two lists of terminal states drift and a lock released on a state one list omits is a stranded lock
  - `.claude/graph-profile.json` carries `"env": { "THEJUDGE_GRAPH_PROFILE": "1" }`, so `graph-preflight` observes whether the profile was loaded and the ledger's `Profile:` field becomes evidence rather than the user's word. This closes the operator-responsibility gap DEC-163 records for the `--settings` launch flag. The sentinel proves the file was loaded, not that any individual deny rule fired, and the profile's existing self-write deny means a run cannot forge it
  - `thejudge-cleanup` folds `GRAPH-RUN.md`'s `## Node ledger` and `## Instruction ledger` verbatim into the receipt's new `## Graph run` section before deleting `PRD/work/<slug>/`, and refuses the delete when a ledger exists and that section does not. Otherwise the proof that a run refused a pre-authorization survives only until the run succeeds — cleanup deletes the folder the ledger lives in. Verbatim rather than summarized: a summary of a refusal ledger is the driver grading its own compliance
  - the `define` node parks on any non-empty `PRD/sections/` diff — the complete diff and the new stable IDs under `## Open gate`, `STATUS.owner-action`, stop. Node 8 (`land`) was otherwise the first human touch, so code would exist against product truth no one had read; the 2026-08-17 leak is what that costs. The whole diff gates, not new `DEC-###` alone, because that leak wrote six requirements and a flow alongside its two decisions. A `define` node that writes only `DESIGN-BRIEF.md` never interrupts a run. This is the single place autonomy is traded for control, and it is the trade the workflow was asked for: the owner reviews product behavior, the run keeps branching, stashing, slicing, commits, and PR plumbing
  - `graph-gate-review` is the owner-facing half of that gate and the third graph skill: it walks the recorded diff one stable ID at a time, restating each in product terms before showing it, takes `accept` / `edit` / `reject` per item, records every verdict and reason under `## Gate verdicts`, then resolves the gate and restores the lifecycle-matched `STATUS.*` marker so `/graph-run` resumes. It lands in the seam the contract already describes — a package at `owner-action` parks again *unless* the recorded gate is resolved — rather than inventing a resume path. It never dispatches, never advances a node, and never writes `DESIGN-BRIEF.md`, `GAMEPLAN.md`, or slice docs; a `reject` reverts the ID but burns the number. A park with no way through it is friction that gets routed around, which is how a gate stops binding
  - every dispatch prompt `graph-run` emits carries an absolute `Working directory:` line and requires the node to propagate it into any prompt the node itself writes, and node 6's writes are asserted to lie inside `.worktrees/implement-<slug>/` or `PRD/work/<slug>/`. The rig fix in this decision constrains fixture reps; this constrains production runs, where the identical inheritance exists and a node fans out to its own subagents. The rig's before/after snapshot does not port — a real run is supposed to change the repository — so the write-scope assertion is its production equivalent rather than the same check reused
  - `graph-preflight` takes a lockfile at `.worktrees/.graph-run.lock` recording slug, run ID, PID, and start time; a second run refuses while it is held and names the holder, every terminal state releases it, and a lock whose PID is dead is reported stale rather than silently stolen. Two runs sharing one launch checkout is the same shared-working-directory hazard as the leak, with no isolation between them. Acquisition and staleness detection are a tested pure function
  - the predicate covers six skills, not four: `thejudge-implement-all` and `thejudge-cleanup` gain the `## Mode` section the phase skills carry, so their graph-mode behavior is declared and the cleanup fixture can measure it. `superpowers:requesting-code-review` at node 7 is not this repository's to gate
  - a graph run may `git merge` and `git pull`, provided it is not into the trunk and not forced. Integrating an updated base is ordinary mechanics, and denying it left a run unable to refresh its own worktree. `.claude/graph-profile.json` carries the two allows against 48 denies: the strategy overrides that discard one side (`-s ours`, `-X ours`, `-X theirs`, `--allow-unrelated-histories`, `git pull --force`) and every `main` / `master` push spelling reachable through the two permitted push allows. The limit is stated rather than hidden — a permission rule reads command text, and `git merge <ref>` names the branch merged *from*, never the branch merged *into*, so the target is the current checkout and no rule can see it. Enforcement is therefore at the push, not at the merge: a run can make a local merge into `main`, cannot publish it (push and force-push denied), and cannot hide it (`git reset --hard` and `git clean` denied). Node 8 (`land`) is unchanged — the pull-request merge stays the owner's, and `gh pr merge` / `gh pr close` stay denied. `graph-workflow-contract.md` carries the rule and its limit; `scripts/graph-preflight.test.mjs` holds five tests over it, including a false-positive check that a branch named `main-line-feature` stays pushable
- Related requirements:
  - (none — repository workflow and developer tooling only; no functional requirement is added or changed)
- Notes:
  - continues the tooling-discipline lineage of DEC-063, DEC-064, DEC-086, DEC-154, and DEC-155; the write-time guard plus `test:scripts` drift guard reuses exactly the pattern DEC-155 established for CI sub-check coverage
  - the general principle, stated once so later work inherits it: a boundary an agent can rationalize past is not a boundary. Convert it to a check, or record it honestly as convention rather than claiming enforcement (as DEC-163 does for `nohup`, the trailing `&`, and the `--settings` launch requirement)
  - four limits are recorded rather than converted, so a later reader does not mistake absence for oversight. (1) `graph-ledger-check.mjs` reads dispatch prompts and ledger rows that `graph-run` itself wrote, so a driver that pre-authorizes and then paraphrases its own dispatch prompt passes it clean — every other check here reads ground truth (source text, byte diffs, git state, process state), and this one reads a self-report, covering the failure that actually occurred. Honest closure is transcript-side or splitting ledger authorship from the driver; neither is in scope. (2) Node 7's review is not independent: `superpowers:requesting-code-review` reviews work from the same run that dispatches it, has no autonomous mode, and cannot be predicate-gated. (3) No cost or wall-clock budget bounds a whole run — only the per-node `gate-qc` (three) and `review` (two) caps. Accepted by the owner on 2026-08-17: a run consuming a full session window is not worth engineering around before the workflow runs end to end; revisit once the lock and the `define` gate are measured. (4) The env sentinel proves the profile file was loaded, not that any deny rule fired
  - the `define` gate is the one place this decision chooses a human over a check, and the reason is that no check can stand in for the judgment: a script can detect that `PRD/sections/` changed, but only the owner can say whether the product truth written there is the product they want. Every other boundary here is mechanical because the judgment was already settled and only compliance was in doubt
  - no `system-map.md` entry is added, for the same reason given in DEC-163
  - DEC-161 and DEC-162, REQ-146..151, and NFR-015 are occupied by the reverted leak preserved on `rescue/fixture-leak-card-collection-20260817`; DEC-163 and this decision skip past them, and later IDs resume at DEC-165 / REQ-152 / NFR-016, so nothing collides if that work is ever revived. See Q-005

### DEC-165
- Decision: TheJudge supports two agent runtimes, not three. `.cursor/` is deleted from the repository, `.claude/skills/` becomes the canonical skill tree edited directly, and `npm run skills:ai-sync` mirrors it to `.agents/skills/` for Codex. Supersedes DEC-115's canonical-editing clause (`.cursor/skills/thejudge-*` canonical, `.agents/skills/` and `.claude/skills/` both mirrors); DEC-115's response-discipline decision itself is unchanged and stays confirmed.
- Status: confirmed
- Context: The Cursor subscription was cancelled on 2026-08-17, so the canonical tree belonged to a runtime nobody runs — every skill edit went to a directory neither remaining agent reads, and reached Codex and Claude Code only by way of the sync script. That is a third copy to keep in step, a third tree in the protected-path set, and a third destination in every fixture and instruction path, all for a runtime with no user. Claude Code was chosen as canonical over the vendor-neutral `.agents/` name because it drives the graph workflow and already owns `.claude/` — the permission profile and settings the workflow depends on sit beside the skills that read them.
- Impact:
  - documentation, agent workflow, and developer tooling only; no product UI, API, prompt, provider, or data behavior changes
  - `.cursor/` is deleted whole: twenty skill files plus `.cursor/rules/playwright-mcp-cleanup.mdc`, whose rule is already carried by both `CLAUDE.md` and `AGENTS.md`, so no instruction is lost
  - canonical editing becomes `.claude/skills/<skill>/`; `.agents/skills/` is the mirror and not an edit target. Verification collapses to one command: `diff -rq .claude/skills .agents/skills` produces no output
  - twenty-four tracked files drop every Cursor reference and path. In the skill trees (four files edited in `.claude/skills/`, regenerated into `.agents/skills/` by the sync): `thejudge-kickoff/SKILL.md`, `thejudge-kickoff/reference.md`, `thejudge-map-out/SKILL.md`, `thejudge-refinement/SKILL.md` — the first, third, and fourth carrying the same `(Cursor / Claude Code)` command prefix. Elsewhere: `AGENT-SKILLS.md` (catalog table, the skill-authoring workflow, the command-prefix line, and the `.cursor/skills/` reference path), `README.md`, `PRD/README.md`, `PRD/instructions/skill-testing.md`, `PRD/instructions/graph-workflow-contract.md` including its "three synced trees" boundary, `PRD/instructions/doc-lifecycle.md`, `PRD/instructions/workflow-reference.md` (a command-prefix line of the same kind), `.claude/graph-profile.json`'s deny rules, and `.prettierignore`
  - the portable guide in `docs/prd-workflow-guide/` is scrubbed too rather than left generic — `START-HERE.md`, `04-skills.md`, `templates/AGENT-SKILLS.md`, `templates/README.md`, `templates/scripts/sync-agent-skills.sh`, and `templates/PRD/instructions/workflow-reference.md` name only Codex and Claude Code
  - `scripts/sync-agent-skills.sh` is repointed in the same change that deletes `.cursor/`, not left to the later Node port: its source becomes `$ROOT/.claude/skills` and the second `rsync` leg that wrote `.claude/skills/` is dropped, since that tree is now the source. Deleting `.cursor/` without it would leave `npm run skills:ai-sync` failing against a source directory that no longer exists. The `skills:ai-sync` script name in `package.json` is unchanged; only its implementation changes, and only when DEC-164's port lands
  - DEC-164's protected set narrows from three skill trees to two accordingly, and its `mirrorSkillTrees()` exemption is pinned to this source and destination
  - historical references to `.cursor/` in `PRD/instructions/receipts/*` are left exactly as written: receipts are durable history and are never rewritten to match later structure. `PRD/work/graph-workflow/PLAN-spine.md` carried the same kind of reference and went with the work folder at cleanup on 2026-08-20, so the receipts are now the only keep of that category
- Related requirements:
  - (none — repository workflow and developer tooling only; no functional requirement is added or changed)
- Notes:
  - a completeness grep must accept that some files keep the word by design, or it can never pass: this decision's own body and its `decisions.md` router row describe deleting `.cursor/`; DEC-115 records the superseded canonical clause; `decisions/conversation-ux.md` and `functional-requirements.md` name "Claude/ChatGPT/Cursor" as the chat-UI reference product behind DEC-126/DEC-127, which has nothing to do with the runtime; `PRD/instructions/receipts/*` is durable history; and the frontend's CSS `cursor:` properties are unrelated. Card data needs no exclusion — every hit there is *Precursor Golem*, which a word-boundary match does not see. The gate is `git ls-files | grep -i '\.cursor'` plus a `git grep -lwiI cursor` with those paths excluded by pathspec
  - no `system-map.md` entry is added, for the same reason given in DEC-163 and DEC-164

### DEC-166
- Decision: The graph workflow's safety boundaries move from a launch-flag permission profile into a committed `PreToolUse` hook in `.claude/settings.json`, which fires in every session and inside every dispatched subagent. The hook enforces two tiers — a universal tier always, and the graph-only tier while `.worktrees/.graph-run.lock` is held — and carries four further jobs: an owner kill switch at `.worktrees/.graph-stop` that halts at the node boundary with the terminal state written and the lock released; a per-node tool-call cap that parks on overrun; a default-FAIL slice criteria file whose entries cannot flip to `true` without an observed evidence read; and denial of the raw-Bash, `nohup`, and background-`&` writes no permission rule can reach. The hook is proven rather than assumed: `graph-preflight` refuses to start a run whose canary deny does not fire, and the driver rechecks at every node boundary that the hook is still firing, ending at `BLOCKED` rather than falling back to the profile. Separately, node 7 replaces `superpowers:requesting-code-review` with a fresh-context reviewer holding no write tools and grading against the slice's own acceptance criteria, and the no-pre-authorization rule is re-read from the contract before every node dispatch. `.claude/graph-profile.json` is retained as a second independent layer. Extends DEC-163 and DEC-164 without removing any boundary either established.
- Status: confirmed
- Context: DEC-164 converted the graph workflow's boundaries from prose into scripts, but left the enforcement layer itself conditional. `graph-profile.json` binds only in a session launched with `claude --settings .claude/graph-profile.json`; without that flag every entry is inert and the whole boundary list is convention. The contract's own protected-path reach table records raw Bash — `cp`, `rsync`, redirection — as enforcement "none". Two boundaries cannot fire even with the profile loaded: `nohup` is stripped as a wrapper before rules are matched, and a trailing `&` is consumed as a command separator before any rule sees the command text. Alongside that, node 7's reviewer graded work it had helped produce, the owner's only in-flight lever was Ctrl-C — which strands the lock and leaves the ledger mid-node — no run had any cost bound, node 6's `ok` was a self-report, and the no-pre-authorization rule lived only in files read once at run start, which is where a long run's compaction drops things. The hooks mechanism resolves the conditional half: hooks run in the application process rather than the context window, a `PreToolUse` hook can deny by exit code 2 with the reason returned to the agent, and hook input carries `agent_id` / `agent_type` because hooks fire inside subagents — which every graph node is. The profile is kept rather than deleted because it narrows the tool surface in ways a hook does not, and two independent layers are cheaper than re-testing roughly 120 deny rules as hook code.
- Impact:
  - agent workflow, developer tooling, and repository configuration only; no product UI, API, prompt, provider, or data behavior changes
  - `.claude/settings.json` is created — it does not exist in this repository today, which has only the personal, gitignored `settings.local.json`
  - the contract's protected-path reach table loses its raw-Bash "none" row, because the hook covers that mechanism; the env sentinel and the ledger's `Profile:` field survive as informational rather than load-bearing, since a run no longer depends on the profile having loaded
  - the two convention-only boundary notes — `nohup` and trailing `&` — are retired from the contract, because the hook reads command text the rule matcher never sees
  - the contract's stray line naming `graph-ui-shape` and `graph-enrich-define` as domain node packs is retired with them: never built, referenced exactly once, and the only frontend/backend split anywhere in the workflow. `PRD/work/graph-single-door-workflow` records the same deletion as belonging to this work, so it is claimed here and not there
  - a hook that is not firing is refused rather than worked around: `graph-preflight` proves the hook with a canary call the universal tier must deny and ends the run at `BLOCKED` if the deny does not come, and the driver confirms at every node boundary that the hook's counter advanced during the node just finished. Nothing falls back to the profile, which the contract already directs treating as absent when unverified
  - the hook script and `.claude/settings.json` join the protected set, so a run cannot weaken its own enforcer
  - the graph tier is gated on `.worktrees/.graph-run.lock` so that always-on enforcement does not block skill authoring, `CLAUDE.md` edits, or settings edits in ordinary sessions, which is the cost of committing the hook rather than passing it at launch
  - the node table gains a per-node tool-call cap column; an overrun parks as `PARKED` and adds no fifth terminal state, because `graph-run`'s `## Terminal states` table stays the single authority and a second list drifts
  - the cap is a budget for one dispatch of a node, not a per-run total for it, so the counter is keyed by run id, node, and attempt. A node re-entered by a loop-back starts a fresh budget; the contract's existing loop caps — at most three FAIL returns to `define`, at most two returns to `build` — stay the only bound on how many times a node is dispatched, and the cap adds no third loop limit. A per-run total would instead starve the retry and park it before it did any work, for a reason unrelated to runaway behavior
  - three `.worktrees/` records keep one writer each, so no mechanism can corrupt another's state: `graph-preflight` alone writes `.graph-run.lock`, `graph-run` alone writes `.graph-run-state.json` (the current node, written immediately before each dispatch, which is how a stateless `PreToolUse` hook knows which node to count against), and the hook alone writes `.graph-node-calls.json` and the observed-evidence log. The current node is deliberately not folded into the lock record: the lock is the concurrency guard, an unreadable lock is a hard blocker for the next run, and a per-dispatch rewrite would put that guard at risk at every node boundary
  - `thejudge-map-out` emits a default-FAIL criteria file per slice, derived from the slice doc's stated acceptance criteria rather than hand-maintained beside it. Each criterion carries an `evidence` block — a command pattern, file paths, or both — authored beside the criterion in the slice doc, and that block is what maps an observed tool call to a criterion; without it the hook has no way to know which read belongs to which criterion. A criterion with no machine-checkable evidence is marked `manual`, and its evidence event is a dated observation line naming the criterion id, written by the agent and observed by the hook
  - `AGENT-SKILLS.md`'s node-7 row and the graph catalog's launch-flag instructions change; the "launch graph runs with `--settings`" guidance stops being the thing enforcement depends on
- Related requirements:
  - REQ-152
  - REQ-153
  - REQ-154
  - REQ-155
  - REQ-156
  - REQ-157
  - REQ-158
  - REQ-159
  - NFR-016
  - FLOW-020
- Notes:
  - the caps are tool calls, not spend. The Agent SDK's `max_turns` and `max_budget_usd` do not apply here — `graph-run` is a skill dispatching subagents inside an interactive session, not an SDK program — and dollar cost is not observable to a hook. A cap on calls catches the runaway shape that actually burns a run: a node looping on itself
  - whether a hook deny survives `permission_mode: "bypassPermissions"` is not explicitly documented. It is stated as an open measurement rather than asserted, and a slice verifies it against the running binary before the contract claims it
  - mid-run steering — the `STEER.md` half of the kill-switch pattern — is deliberately excluded. Every line it carried would be a user instruction arriving inside an autonomous run, needing an `## Instruction ledger` row classified `answered-once` or `refused`, which is the whole surface of a separate package
  - `scripts/graph-ledger-check.mjs` stays a schema check over inputs `graph-run` writes itself. REQ-157 closes that shape for node 6's slice criteria; it does not close it for the ledger, and the contract's existing statement of that limit stands
  - a `manual` criterion's evidence proves the check happened, not that it passed: the hook observes the write, and the content of the observation line is still the agent's word. Stated as a named limit rather than closed, in the same spirit as the raw-Bash row this decision retires
  - the new stable IDs skip the block DEC-164 and Q-005 reserve. REQ-146..151, NFR-015, and FLOW-019 remain occupied by the reverted 2026-08-17 leak preserved on `rescue/fixture-leak-card-collection-20260817`; this decision resumes at REQ-152, NFR-016, and FLOW-020 as DEC-164's note directs, running through REQ-159
  - no `system-map.md` entry is added, for the same reason given in DEC-163, DEC-164, and DEC-165
  - **Shipped 2026-08-20** (PR #93, merge `b83a7f9`, nine slices A–I). The impact
    list above held. Six deltas worth recording, none of which changed the
    decision:
    - `test:scripts` ran `node --test scripts/*.test.mjs`, which does not reach
      `scripts/lib/`. The glob was widened so the new pure-module tests actually
      run; without it they would have existed without ever executing
    - the hook also denies removal of `.worktrees/.graph-stop` while the lock is
      held. The kill switch is defeated without it, and it is lock-gated, so the
      owner clearing the sentinel to resume is unaffected
    - the per-node caps are estimates, not measurements. No completed
      `GRAPH-RUN.md` ledger existed to size them against, so only `build` rests
      on an observation — this package's own implementation session, ~15 tool
      calls per slice. Recorded as estimates to re-tune once real ledgers exist
    - node 7's reviewer found four defects on its first outing, three real and
      fixed with regression tests: `&` inside a redirection read as a background
      launch (so `2>&1` was denied under a live lock), the secrets rule denying
      any *mention* of the path rather than access to it, and `git -C <path>
      push --force` slipping past a rule keyed on `argv[1]`. The reviewer earned
      its place immediately
    - `mv` belongs to the destructive-command set, not the copy set: a move
      writes its destination *and* removes its source
    - slices A–E's criteria files carry `true` without evidence-log entries,
      because they were verified before the mechanism existed. Stated in the
      slice doc rather than hidden; the enforcement is proven by a live round
      trip on a throwaway slice, and it binds from the next package mapped out
  - **The `bypassPermissions` measurement resolved**, as this decision's note
    above required. On 2026-08-20, `claude` 2.1.234, a session run with
    `--permission-mode bypassPermissions` had `pkill -f
    definitely-no-such-process-xyz` **denied** by the hook. Recorded in the
    contract as that measurement — one command, one moment, one binary version —
    and explicitly not as a property of the harness

### DEC-167
- Decision: `graph-run` becomes TheJudge's single intake door. `--branch` becomes optional and is derived as `thejudge-auto/<slug>` from a door-proposed slug that node 2 then reuses; the door accepts context documents, copying them verbatim into `PRD/work/<slug>/intake/` as evidence that never binds refinement; node 2 links prior shipped runs from the receipts corpus; a request too thin to package ends the run at `BLOCKED`; and cleanup folds intake into the receipt. `thejudge-prepare` is retired as an entry point but keeps its skill, its contract, and its controlling predicate. Extends DEC-163 and DEC-166 without changing the node table, the `define` gate, or any boundary; the one definition it amends is `graph-run`'s `BLOCKED`, widened to cover a request too thin to package. The package is implemented in an ordinary session, because a graph run may not edit `thejudge-*` skills.
- Status: confirmed
- Context: Two orchestrators — `thejudge-prepare` and `graph-run` — both turn one request into one package, so choosing between them is the last nuanced per-step decision the owner makes at intake. Three narrower gaps sit beside it. `--branch <name>` is mandatory and never inferred, so the owner invents a branch name before describing the thing, and because `graph-preflight` is node 1 while the slug is proposed at node 2 by `thejudge-kickoff`, branch and slug are decided independently and can disagree. No skill reads `PRD/instructions/receipts/`, so a run against shipped code starts blind to the receipt documenting how that code was built. And `NO ACTIONABLE PACKAGE`, which `thejudge-kickoff` can return, appears nowhere in `graph-run` or the contract, leaving the driver with an outcome it has no rule for. The intake gap is recorded rather than hypothetical: the `graph-run-boundary-enforcement` receipt states that all six of that package's findings came from `docs/whatIsGraph/graph-hardening-handoff.md`, that the promotion checklist expected the file marked closed or retired, and that cleanup could do neither because it is untracked local material outside the repository — a shipped package whose evidence chain points at a file the repository does not contain. Uniform depth needs no grading mechanism: `graph-run` already advances straight to `gate-qc` on an empty `PRD/sections/` diff, so a change touching no product truth costs tokens and wall-clock rather than owner attention.
- Impact:
  - `graph-run` accepts a bare request; `--branch <name>` becomes optional, is used verbatim when supplied, and is otherwise derived
  - the door proposes the slug from the request and any intake material before node 1, uses `thejudge-auto/<slug>` as the branch, and passes that slug to node 2, which uses it rather than proposing a second name — resolving an ordering problem, since node 1 needs a branch and node 2 is where slugs are born
  - the door accepts file paths or markdown pasted in the same message, and writes each item verbatim into `.worktrees/.graph-intake/<run-id>/` before node 1 is dispatched. Node 2 reads it there and, once `thejudge-kickoff` has created `PRD/work/<slug>/`, copies it into `intake/`, commits it on the branch, and deletes the staging copy — so intake travels with the branch
  - staging is outside the working tree because node 1 resolves the working tree before the branch exists: `scripts/graph-preflight.mjs` counts untracked files and stashes with `git stash push -u`, which would carry both the intake and its untracked source away before node 2 could read either. `.worktrees/` is gitignored and `git stash push -u` spares ignored paths
  - intake is evidence and never authority: it may state findings, mark matters settled, and propose a slug, but every product decision is still made with the owner at the `define` gate. Material that could bind refinement would route product truth around the gate
  - intake citations are recorded and not fetched; transitive following is unbounded
  - node 2 greps `PRD/instructions/receipts/`, already named `<slug>-<date>.md`, and writes one `## Prior run` line per match into `IDEA.md` — a flat list of matches, not a chain walk, because receipts carry no parent pointer
  - `graph-run` gains an edge for `NO ACTIONABLE PACKAGE` returned by node 2, terminating at `BLOCKED`. It cannot be `PARKED`: parking means resuming from a recorded gate, and a thin request leaves no artifact to resume from — recovery is a new run. Mechanically the same point holds, on the intake path too: parking needs a package folder for `## Open gate`, a `STATUS.*` marker, and a board row, and intake stays staged outside the working tree until node 2 has created the package folder, so none of the three exists. No fifth terminal state is added, but `graph-run`'s terminal-state paragraph is amended: as written `BLOCKED` is an external condition outside the repository, `PARKED` is anything requiring a human decision, and the tiebreak is to park, so a thin request — neither external nor outside the repository — would fall to `PARKED` and have nothing to park against. The amendment widens `BLOCKED` to cover it
  - a `BLOCKED` of that kind leaves the `thejudge-auto/<slug>` branch node 1 pushed. The run does not delete it — `graph-preflight` forbids tidying a failed run and node 1 may have auto-committed real work onto it — so the report names the branch, whether node 1 committed or stashed, and the staging path, and the recovery action names an explicit `--branch` for the retry, since the same description derives the same slug and would hit exit-code-2
  - `thejudge-cleanup` writes an `## Intake` section naming each intake file and its stated origin, so deleting `PRD/work/<slug>/` loses nothing traceable. This discharges the first follow-up on `receipts/graph-run-boundary-enforcement-2026-08-20.md`
  - `AGENT-SKILLS.md` stops naming `thejudge-prepare` as an entry point in two specific places: the `## Workflow sequence` mermaid diagram, whose two `prepare` edges become `graph-run` edges so the diagram shows one door, and the skill-catalog `When` cell, which stops reading as an intake route. Its `## Graph workflow skills` paragraph is corrected from two `graph-*` skills to three, matching its own table. `graph-workflow-contract.md` gains `graph-run` as the named entry point; it never named `thejudge-prepare` as one, so nothing is removed there — its six `thejudge-prepare` mentions are the controlling predicate and README section ownership. The skill file, `preparation-contract.md`, and the `thejudge-prepare is controlling` predicate in all six phase skills are unchanged — this retires a door, not a contract
  - a new issue against an `active`, already-mapped-out package still goes to `thejudge-amend`; a second package against the same code would give two branches touching the same files
  - all 14 skills remain callable and none is deleted; deletion is revisited only after the flow has run several times
  - no change to the node table, models, caps, loop limits, the `define` gate's trigger, or any boundary in `## Boundaries`. The single exception to "nothing else moves" is the widened `BLOCKED` definition above, which is a terminal-state definition and not a boundary
  - the package is implemented in an ordinary session rather than by a graph run. It edits `thejudge-kickoff`, `thejudge-refinement`, and `thejudge-cleanup`, and `## Boundaries` forbids a graph run from modifying any `thejudge-*` skill in either synced tree, with `.claude/graph-profile.json` denying the `Edit` for both trees. A graph run would end `PROMPTED` on most of these slices. The boundary is left exactly as written on purpose: it exists so an autonomous run cannot rewrite the skills that govern it, and this package is precisely that case
- Related requirements:
  - REQ-160
  - REQ-161
  - REQ-162
  - REQ-163
  - REQ-164
  - FLOW-021
  - FLOW-022
  - DEC-163
  - DEC-166
- Notes:
  - the idea's original grounded claim that a stray `graph-ui-shape` / `graph-enrich-define` line still needed deleting is discharged: DEC-166 claims that deletion explicitly
  - "evidence, not authority" is a contract rule, not an enforced one. No mechanism prevents refinement adopting an intake claim wholesale; what catches it is the `define` gate, which parks on any resulting `PRD/sections/` diff for the owner to walk one stable ID at a time
  - no size gate is placed on intake, because a gate would refuse exactly the thorough handoff document this accepts. Node 1's own thresholds — 10 files, 200 changed lines — are a size gate the first pass of this design did not account for; staging outside the working tree is what keeps them off the intake path
  - `docs/whatIsGraph/` is deliberately not committed here; sweeping untracked working material into the repository stays the owner's call
