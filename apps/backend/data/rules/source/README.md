# Official Rules Source

Place the official Magic Comprehensive Rules TXT file here as `MagicCompRules.txt`, then run:

```sh
npm run rules:build
```

The generated backend metadata is written to `apps/backend/data/rules/rulesMetadata.json`.
