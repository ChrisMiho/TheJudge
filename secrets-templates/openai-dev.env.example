# Copy to repository root: .secrets/openai-dev.env
# mkdir -p .secrets && cp secrets-templates/openai-dev.env.example .secrets/openai-dev.env
# Edit the copy only; never commit files under .secrets/ (see PRD/instructions/secrets-handling.md).

OPENAI_API_KEY=
